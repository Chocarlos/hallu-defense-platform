# Traceability Matrix

Statuses: `not_started`, `designed`, `implemented`, `tested`, `documented`, `accepted`.

No item may be marked `accepted` without implementation, tests, documentation, and evidence.

| ID | Description | Files / modules affected | Related contracts | Related endpoints | Tests required | Evidence of validation | Status | Risks / notes |
|---|---|---|---|---|---|---|---|---|
| FND-001 | Root agent instructions exist and define working loop | `AGENTS.md`, `scripts/ci/check_foundation_docs.py`, `apps/api/tests/test_foundation_docs.py` | n/a | n/a | foundation docs validator, focused negative tests | `check_foundation_docs.py` validates required AGENTS working-loop, non-negotiable, architecture, and command markers; `test_foundation_docs.py`: 6 passed | tested | Must keep updated as workflow evolves |
| FND-002 | Master product plan exists | `docs/PLAN_MASTER.md`, `scripts/ci/check_foundation_docs.py`, `apps/api/tests/test_foundation_docs.py` | all | all | foundation docs validator, focused negative tests | `check_foundation_docs.py` validates master plan product goal, surfaces, mandatory stack, public contracts, verification pipeline, milestones, and required contract markers; `test_foundation_docs.py`: 6 passed | tested | Plan is high-level, not acceptance evidence |
| FND-003 | Traceability matrix tracks every prompt requirement | `docs/TRACEABILITY_MATRIX.md`, `scripts/ci/check_traceability_matrix.py`, `apps/api/tests/test_traceability_matrix.py`, `Makefile`, `.github/workflows/ci.yml` | all | all | traceability validator, focused negative tests | `check_traceability_matrix.py` validates 163 requirement rows, status vocabulary, table shape, unique IDs, accepted-row evidence, and Makefile/CI wiring; `test_traceability_matrix.py`: 6 passed | tested | Needs continuous maintenance; gate validates matrix structure and evidence hygiene, not semantic completeness of every product requirement |
| FND-004 | Worklog records commands and outcomes | `docs/WORKLOG.md`, `scripts/ci/check_worklog.py`, `apps/api/tests/test_worklog.py`, `Makefile`, `.github/workflows/ci.yml` | n/a | n/a | worklog validator, focused negative tests | `check_worklog.py` validates dated headings, non-empty entries, latest-entry required sections, validation evidence, and Makefile/CI wiring; `test_worklog.py`: 6 passed | tested | Gate validates format and latest-entry evidence hygiene; it does not semantically audit every historical entry |
| FND-005 | Required ADRs exist | `docs/ADR/*.md`, `scripts/ci/check_foundation_docs.py`, `apps/api/tests/test_foundation_docs.py` | n/a | n/a | foundation docs validator, focused negative tests | `check_foundation_docs.py` validates 7 ADR files and required architecture, data-plane/control-plane, verification-pipeline, security-model, sandbox-model, and policy-engine ADR topics with status/context/decision/consequences sections; `test_foundation_docs.py`: 6 passed | tested | Existing older ADR remains informational |
| FND-006 | Monorepo skeleton exists | `apps`, `packages`, `infra`, `evals`, `scripts`, `package.json`, `apps/api/pyproject.toml`, `scripts/ci/check_foundation_infra.py`, `apps/api/tests/test_foundation_infra.py`, `Makefile`, `.github/workflows/ci.yml` | all | all | foundation infrastructure validator, focused negative tests | `check_foundation_infra.py` validates 29 required app/package/infra/eval/script paths, root Node workspaces, root npm scripts, and API Python project metadata; `test_foundation_infra.py`: 7 passed | tested | Static skeleton gate does not prove runtime service availability |
| FND-007 | Standard task runner exists | `Makefile`, `scripts/ci/check_foundation_infra.py`, `apps/api/tests/test_foundation_infra.py`, `.github/workflows/ci.yml` | n/a | n/a | foundation infrastructure validator, focused negative tests | `check_foundation_infra.py` validates 16 standard Makefile targets, `.PHONY` coverage, virtualenv Python selection, and expected command markers for lint/typecheck/test/build/contracts/OpenAPI/policy/sandbox/evals/security/docs gates; `test_foundation_infra.py`: 7 passed | tested | Host still lacks `make`/`nmake`; this gate validates target definitions and equivalent commands continue to be run directly |
| FND-008 | Docker Compose includes required local services | `docker-compose.yml`, `infra/prometheus/prometheus.yml`, `infra/grafana/**`, `infra/otel/otel-collector-config.yaml`, `infra/rag/**`, `scripts/ci/check_local_runtime_config.py`, `apps/api/tests/test_local_runtime_config.py`, `Makefile`, `.github/workflows/ci.yml`, `infra/security/keycloak/realm-hallu-defense.json` | n/a | n/a | compose config validation, local runtime config validator, RAG persistence config validator, focused negative tests, live local smoke | Compose includes API, console, Postgres/pgvector with pgvector init migrations for RAG evidence chunks and corpus grants, Redis, MinIO, OpenSearch, Prometheus scrape config, Grafana provisioning, and OpenTelemetry collector config; `check_rag_persistence_config.py` validates OpenSearch service wiring; `check_local_runtime_config.py` validates 9 services and 3 volumes; `test_local_runtime_config.py`: 9 passed; `docker manifest inspect minio/minio:RELEASE.2025-09-07T16-13-09Z` verified the pinned MinIO tag; `docker compose config --quiet` passed; `docker compose up -d --build` started the full stack with a temporary port override because host port 8000 was occupied; API `/health` and `/metrics`, console, Prometheus readiness, Grafana health, MinIO health, OpenSearch HTTP, Postgres `pg_isready`, and Redis `PING` all passed; `docker compose down --remove-orphans` cleaned up containers; Batch 2 adds a `keycloak` service (quay.io/keycloak/keycloak:26.3, `start-dev --import-realm`, host port 8081) importing `realm-hallu-defense.json`, extending `_validate_keycloak` coverage so the gate reports 10 services and 3 volumes (`test_local_runtime_config.py`: 14 passed) with `docker compose config --quiet` still passing; the realm underpins the SEC-014 live OIDC smoke | tested | Runtime smoke used temporary host ports `18000`/`13000` because local process `Manager` occupied `8000`; K8s/Helm/Terraform remain unverified locally; the Batch 2 keycloak service adds a local OIDC provider whose live token mint was verified independently, with the full keycloak-live job running in CI |
| FND-009 | Initial CI exists | `.github/workflows/ci.yml`, `.github/workflows/security.yml`, `.github/workflows/evals.yml`, `scripts/ci/check_foundation_infra.py`, `apps/api/tests/test_foundation_infra.py`, `Makefile` | all | all | foundation infrastructure validator, focused negative tests | `check_foundation_infra.py` validates `ci.yml`, `security.yml`, and `evals.yml` exist and that `ci.yml` wires backend tests, OpenAPI/docs/traceability/worklog/foundation infra gates, policy/security/RAG/Grafana checks, and TypeScript typecheck/test/build; `test_foundation_infra.py`: 7 passed | tested | Local validator proves workflow wiring, not remote GitHub Actions execution |
| FND-010 | Security CI exists | `.github/workflows/security.yml` | n/a | n/a | secret scan, auth config, OIDC provider smoke, encryption config, secrets config, audit ledger config, approval queue config, corpus grants config, sandbox isolation config, Python dependency audit, npm audit, container image scan | `secret_scan.py` passed; `check_auth_config.py` validates auth/RBAC production defaults and CI wiring; `oidc_provider_smoke.py` is wired into the security workflow and skips by default unless deployed-provider URL/JWT env vars are supplied; `check_encryption_config.py` validates the encryption baseline; `check_secrets_config.py` validates the Vault-compatible secrets baseline; `check_audit_ledger_config.py` validates the audit persistence gate; `check_approval_queue_config.py` validates approval persistence wiring; `check_corpus_grants_config.py` validates corpus grant persistence and lifecycle wiring; `check_sandbox_isolation_config.py` validates Docker sandbox isolation wiring; `python_dependency_audit.py` passed after upgrading vulnerable `pytest`; `npm audit --omit dev` 0 vulnerabilities; security workflow builds and scans API, console, and sandbox images with Trivy and local validator verifies the gate | tested | Runtime Trivy image scan remains GitHub Actions evidence; live OIDC provider smoke and live Docker sandbox smoke execution require the live CI lane or local Docker |
| FND-011 | Evals CI exists | `.github/workflows/evals.yml` | n/a | n/a | eval smoke, expanded offline scenario evals | `evals/runners/smoke.py` passed for 2 scenarios and wrote `evals/reports/smoke-metrics.json`; `evals/runners/scenarios.py` passed for 21 scenarios and wrote `evals/reports/scenario-metrics.json`; evals workflow runs both runners | tested | Scenario coverage now spans documents, code-agent test claims, false repo file/function claims with sandbox inspection, semantic implementation/fix claims with changed-line and command evidence, tools, sandbox abuse, policy-level and content-level prompt injection, and data poisoning; richer semantic analyzers remain future work |
| FND-012 | Claude Fable delegation path exists for scoped teammate work | `.claude/agents/fable-platform-engineer.md`, `.claude/workflows/fable-delegate.js`, `.gitignore`, `docs/development/fable-delegation.md`, `docs/development/fable-project-brief.md`, `docs/development/fable-prior-session-report.md`, `docs/development/fable-enterprise-batch.md`, `apps/api/tests/test_fable_delegation_config.py` | n/a | n/a | git HEAD/worktree validation, Fable workflow probe, saved workflow syntax check, context-read probe, batch delegation launch, static delegation config tests | Initial repository baseline commit `8dec1b3` resolves `HEAD`; pre-refresh verified delegation commit is `4ac9df5`; `git rev-parse --verify HEAD` and `git rev-parse --verify fable5/delegation` both resolved `4ac9df52d37c5999c6aaae7c567c124e51b9a026` before the coordination refresh; `.claude-fable-work/`, `.codex-fable-work/`, `.claude/worktrees/`, and `.claude/settings.local.json` are ignored; persistent branch/worktree `fable5/delegation` exists at `.claude/worktrees/fable5-delegation` and must be kept aligned with `master` before write-mode delegations; `node --check .claude/workflows/fable-delegate.js` passed; inline Claude Code workflow probe `wf_55d0feb2-b18` returned `model=claude-fable-5`, `headResolves=true`, `shortHead=8dec1b3`, `agentsMdVisible=true`, and clean worktree; saved workflow probe `wf_6874c64f-008` returned `success=true`, `mode=read`, clean isolated worktree, required docs visible, and no changed files; context-package workflow probe `wf_e4e6e96f-aa3` returned `success=true`, `projectBriefRead=true`, `priorSessionReportRead=true`, `acceptanceMet=true`, `HEAD=4da8223`, and no changed files; direct `mcp__claude_code.Agent` retry with `model=fable`, `mode=auto`, and worktree isolation failed with `Agent type 'general-purpose' not found`; batch workflow `wf_6e5f935f-e44` launched five parallel Fable scopes for RAG, NLI/provider, console workflows, runtime gates, and contract drift; `test_fable_delegation_config.py`: 4 passed and proves write-mode context guards, required context injection, Fable model/worktree isolation, persistent branch docs, and batch backlog docs | tested | Direct auto-permission Fable route remains unavailable in this session; use workflow-based Fable batches and treat blocked/no-diff summaries as non-integrable until Codex validates actual diffs |
| CTR-001 | `Claim` Pydantic contract | `apps/api/src/hallu_defense/domain/models.py` | Claim | `/claims/*` | contract tests | current tests partial | tested | JSON Schema sync needs broader tests |
| CTR-002 | TypeScript contract surface | `packages/contracts/src/index.ts` | all exported interfaces | n/a | TS typecheck, schema coverage check | `npm run typecheck`; `check_json_schemas.py` verifies 42 exported TS interfaces have schemas | tested | Generated sync not yet implemented |
| CTR-003 | `Claim` JSON Schema | `packages/contracts/schemas/claim.schema.json`; examples | Claim | n/a | schema/example validation | `check_json_schemas.py`: 36 schemas, 36 valid examples, 36 invalid examples, 36 TS interfaces | tested | Pydantic accepts some omitted defaults that JSON Schema now requires |
| CTR-004 | `Evidence` Pydantic/TS/JSON Schema | API/models/contracts/schemas/examples | Evidence | `/evidence/retrieve` | contract tests | schema/example validation passed | tested | Persistent evidence store pending |
| CTR-005 | `ClaimVerdict` Pydantic/TS/JSON Schema | API/models/contracts/schemas/examples | ClaimVerdict | `/claims/verify` | contract tests | schema/example validation passed | tested | Status/action vocabulary mismatch with prompt needs v2 mapping |
| CTR-006 | `VerificationRun` Pydantic/TS/JSON Schema | API/models/contracts/schemas/examples | VerificationRun | `/verification/run`, `/audit/export` | e2e/schema tests | schema/example validation and smoke/API tests passed | tested | Schema uses refs to Claim/Evidence/Verdict |
| CTR-007 | `ToolCallEnvelope` Pydantic/TS/JSON Schema | API/models/contracts/schemas/examples/MCP Ajv runtime validator | ToolCallEnvelope | `/tools/*` | tool/schema tests | schema/example validation, API unit tests, MCP runtime schema validation, and approved execution grant validation passed | tested | Nested tool-specific input schemas are still interpreted by backend validator |
| CTR-008 | `SandboxRun` Pydantic/TS/JSON Schema | API/models/contracts/schemas/examples | SandboxRun, Evidence | `/repo/checks/run` | sandbox/schema tests | schema/example validation passes with typed `evidence`; sandbox tests prove command and inspection evidence are emitted | tested | Public contract is manually synchronized; code generation still pending |
| CTR-009 | `ErrorResponse` public error contract | API/models/contracts/OpenAPI/schemas/examples | ErrorResponse | all REST endpoints | error contract/schema tests | error tests and schema/example validation passed | tested | Error JSON Schema now exists |
| CTR-010 | `AuditEvent` and `AuditExportResponse` contracts | API/models/contracts/SDK/schemas/examples/audit ledger | AuditEvent, AuditExportResponse | `/audit/export` | audit/schema tests, persistent ledger tests | audit tests, SDK exportAudit test, schema/example validation, and JSONL audit ledger persistence/redaction tests passed | tested | JSONL backend is local-file persistence; production database/object-store ledger integration remains future work. Batch 1 adds a PostgreSQL ledger backend (PY-019) behind the same contract; live-DB integration remains Docker/CI |
| CTR-011 | `DocumentInput` TS/JSON Schema contract | contracts/schemas/examples/MCP validator | DocumentInput | `/evidence/retrieve`, `/verification/run` | schema examples, MCP tests | `check_json_schemas.py`: 16 schemas/examples passed; MCP validates document inputs with shared schema | tested | Pydantic defaults still allow omitted authority outside public schema examples |
| CTR-012 | `VerificationRunRequest` TS/JSON Schema contract | contracts/schemas/examples/MCP validator | VerificationRunRequest | `/verification/run` | schema examples, MCP tests | schema/example validation passed; MCP `repair_response` validates request with shared schema | tested | Pydantic allows defaults for optional fields; schema requires only `message_text` |
| CTR-013 | `EvidenceRetrievalRequest`/`EvidenceRetrievalResponse` TS/JSON Schema contracts | contracts/schemas/examples/MCP validator | EvidenceRetrievalRequest, EvidenceRetrievalResponse | `/evidence/retrieve` | schema examples, MCP request/output tests | schema/example validation passed for request and response; MCP validates retrieval input before proxying and validates retrieval output before returning `structuredContent`; SDK uses the typed request/response pair | tested | Contract coverage is structural; live persistent retrieval behavior remains tracked under RAG rows |
| CTR-014 | `ToolValidationResponse` TS/JSON Schema contract | contracts/schemas/examples/MCP validator | ToolValidationResponse | `/tools/validate-input`, `/tools/validate-output` | schema examples, MCP output tests | schema/example validation passed; MCP validates pre/post tool validation outputs | tested | Tool-specific nested input schema evaluation remains backend logic |
| CTR-015 | `PolicyEvaluationRequest` TS/JSON Schema contract | contracts/schemas/examples/MCP validator | PolicyEvaluationRequest | `/policy/evaluate` | schema examples, MCP negative tests | schema/example validation passed; MCP rejects missing `action` before proxying | tested | Runtime OPA adapter integration pending |
| CTR-016 | `PolicyEvaluationResponse` TS/JSON Schema contract | contracts/schemas/examples/MCP validator | PolicyEvaluationResponse | `/policy/evaluate` | schema examples, MCP output tests | schema/example validation passed; response now requires body `trace_id`; MCP validates and returns `explain_policy` output without wrapper duplication | tested | Public contract is still manually synchronized across Pydantic/TS/JSON Schema |
| CTR-017 | `RepoChecksRunRequest` TS/JSON Schema contract | contracts/schemas/examples/MCP validator | RepoChecksRunRequest | `/repo/checks/run` | schema examples, MCP negative tests | schema/example validation passed; MCP rejects empty command arrays before proxying | tested | Sandbox enforcement gaps remain tracked under SBOX items |
| CTR-018 | `SourceSpan` and `Freshness` TS/JSON Schema contracts | contracts/schemas/examples | SourceSpan, Freshness | Claim/Evidence nested fields | schema examples, TS/schema coverage | schema/example validation and TS coverage check passed | tested | Existing `Claim`/`Evidence` schemas still inline equivalent shapes instead of `$ref` |
| CTR-019 | Claim extraction/classification/verification endpoint schemas | contracts/schemas/examples, TS contracts | ClaimExtraction*, ClaimClassification*, ClaimVerification* | `/claims/extract`, `/claims/classify`, `/claims/verify` | schema examples, OpenAPI presence tests | 6 claim wrapper schemas validate with examples and TS coverage | tested | Dedicated endpoint behavior tests can be expanded beyond contract shape |
| CTR-020 | Evidence retrieval request and response schemas | contracts/schemas/examples, TS contracts | EvidenceRetrievalRequest, EvidenceRetrievalResponse | `/evidence/retrieve` | schema examples, MCP live tests | request/response schemas validate with `metadata_filter`; MCP live test exercises retrieval response contract | tested | Persistent corpus and external hybrid backend remain pending |
| CTR-021 | Response repair and audit export request/response schemas | contracts/schemas/examples, TS contracts | ResponseRepair*, AuditExport* | `/response/repair`, `/audit/export` | schema examples, API/audit tests | repair/audit schemas validate; audit export endpoint tests pass | tested | Response repair behavior still needs deeper semantic tests |
| CTR-022 | TypeScript to JSON Schema coverage gate | `scripts/ci/check_json_schemas.py`, contracts | all exported TS interfaces | n/a | contract coverage script | `check_json_schemas.py` fails if exported TS interfaces lack matching JSON Schema; latest focused run validated 57 schemas, 57 valid examples, 57 invalid examples, and 57 TypeScript interfaces, including eval smoke, expanded scenario report, scenario history, replay, and retrieval request/response contracts | tested | Coverage checks names and examples, not semantic equivalence of every property |
| CTR-023 | Approval workflow contracts | API/models/contracts/schemas/examples/SDK/approval queue | ApprovalRecord, ApprovalExecutionGrant, ApprovalList*, ApprovalDecision*, ToolValidationResponse.approval_id | `/approvals/list`, `/approvals/decide`, `/tools/validate-input` | schema examples, API tests, SDK tests, persistent queue tests | approval schemas/examples validate; `ApprovalDecisionRequest.decided_by` is optional/deprecated for API callers; high-risk API flow proves reviewer role enforcement, body spoof ignored, and persisted reviewer ID from request principal; SDK approval methods pass tests; JSONL approval queue tests cover reload, tenant isolation, redaction, repeated-decision blocking, grant hashing, grant expiration, and single-use grant consumption | tested | JSONL backend is local-file persistence; deployed distributed queue storage remains future work. Batch 1 adds a PostgreSQL approval-queue backend (PY-020) replaying the same contract with atomic decide-once/consume-once guards; live-DB storage remains Docker/CI |
| CTR-024 | Document ingestion request/response contracts | API/models/contracts/schemas/examples/SDK/MCP | DocumentIngestionRequest, DocumentIngestionResponse | `/documents/ingest`, MCP `ingest_documents` | schema examples, API tests, SDK tests, MCP tests | 44 JSON schemas/examples validate; Pydantic and TypeScript expose ingestion contracts; SDK and MCP live tests call `/documents/ingest` with trace and tenant propagation | tested | Local backend returns explicit non-persistence warning until a persistent backend is configured |
| CTR-025 | Corpus grant registry contracts | API/models/contracts/schemas/examples/SDK | CorpusGrant, CorpusGrantUpsertRequest, CorpusGrantDisableRequest, CorpusGrantListRequest, CorpusGrantHistoryRequest, CorpusGrantHistoryDiff, CorpusGrantHistoryDiffRequest, CorpusGrantResponse, CorpusGrantListResponse, CorpusGrantHistoryResponse, CorpusGrantHistoryDiffResponse | `/rag/corpus-grants/upsert`, `/rag/corpus-grants/disable`, `/rag/corpus-grants/list`, `/rag/corpus-grants/history`, `/rag/corpus-grants/history/diff` | schema examples, API tests, SDK tests | Pydantic, TypeScript, JSON Schema, and examples expose tenant-scoped corpus grants with lifecycle version, optional `expected_version` optimistic concurrency, disabled state, latest-state pagination, append-only history pagination, `actor_id`, `updated_at_from`, `updated_at_to`, and per-revision diff summaries with `action`, `previous_version`, changed role sets, and disabled-state changes; focused `check_json_schemas.py` validates 47 schemas/examples/interfaces; SDK unit and live API tests exercise upsert/disable/list/history/history-diff with role headers and expected-version/history filter payloads; registry tests prove JSONL reload, injectable PostgreSQL storage, and DSN-backed `PsycopgCorpusGrantSqlConnection` replay the same public contract | tested | Default psycopg DSN wiring exists for PostgreSQL corpus grants; high-throughput deployments should inject a pooled adapter. Batch 1 adds the shared `PooledPostgresProvider` (PY-018) that can back corpus-grant storage; live pooled-DB validation remains Docker/CI |
| API-001 | `POST /claims/extract` exists | `apps/api/src/hallu_defense/api/routes.py` | Claim | `/claims/extract` | endpoint test, OpenAPI | Endpoint test proves atomic claim extraction returns deterministic claim IDs, preserves repository file extensions such as `missing.py`/`service.py`, returns source spans tied to `message_id`, and propagates the trace header | tested | Extraction is deterministic sentence/atomic-part parsing; deeper linguistic extraction remains future improvement |
| API-002 | `POST /claims/classify` exists | routes/classifier | Claim | `/claims/classify` | endpoint test | Endpoint test proves repo-state, test-result, and opinion claims are classified with expected risk and evidence requirements | tested | Classifier remains deterministic rules-based and can be expanded with more task/domain cases |
| API-003 | `POST /evidence/retrieve` exists | routes/retrieval/RAG access policy | Evidence | `/evidence/retrieve` | endpoint test, ABAC metadata filter test, corpus reader-role tests | Endpoint returns retrieved evidence and claim evidence maps; tests prove tenant context reaches persistent backend, inline metadata filters work, cross-tenant `owner_tenant_id`/`tenant_id` metadata filters are rejected with 403 before backend search, restricted inline documents are rejected without `corpus_reader_roles`, restricted persistent evidence is filtered unless the principal has the required metadata reader role, durable registry reader roles filter persistent corpus evidence by tenant/corpus, persistent backend failures return 503, and the live OpenSearch smoke proves retrieval remains tenant-isolated against a running local cluster | tested | Metadata filter semantics are exact-match only; persistent evidence must include `metadata.corpus_id` for registry reader grants to apply |
| API-004 | `POST /claims/verify` exists | routes/verifier | ClaimVerdict | `/claims/verify` | endpoint test | Endpoint test proves contradictory document sources produce a typed `CONTRADICTED` verdict with supporting/contradicting evidence IDs in `validator_trace` | tested | Contradiction detection is deterministic and numeric/source-overlap based; deeper semantic contradiction detection remains future work |
| API-005 | `POST /response/repair` exists | routes/repair | ClaimVerdict | `/response/repair` | endpoint test | Endpoint test proves repair keeps supported claims with citations, removes unsupported claims, records repaired IDs, and returns final decision `repaired` | tested | Repair is conservative template-based rewriting; richer prose-preserving repair remains future work |
| API-006 | `POST /tools/validate-input` exists | routes/tool_safety/approvals/rate limiter | ToolCallEnvelope, ToolValidationResponse, ApprovalRecord, ApprovalExecutionGrant | `/tools/validate-input` | tool tests, approval queue persistence tests, execution grant tests, rate-limit tests | high-risk validation test creates pending approval with redacted sensitive inputs; JSONL approval queue persists redacted pending records by tenant; approved execution grants authorize the matching high-risk tool call once and reused grants fail with 403; tool validation now applies a configurable fixed-window in-memory rate limit scoped by tenant, authenticated subject, and tool name before creating new approval requests while reviewed execution grants bypass the limiter | tested | Rate limit is per API process; distributed/shared quotas remain future deployment work |
| API-007 | `POST /tools/validate-output` exists | routes/tool_safety | ToolCallEnvelope | `/tools/validate-output` | tool tests | `test_core_flow.py` validates secret-key redaction, common PII redaction, and conservative unlabeled-number handling | tested | PII detection is deterministic and intentionally conservative |
| API-008 | `POST /policy/evaluate` exists | routes/policy | PolicyEvaluation | `/policy/evaluate` | policy tests | Endpoint tests cover trace_id body, default allow, cross-tenant block, high-risk review, secret block, prompt injection block, indirect prompt injection block, data poisoning block, contradictory tool output rewrite, sandbox network review, deterministic repo evidence block, OPA adapter mapping, and fail-closed OPA errors | tested | OPA runtime is opt-in via config; production bundle distribution and policy data loading still pending |
| API-009 | `POST /repo/checks/run` exists | `apps/api/src/hallu_defense/api/routes.py`, `apps/api/src/hallu_defense/api/dependencies.py`, `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py` | SandboxRun | `/repo/checks/run` | sandbox tests, Docker backend tests, isolation config gate | Endpoint still returns typed `SandboxRun`; `SandboxRunner` keeps path traversal, allowlist, destructive/network preflight, artifacts, and inspection evidence while delegating command execution to `SandboxExecutionBackend`; `test_sandbox_docker_backend.py` covers host compatibility, Docker argv isolation flags, timeout kill, minimal env, and production/staging host fail-closed settings; `check_sandbox_isolation_config.py` validates API dependency wiring | tested | Local default remains host for compatibility; Docker backend live execution is opt-in and CI/live-pending when Docker is unavailable |
| API-010 | `POST /audit/export` exists | routes/audit/audit ledger/auth | VerificationRun, AuditEvent, AuditExportResponse | `/audit/export` | audit tests, tenant filter tests, RBAC tests | endpoint exports tenant-filtered verification runs and audit events; persistent JSONL ledger reload and tenant filtering tests pass; auth-required integration test proves non-auditor callers receive 403 and `auditor` callers can export | tested | Tenant identity is still header-derived; full JWT/JWKS identity binding remains pending |
| API-011 | Every endpoint has request/response/error models | routes/models/schemas | all | all | OpenAPI inspection, schema examples | OpenAPI test confirms required endpoints including `/documents/ingest`, `/rag/corpus-grants/upsert`, `/rag/corpus-grants/disable`, `/rag/corpus-grants/list`, `/rag/corpus-grants/history`, `/rag/corpus-grants/history/diff`, and `ErrorResponse`; 47 schemas/examples validate and cover TS interfaces | tested | Standalone schemas now cover current TS endpoint contracts; code generation remains pending |
| API-012 | Every endpoint exposes trace_id | middleware/routes/SDK/MCP | all | all | endpoint and live contract tests | Required endpoints tested for `x-trace-id`; SDK and MCP live tests verify propagated run trace IDs | tested | Trace is header-wide for most endpoints; body trace exists on `VerificationRun`/audit/error responses |
| API-013 | Every endpoint writes audit event | audit service/middleware/auth/SDK/MCP | all | all | audit tests, OIDC route auth tests, and live contract tests | Required endpoints tested through `/audit/export`; SDK/MCP live tests verify `/verification/run` audit events; OIDC route test proves the HTTP audit event uses the verified JWT tenant when `x-tenant-id` is omitted; corpus grant upsert and disable routes also record explicit `corpus_grant_upsert`/`corpus_grant_disable` audit events | tested | Middleware audit falls back to header/local tenant only before authenticated context exists; persistence remains tracked under audit ledger requirements |
| API-014 | `POST /approvals/list` exists | routes/approval_queue/auth | ApprovalListResponse | `/approvals/list` | approval API tests, OpenAPI, persistent queue tests, RBAC matrix tests | OpenAPI and high-risk approval flow tests pass; JSONL reload tests prove tenant-scoped pending approvals remain listable after restart; endpoint role matrix maps list access to `approval_reviewer` when auth is required | tested | Tenant identity is still header-scoped; full JWT/JWKS identity binding remains pending |
| API-015 | `POST /approvals/decide` exists | routes/approval_queue/auth | ApprovalDecisionResponse, ApprovalExecutionGrant | `/approvals/decide` | decision/cross-tenant tests, persistent queue tests, execution grant tests, auth tests | Tests prove `approval_reviewer` role is required, missing role returns 403, body `decided_by` spoofing is ignored, persisted `decided_by` comes from `x-subject-id`, approve returns execution grant, cross-tenant lookup returns 404, repeated decision returns 409, decided-state replays after JSONL reload, grants hand off into `/tools/validate-input`, signed gateway headers can bind tenant/subject/roles, and `oidc_jwt` can derive tenant/subject/roles from a verified RS256 JWT before role checks, including refresh on unknown JWKS `kid`; the optional deployed OIDC provider smoke gate exercises the same JWT/JWKS validation path when enabled | tested | Live deployed-provider smoke requires externally supplied provider URL/discovery and a short-lived test JWT |
| API-016 | `POST /documents/ingest` exists | routes/ingestion/retrieval/RAG access policy | DocumentIngestionRequest, DocumentIngestionResponse | `/documents/ingest` | endpoint, contract, SDK/MCP live tests, ABAC owner metadata tests, corpus writer-role tests | Endpoint returns trace_id, tenant_id, corpus_id, backend, document_count, indexed_count, evidence_ids, and local non-persistence warnings; tests prove tenant/trace propagation, persistent backend indexing, `owner_tenant_id` stamping, 403 rejection for cross-tenant owner metadata, 403 rejection for conflicting `corpus_id`, metadata `corpus_writer_roles` enforcement, durable registry writer-role enforcement before indexing, persistent backend failures return 503, live OpenSearch ingestion indexes tenant-scoped synthetic documents, and live pgvector smoke indexes tenant A/B synthetic documents without cross-tenant leakage | tested | Default local backend validates but does not persist; managed-service persistence and backfill remain deployment work |
| API-017 | `POST /rag/corpus-grants/upsert` exists | routes/corpus grant registry/auth/audit | CorpusGrantUpsertRequest, CorpusGrantResponse | `/rag/corpus-grants/upsert` | endpoint, RBAC, audit, SDK tests | Endpoint upserts tenant-scoped corpus reader/writer roles, preserves created attribution, increments grant version, re-enables disabled grants, supports optional `expected_version` optimistic concurrency with 409 on stale writes, appends history, requires authenticated `rag_writer` even when auth is optional, returns typed response, and records explicit audit event; registry persistence can replay through JSONL, injected PostgreSQL storage, or DSN-backed psycopg runtime wiring | tested | Connection pooling remains future deployment tuning for high-throughput PostgreSQL corpus grants |
| API-018 | `POST /rag/corpus-grants/list` exists | routes/corpus grant registry/auth | CorpusGrantListRequest, CorpusGrantListResponse | `/rag/corpus-grants/list` | endpoint, RBAC, SDK tests | Endpoint lists only authenticated-tenant latest corpus grants, supports optional `corpus_id`, `include_disabled`, `limit`, and cursor pagination, rejects malformed cursors with 400, and requires authenticated `rag_writer` or `verifier` even when auth is optional | tested | List returns latest state only; history endpoint covers revisions |
| API-019 | `POST /rag/corpus-grants/disable` exists | routes/corpus grant registry/auth/audit | CorpusGrantDisableRequest, CorpusGrantResponse | `/rag/corpus-grants/disable` | endpoint, RBAC, audit, SDK tests | Endpoint logically disables the active tenant/corpus grant, increments version, records disabled attribution, returns 404 for missing grants, supports optional `expected_version` optimistic concurrency with 409 on stale writes, is idempotent for already disabled grants when no stale expected version is supplied, requires authenticated `rag_writer` even when auth is optional, and records explicit audit event | tested | No hard delete endpoint by design; distributed storage remains future work |
| API-020 | `POST /rag/corpus-grants/history` exists | routes/corpus grant registry/auth | CorpusGrantHistoryRequest, CorpusGrantHistoryResponse | `/rag/corpus-grants/history` | endpoint, RBAC, SDK tests, schema examples | Endpoint returns append-only grant revisions in tenant scope, supports optional `corpus_id`, `actor_id`, `updated_at_from`, `updated_at_to`, `limit`, and cursor pagination, rejects malformed cursors or invalid timestamp ranges with controlled 400 responses, preserves append order after JSONL reload, and requires authenticated `rag_writer` or `verifier` even when auth is optional | tested | Snapshot history remains intentionally separate from the companion diff endpoint |
| API-021 | `POST /rag/corpus-grants/history/diff` exists | routes/corpus grant registry/auth | CorpusGrantHistoryDiffRequest, CorpusGrantHistoryDiffResponse | `/rag/corpus-grants/history/diff` | endpoint, RBAC, SDK tests, schema examples | Endpoint returns tenant-scoped append-only revision diffs, reuses `corpus_id`, `actor_id`, timestamp range, `limit`, and cursor filters from history, computes each diff from the real previous revision before filters are applied, reports `action`, `previous_version`, role additions/removals, disabled-state changes, `updated_by`, and `updated_at`, rejects malformed cursors with 400, appears in OpenAPI, and requires authenticated `rag_writer` or `verifier` even when auth is optional | tested | Diff summaries are derived from local append-only history; distributed grant storage remains future work |
| MCP-001 | MCP server exposes required tools | `packages/mcp-server` | all | MCP tools | MCP contract tests | `npm run test`: MCP `tools/list` verifies all required tools including `ingest_documents` | tested | Uses JSON-RPC-compatible stdio; full MCP protocol package integration can deepen later |
| MCP-002 | MCP tools return trace_id | `packages/mcp-server`, SDK trace option | all | MCP tools | MCP tests | `npm run test`: all 8 MCP tools are called live and each returns or preserves `trace_id` | tested | Trace output is MCP `structuredContent`, not a transport header |
| MCP-003 | MCP tools respect tenant context | `packages/mcp-server`, SDK, API tenant fallback | all | MCP tools | MCP tests | `npm run test`: all live MCP tool calls audit under MCP tenant; cross-tenant `tenant_id` arg is rejected | tested | Tenant context is env/header-based, not auth-derived caller identity yet |
| MCP-004 | MCP tools validate input strictly | `packages/mcp-server/src/schema-validation.ts`, `packages/mcp-server/src/server.ts` | Claim, Evidence, DocumentInput, ToolCallEnvelope, PolicyEvaluationRequest, RepoChecksRunRequest, VerificationRunRequest | MCP tools | MCP validation tests | `npm run test`: invalid nested Claim, empty repo commands, missing policy action, and unsupported fields are rejected before proxying | tested | Full MCP protocol package integration can still deepen later |
| MCP-005 | MCP tools return typed validated output | MCP server + public JSON Schemas | ClaimVerdict, DocumentIngestionResponse, EvidenceRetrievalResponse, ToolValidationResponse, PolicyEvaluationResponse, SandboxRun, VerificationRun | MCP tools | MCP output contract tests | `npm run test`: all tool outputs backed by public schemas validate before returning | tested | Some MCP wrapper shapes include `trace_id` in `structuredContent` outside those response schemas |
| MCP-006 | MCP document ingestion tool | `packages/mcp-server`, SDK, public schemas | DocumentIngestionRequest, DocumentIngestionResponse | MCP `ingest_documents`, `/documents/ingest` | MCP live tests, schema validation | MCP exposes `ingest_documents`, validates input/output against shared schemas, propagates MCP tenant context, and live test verifies local backend warning | tested | Full MCP protocol package integration can still deepen later |
| PY-001 | Verification orchestrator | `services/orchestrator.py` | VerificationRun | `/verification/run` | e2e tests | smoke/API tests | tested | Needs richer audit events |
| PY-002 | Atomic claim extractor | `services/claim_extractor.py`, `apps/api/tests/test_claim_services.py` | Claim | `/claims/extract` | extraction tests | dedicated service tests and endpoint e2e passed | tested | Extraction is deterministic sentence/atomic-part parsing; deeper linguistic extraction remains future improvement |
| PY-003 | Claim classifier | `services/claim_classifier.py`, `apps/api/tests/test_claim_services.py` | Claim | `/claims/classify` | classifier tests | dedicated service tests and endpoint e2e passed | tested | Needs broader taxonomy calibration beyond deterministic keyword categories |
| PY-004 | Hybrid retriever BM25 + vector + metadata | `services/retrieval.py`, `services/rag_index.py` | Evidence | `/evidence/retrieve` | retrieval tests | local hybrid scorer, metadata filtering, ranking traces, stable source-scoped persistent evidence IDs, persistent backend tenant propagation, OpenSearch template installation request construction, OpenSearch bulk-error fail-closed handling, pgvector runtime factory wiring through psycopg DSN, pgvector dimension fail-closed validation, live OpenSearch smoke coverage, live pgvector smoke coverage, and OpenSearch/pgvector adapter query construction tested; focused RAG/bootstrap tests pass | tested | Local deterministic ranker remains default; managed pgvector service validation and pooling remain pending |
| PY-005 | Claim verifier | `services/verifier.py` | ClaimVerdict | `/claims/verify` | verifier tests | document contradiction, test/build result, deterministic repo file/function/diff claim tests, Python and TypeScript symbol claim tests, diff-to-symbol claim tests, changed-line implementation claim tests, fix/validation command-evidence tests, focused command-target tests using structured sandbox command metadata, sandbox-run evidence bridge tests, and optional provider-backed NLI adjudication tests pass | tested | Provider NLI is disabled by default and only used for document/web evidence fallback; deterministic repo/test/tool claims remain separate |
| PY-006 | Contradiction detector | `services/verifier.py` | ClaimVerdict | `/claims/verify` | contradiction tests | numeric/test contradiction tests | tested | Source contradiction detection pending |
| PY-007 | Repair/abstain/block engine | `services/repair.py`, `apps/api/tests/test_claim_services.py` | ClaimVerdict | `/response/repair` | repair tests | dedicated service tests cover block precedence, human review, and abstain-without-supported-claims; endpoint e2e covers citation-preserving repair | tested | Repair is conservative template-based rewriting; richer prose-preserving repair remains future work |
| PY-008 | Formal rule engine | `services/policy.py`, `services/opa.py`, `infra/opa` | PolicyDecision | `/policy/evaluate` | Rego/Python tests | Python policy tests pass; optional OPA adapter maps `opa eval` decisions and fails closed on OPA errors; CI installs OPA; local fallback static Rego checks pass | tested | `opa` binary unavailable on this local PATH; runtime adapter is disabled by default until `HALLU_DEFENSE_OPA_ENABLED=true` |
| PY-009 | Tool input validator | `services/tool_safety.py` | ToolCallEnvelope | `/tools/validate-input` | unit tests, endpoint rate-limit tests | schema-required keys, risk classification, dangerous tool detection, approval-required handling, PII/secret output redaction, and tenant/subject/tool-scoped validation rate limiting pass focused tests | tested | Rate limit is in-memory and process-local; distributed enforcement remains future work |
| PY-010 | Tool output validator | `services/tool_safety.py` | ToolCallEnvelope | `/tools/validate-output` | unit tests | focused tool output tests pass for nested secret-key redaction, email redaction, US-style SSN redaction, phone redaction, and non-phone numeric preservation | tested | PII model covers common deterministic patterns only |
| PY-011 | Approval workflow backend | `services/approvals.py`, `services/auth.py`, `config.py`, `api/dependencies.py`, routes, docs/security, CI gate, `services/postgres.py`, `infra/rag/pgvector/004_approval_queue.sql` | ApprovalRecord, ApprovalExecutionGrant, ApprovalList*, ApprovalDecision* | `/tools/validate-input`, `/approvals/list`, `/approvals/decide` | approval API tests, auth tests, persistent queue tests, config validator | High-risk tool validation creates tenant-scoped pending approvals; decide endpoint supports approve/reject, requires `approval_reviewer`, ignores body reviewer spoofing, records reviewer identity from request principal, and blocks cross-tenant decisions; signed gateway headers can cryptographically bind tenant/subject/roles; `oidc_jwt` verifies RS256/JWKS, issuer, audience, `exp`, optional `nbf`/`iat`, subject, roles, and tenant before deriving the request principal, supports local JWKS path, remote JWKS URL, OIDC discovery `jwks_uri`, TTL cache, and refresh on unknown `kid`; JSONL backend persists request/decision snapshots and hashed execution grant states, replays latest state by approval ID, redacts sensitive tool payloads, rejects `memory` in production/staging, fails closed on corrupt records, binds grants to tenant/tool fingerprint, expires them, and consumes them once; focused tests and config validator pass | tested | Distributed queue storage and deployed identity-provider smoke remain future deployment work. Batch 1 adds a `PostgresApprovalQueueStorage` backend (PY-020) sharing the pooled Postgres provider; live-DB durability remains Docker/CI |
| PY-012 | Sandbox/test runner | `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py`, `apps/api/src/hallu_defense/config.py`, `apps/api/src/hallu_defense/api/dependencies.py` | SandboxRun, Evidence | `/repo/checks/run` | sandbox tests, Docker backend tests, isolation checker | Path traversal, script escape, destructive command, network deny, env scrub, stdout/stderr/exit code, artifact capture, typed command/inspection evidence with `sandbox_command.v1` metadata, git diff inspection, changed ranges, changed lines, Python and TS changed symbols, inspectable file inventory, Python AST symbols, and JS/TS static symbols tests pass; Batch 3 adds `SandboxExecutionBackend`, `HostSubprocessBackend`, and `DockerContainerBackend`, with tests proving host compatibility, pinned Docker argv, minimal container env, timeout kill, and production/staging host fail-closed behavior | tested | Docker live execution and persistent artifact storage remain deployment work; host backend stays default outside production-like environments |
| PY-013 | Audit ledger | `services/audit.py`, `config.py`, `api/dependencies.py`, `docs/security/audit-ledger.md`, `services/postgres.py`, `infra/rag/pgvector/003_audit_ledger.sql` | VerificationRun, AuditEvent | `/audit/export` | audit tests, redaction tests, config validator | JSONL append-only backend persists and reloads verification runs/events, tenant/trace filters exports, redacts sensitive-looking keys/text before storage, fails closed on corrupt records, and rejects `memory` backend in production/staging; focused tests and config validator pass | tested | JSONL is a local persistence backend; distributed database/object-store audit ledger and retention jobs remain future deployment work. Batch 1 adds a `PostgresAuditLedgerStorage` backend (PY-019) with export bounded by `audit_export_max_records`; live-DB durability remains Docker/CI |
| PY-014 | Trace system | middleware/orchestrator/current trace id/OpenTelemetry spans | VerificationRun, ErrorResponse, AuditEvent | all REST endpoints | trace tests | Required endpoints tested for `x-trace-id`; error bodies include trace_id; OpenTelemetry tests prove HTTP spans reuse safe trace IDs and record route/status/outcome without sensitive request attributes; domain-span tests cover verification stages, policy decisions, and sandbox execution without tenant/payload/secret attributes; Compose smoke started the local OTel collector with API OTLP wiring | tested | End-to-end exported span capture from the live collector remains a future assertion |
| PY-015 | Continuous eval system | `evals` | metrics | n/a | eval smoke and metric tests | smoke runner measures final decision accuracy, trace/ledger coverage, claim precision/recall, unsupported-claim recall, groundedness, faithfulness, false-positive blocking, critical pass-through, p95 latency, and cost per run | tested | Metrics are initial smoke-set metrics; full golden-set coverage and calibration remain pending |
| PY-016 | Provider abstraction | `services/providers.py`, `services/secrets.py`, API dependencies/config, `.env.example`, `docs/security/providers.md`, `apps/api/tests/test_provider_adapters.py`, `SECURITY.md` | n/a | n/a | provider adapter unit tests, secret scan, typecheck | OpenAI-compatible, Ollama/local, and deterministic mock providers are selected by settings; OpenAI-compatible credentials are read only through `SecretManager`; mock backend is rejected in production-like environments; injected transport tests prove request payloads, auth header construction, timeout use, response parsing, and no real network dependency | tested | Runtime provider connectivity is not verified locally |
| PY-017 | Provider-backed NLI adjudication fallback | `services/nli.py`, `services/verifier.py`, API dependencies/config, `.env.example`, `docs/security/providers.md`, `apps/api/tests/test_nli_adjudicator.py` | ClaimVerdict | `/claims/verify`, `/verification/run` | NLI adjudicator tests, verifier regression tests, lint/typecheck/API tests | Optional `HALLU_DEFENSE_PROVIDER_NLI_ENABLED` adjudicator requests strict JSON from the selected provider, redacts secret-like prompt text, rejects malformed output and unknown evidence IDs, refuses support without cited evidence, routes high-risk supported claims to human review, and is not used for repo/test deterministic claims; 7 focused tests and full API tests pass | tested | Disabled by default; runtime provider connectivity and calibration are not locally verified |
| PY-018 | Postgres shared connection pool provider | `services/postgres.py`, `apps/api/tests/test_postgres_provider.py` | n/a | n/a | provider/pool unit tests | `services/postgres.py` defines the `SqlConnectionProvider` protocol, a `PooledPostgresProvider` that shares a bounded psycopg connection pool across audit, approval, and corpus-grant storage, and a `RecordingSqlProvider` fake for deterministic tests; `test_postgres_provider.py`: 17 passed covering pool acquire/reuse, bounded capacity, cursor execution, and rollback-on-error | tested | Pool behavior is proven against the recording fake; live psycopg pool behavior against a real PostgreSQL requires Docker/CI and is not exercised on local Windows |
| PY-019 | Postgres audit ledger backend with bounded export | `services/audit.py`, `apps/api/tests/test_audit_ledger.py`, `scripts/ci/check_audit_ledger_config.py`, `infra/rag/pgvector/003_audit_ledger.sql` | VerificationRun, AuditEvent | `/audit/export` | audit backend tests, config gate | `services/audit.py` adds `PostgresAuditLedgerStorage` beside the JSONL `AuditLedgerStorage`, persisting redacted verification runs/events through the shared Postgres provider and bounding exports by `audit_export_max_records` with `ORDER BY created_at DESC, id DESC LIMIT`; migration `003_audit_ledger.sql` creates the tenant-scoped ledger table; `test_audit_ledger.py` passed and `check_audit_ledger_config.py` validated the persistence gate | tested | Backend parity is proven against the recording provider; durability, retention, and export ordering against a live PostgreSQL remain Docker/CI evidence |
| PY-020 | Postgres approval queue backend with atomic decide-once/consume-once guards | `services/approvals.py`, `apps/api/tests/test_approval_queue.py`, `scripts/ci/check_approval_queue_config.py`, `infra/rag/pgvector/004_approval_queue.sql` | ApprovalRecord, ApprovalExecutionGrant | `/approvals/decide`, `/tools/validate-input` | approval backend tests, config gate | `services/approvals.py` adds `PostgresApprovalQueueStorage` beside the JSONL `ApprovalQueueStorage`, enforcing decide-once and consume-once through atomic `UPDATE ... WHERE ... RETURNING` statements that return no row on a repeated decision or reused grant; migration `004_approval_queue.sql` creates the tenant-scoped approval and grant tables; `test_approval_queue.py` passed and `check_approval_queue_config.py` validated the persistence gate | tested | Atomicity is proven against the recording provider; truly concurrent decide/consume races against a live PostgreSQL remain Docker/CI evidence |
| TS-001 | Official SDK TypeScript | `packages/sdk` | all | REST | SDK tests, typecheck, schema coverage | `npm run test`: 5 SDK tests pass; TS contracts typecheck; schemas cover 33 exported interfaces | tested | Retry behavior and typed error payload details still need expansion |
| TS-002 | Agent tool-calling adapters | `packages/agent-adapters` | ToolCallEnvelope, ToolValidationResponse, ApprovalExecutionGrant | tools | adapter tests, typecheck, build | `@hallu-defense/agent-adapters` provides typed envelope builders, approved execution grant attachment, pre-tool validation, post-tool validation, sanitized output handling, and typed blocked-validation errors; package typecheck/build pass and 5 Vitest tests pass | tested | Initial adapter is provider-agnostic SDK glue; broader framework-specific adapters remain future work |
| TS-003 | Next.js console | `apps/console` | VerificationRun | `/verification/run` | build/typecheck | passed | tested | Uses demo fallback fixture |
| TS-004 | Approval queue UI | console, `apps/console/e2e/approval-queue.spec.ts`, `apps/console/playwright.config.ts`, `apps/console/vitest.config.ts`, `packages/sdk/src/client.ts`, `apps/api/src/hallu_defense/config.py`, `apps/api/src/hallu_defense/main.py`, `.github/workflows/ci.yml` | ApprovalRecord, ApprovalListResponse, ToolCallEnvelope | `/tools/validate-input`, `/approvals/list`, `/approvals/decide` | UI build/typecheck, deterministic Playwright browser e2e against the real API with persistent JSONL approval queue | Console panel lists pending approvals and can enqueue/approve/reject through typed SDK; `npm run test:e2e` boots the real FastAPI on port 18100 with a reset JSONL approval queue plus the production Next build on port 3100, enqueues a high-risk `delete_repository` call from the UI, sees it pending, approves it from the UI, reloads and confirms it left the pending queue, repeats the reject flow, and asserts `api_key` stays `[REDACTED]` in both the `/approvals/list` API response and the rendered UI; the browser run exposed and fixed SDK browser `fetch` binding for default and injected fetch implementations; CORS for the alternate e2e port is configured through the validated `HALLU_DEFENSE_CORS_ALLOW_ORIGINS` setting without changing secure defaults; Vitest explicitly excludes `e2e/**`; `.github/workflows/ci.yml` installs Playwright Chromium and runs `npm run test:e2e`; expanded local Playwright e2e suite: 7 passed | tested | E2E runs locally through alternate ports 18100/3100 because host port 8000 stays occupied; remote GitHub Actions execution evidence is still pending |
| TS-005 | Replay UI | console, `apps/api/src/hallu_defense/api/routes.py`, `apps/api/src/hallu_defense/services/orchestrator.py`, `apps/api/src/hallu_defense/services/audit.py`, `apps/api/src/hallu_defense/domain/models.py`, `packages/contracts/**`, `packages/sdk/src/client.ts`, `apps/console/app/run-console.tsx`, `apps/console/e2e/replay.spec.ts` | VerificationReplayRequest, VerificationReplayResponse, VerificationRun | `/verification/replay` | replay API tests, RBAC tests, redaction tests, SDK unit/live tests, schema/example validation, browser e2e | `POST /verification/replay` looks up tenant-scoped source runs, excludes replay-generated runs from source selection, re-applies content-security policy, re-executes verification and repair over the redacted stored snapshot, returns `trace_id`, `source_trace_id`, `source_created_at`, `source_final_decision`, `decision_changed`, and `replayed_run`, and appends a `verification_replay` audit event plus the replayed run; missing and cross-tenant traces fail closed with identical 404s; audit ledger snapshot redaction covers `canonical_form`, claim metadata, evidence `source_ref`, evidence `structured_content`, verdict reason/validator trace, and bare secret-shaped values; role matrix requires `verifier`; 8 focused replay API tests, SDK `replayVerification` unit plus live tests, 57-schema contract gate, OpenAPI regeneration, console replay panel, and Playwright e2e (UI-created run replayed, fail-closed missing trace, claim/verdict redaction) all pass | tested | Replay uses the stored snapshot by design; it does not re-run live retrieval against current corpora |
| TS-006 | Claim/evidence/verdict viewers | console | Claim/Evidence/Verdict | n/a | build/manual | build passed | tested | Needs route-level detail view |
| TS-007 | Policy explanation viewer | console, `apps/console/e2e/operations.spec.ts` | PolicyEvaluationResponse | `/policy/evaluate` | console typecheck/build, runtime smoke, Playwright browser e2e | Console panel calls typed SDK `evaluatePolicy`, renders allow/block action, policy version, explanation, and matched rules; console typecheck/build pass and worker runtime smoke saw HTTP 200 with policy panel text; Playwright e2e submits `secret_detected` attributes through the real browser/API path and asserts the blocked action, `secret_leakage_blocks_output` matched rule, and explanation text render in the panel | tested | Policy engine itself remains Python-only until OPA lands |
| TS-008 | Diff/test/artifact viewer | console, `apps/console/e2e/operations.spec.ts`, `apps/console/playwright.config.ts` | SandboxRun | `/repo/checks/run` | console typecheck/build, runtime smoke, Playwright browser e2e | Console sandbox panel calls typed SDK `runRepoChecks`, renders commands, exit codes, stdout/stderr snippets, artifacts, `sandbox_command.v1` targets, and `sandbox_inspection.v1` diff/static summaries with redaction helpers; console typecheck/build pass and worker runtime smoke saw HTTP 200 with sandbox panel text; Playwright e2e runs `python --version` through the browser against the real API, asserts `SUPPORTED`, command text, exit code, artifact summary, and keeps sandbox artifacts inside ignored `var/e2e` instead of the repo root | tested | Sandbox isolation hardening remains tracked under SBOX risks |
| TS-009 | Eval dashboard | `apps/console`, `evals/reports/smoke-metrics.json`, `evals/reports/scenario-metrics.json`, `evals/reports/scenario-history.json`, eval report contracts | EvalSmokeReport, EvalSmokeMetrics, EvalSmokeScenarioResult, EvalScenarioReport, EvalScenarioMetrics, EvalScenarioResult, EvalScenarioHistoryEntry, EvalScenarioHistoryReport | n/a | console unit tests, console typecheck/build, eval smoke/scenario artifacts | Console reads the real smoke, expanded scenario, and scenario history eval report artifacts server-side, validates all report shapes without fallback mock data, and renders smoke accuracy/coverage plus expanded scenario pass rate, category rates, guardrail metrics, per-scenario outcomes, latest-vs-previous trend metrics, and recent run history; `Invoke-WebRequest http://127.0.0.1:3000` returned 200 with `Eval scenarios` and `latest pass`; console Vitest, monorepo typecheck/test/build, schema gate, and contract tests passed | tested | Dashboard is still a static/offline view over local report artifacts; live metrics API ingestion remains future work |
| SEC-001 | OIDC JWT/JWKS authentication | `services/auth.py`, `services/oidc.py`, `api/dependencies.py`, `api/middleware.py`, `config.py`, `scripts/ci/oidc_provider_smoke.py`, `docs/security/auth-rbac.md`, `infra/security/auth-policy.json`, `infra/security/keycloak/realm-hallu-defense.json`, `scripts/dev/live_keycloak_oidc_smoke.py`, `.github/workflows/live.yml` | n/a | all | auth tests, OIDC JWT tests, OIDC route auth tests, auth config gate, deployed-provider smoke gate, OpenAPI export | `Principal` abstraction maps subject and roles from verified auth claims; `signed_headers` mode verifies an HMAC-SHA256 trusted gateway signature over tenant/subject/roles/timestamp using `SecretManager`; `oidc_jwt` mode verifies stdlib-only RS256 JWT signatures against JWKS, enforces `kid`, issuer, audience, required `exp`, optional `nbf`/`iat` with skew, subject claim, roles claim, and tenant claim; JWKS can come from local path, direct remote URL, or OIDC discovery document `jwks_uri`; remote JWKS is cached by TTL and refreshed once on unknown `kid`; `get_request_context()` rejects tenant header/token mismatches and exposes the authenticated tenant to middleware audit; `load_settings()` rejects production/staging with optional auth or unsigned claim headers and requires issuer/audience plus a JWKS source in `oidc_jwt`; `oidc_provider_smoke.py` validates a deployed provider with direct JWKS or discovery plus a short-lived JWT when enabled and skips safely by default; tests cover valid RS256 JWTs, tampered signatures, wrong issuer, expired tokens, remote cache, TTL refresh, discovery, unknown-kid refresh, FastAPI context wiring, route-level JWT tenant propagation, route-level verifier/auditor role enforcement, tenant mismatch, signed headers, endpoint roles, smoke skip, missing smoke JWT, discovery/JWKS/JWT success, expected-claim mismatch, insecure remote URL rejection, and auth production config validation; Batch 2 adds live OIDC verification against a local Keycloak provider (see SEC-014): the `realm-hallu-defense.json` confidential client mints an RS256 client_credentials token whose `aud`/`tenant_id`/roles claims match the verifier, exercised by the env-gated `live_keycloak_oidc_smoke.py` offline `--api` tests and confirmed by an independent live token mint (passed) | tested | Local CI runs the deployed-provider smoke in skipped mode; live provider validation requires external URL/discovery and a short-lived test JWT; Batch 2 adds a runnable self-hosted live provider through the local Keycloak realm, exercised by the live.yml keycloak-live job and verified independently via a real RS256 client_credentials token mint |
| SEC-002 | RBAC/ABAC | `services/auth.py`, `services/oidc.py`, `services/rag_access.py`, `services/corpus_grants.py`, `api/dependencies.py`, routes, policy/security/opa, docs/security, auth config gate | n/a | all guarded endpoints plus policy and RAG endpoints | auth tests, OIDC JWT tests, auth config tests, approval API tests, policy tests, RAG ABAC tests, corpus grant registry tests | Initial ABAC tenant/resource rule blocks cross-tenant access through Python policy and Rego baseline; RAG access policy stamps ingested evidence metadata with `owner_tenant_id`, rejects cross-tenant owner metadata, enforces metadata-declared `corpus_writer_roles`, rejects unreadable inline documents, filters persistent evidence by `corpus_reader_roles`, applies active durable tenant/corpus registry reader/writer grants, and ignores disabled registry grants; corpus grant mutation/list/history/history-diff routes always require authenticated `rag_writer`/`verifier` roles even when local auth is optional; corpus grant storage supports fail-closed JSONL, injectable PostgreSQL replay, and DSN-backed psycopg runtime wiring; high-risk/sensitive action policies require review; `ENDPOINT_ROLE_REQUIREMENTS` defines guarded roles for metrics, claims, evidence, ingestion, corpus grants, tools, policy, approvals, sandbox, audit, and verification | tested | Environment/sensitivity ABAC and pooled PostgreSQL connection lifecycle remain future work |
| SEC-003 | Tenant isolation | dependencies/middleware/retrieval/SDK/MCP/auth/RAG access policy | all | all | tenant tests, auth tests, OIDC JWT tests, OIDC route auth tests, RAG tenant tests | Python, SDK, and MCP tests verify tenant header/context propagation and tenant-filtered audit export; approval decision tests prove cross-tenant decisions return 404 while reviewer identity comes from request principal; signed gateway mode binds `x-tenant-id` into the auth claim signature; `oidc_jwt` derives tenant identity from the verified JWT tenant claim, rejects a mismatching `x-tenant-id` header, and route tests prove `/verification/run` plus HTTP audit events use the JWT tenant when no tenant header is supplied; RAG endpoint tests prove cross-tenant owner metadata is rejected before indexing or persistent search and that corpus grants/history/history-diff never bypass tenant-scoped backend filters, including actor/time-filtered history; PostgreSQL corpus grant migration keys every revision by tenant/corpus/version and replays history by append sequence | tested | Existing persisted chunks created before owner metadata stamping may lack `owner_tenant_id`; backend tenant columns still enforce tenant isolation |
| SEC-004 | Audit ledger active | audit service, docs/security/audit-ledger.md, config, CI | VerificationRun, AuditEvent | `/audit/export` | audit tests, redaction tests, config validator | Audit ledger has memory backend for local/test and JSONL append-only backend for persistence; production/staging reject memory backend; persistent records are redacted before storage; `test_audit_ledger.py` and `check_audit_ledger_config.py` pass | tested | Runtime database-backed ledger, object-store replication, and retention execution remain future work. Batch 1 adds the PostgreSQL audit backend (PY-019) behind the same interface; live-DB runtime remains Docker/CI |
| SEC-005 | Secret/PII redaction | tool_safety/security | ToolValidation | `/tools/validate-output` | security tests | `pytest apps\api\tests\test_core_flow.py -q`: 53 passed; `ruff` and `mypy` passed for the focused change | tested | Broader PII classes, non-US identifiers, and domain-specific sensitive payloads remain future hardening |
| SEC-006 | Secret scan in CI | `scripts/ci/secret_scan.py`, `apps/api/tests/test_secret_scan.py`, `Makefile`, `.github/workflows/security.yml` | n/a | n/a | secret scan, focused scanner tests | `secret_scan.py` returned `No obvious secrets found.`; `test_secret_scan.py`: 5 passed and covers synthetic secret detection, private-key marker detection, generated/local tooling skips, lockfile/binary skips, and current-repo cleanliness | tested | Pattern-based only; scanner intentionally skips generated/local tooling directories and does not replace dedicated secret-management controls |
| SEC-007 | Dependency audit in CI | `apps/api/pyproject.toml`, `scripts/ci/python_dependency_audit.py`, `apps/api/tests/test_python_dependency_audit.py`, `Makefile`, `.github/workflows/*.yml`, `SECURITY.md` | n/a | n/a | pip-audit wrapper tests, Python dependency audit, npm audit | `python_dependency_audit.py` first found `pytest 8.4.2` vulnerable via `PYSEC-2026-1845`; API dev dependency was raised to `pytest>=9.0.3,<10.0.0`, installed as 9.1.1, and the audit then reported no known vulnerabilities; `npm audit --omit dev` reported 0 vulnerabilities | tested | Local editable package is skipped by pip-audit because it is not published on PyPI; container image scanning remains separate |
| SEC-008 | Encryption config | `infra/security/encryption-policy.json`, `docs/security/encryption.md`, `scripts/ci/check_encryption_config.py`, `apps/api/tests/test_security_config.py`, `Makefile`, `.github/workflows/*.yml`, `SECURITY.md` | n/a | n/a | config validator, negative policy tests, CI security gate | `check_encryption_config.py` validates 9 components; tests reject plaintext external interfaces, weak TLS, and plaintext/local key management; full API tests, lint, typecheck, secret scan, JSON schema checks, and npm audit pass | tested | This proves versioned policy and CI enforcement, not runtime TLS/encrypted volume activation in local Docker or future Kubernetes |
| SEC-009 | Egress allowlist | `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py`, `infra/docker/sandbox.Dockerfile`, policy/OPA | SandboxRun, PolicyEvaluation | `/repo/checks/run`, `/policy/evaluate` | sandbox, Docker backend, policy, and isolation config tests | Deny-mode sandbox preflight blocks network attempts; Python policy and Rego baseline require human review for allowlisted sandbox network access; Docker backend argv pins `--network=none` and `test_sandbox_docker_backend.py` fails if the recorded argv lacks it; `check_sandbox_isolation_config.py` also statically pins the flag; live smoke is wired to prove outbound network failure when Docker is available | tested | Full enterprise egress allowlist is still future work; live Docker egress evidence is CI/live-pending when Docker is unavailable locally |
| SEC-010 | Vault-compatible secret manager integration | `apps/api/src/hallu_defense/services/secrets.py`, `apps/api/src/hallu_defense/config.py`, `apps/api/src/hallu_defense/api/dependencies.py`, `infra/security/secrets-policy.json`, `scripts/ci/check_secrets_config.py`, `apps/api/tests/test_secret_manager.py`, `apps/api/tests/test_secrets_config.py`, `docs/security/secrets.md`, `SECURITY.md`, `Makefile`, `.github/workflows/*.yml` | n/a | n/a | secret manager unit tests, secrets policy validator, security CI gate | API has a `SecretManager` abstraction with local env backend restricted to local/test/dev/CI and a Vault KV v2-compatible backend; secret values render as redacted; production/staging reject env backend and require Vault token configuration; OpenAI-compatible provider adapter consumes credentials through the manager; `check_secrets_config.py` and focused tests pass | tested | Runtime connectivity to a deployed Vault-compatible service is not exercised locally |
| SEC-011 | Container image scanning | `.github/workflows/security.yml`, `infra/docker/*.Dockerfile`, `scripts/ci/check_container_scan_config.py`, `apps/api/tests/test_container_scan_config.py`, `docs/security/container-scanning.md`, `SECURITY.md`, `Makefile` | n/a | n/a | container scan config validator, negative config tests, security workflow scan | Security workflow builds `hallu-defense-api:ci`, `hallu-defense-console:ci`, and `hallu-defense-sandbox:ci`, scans all three with Trivy for OS/library HIGH/CRITICAL vulnerabilities, and fails on findings; Dockerfiles set non-root users, with the sandbox image pinned to UID 10001; local validator and focused tests cover required image refs and scan failure settings | tested | Local Trivy runtime scan was not executed; GitHub Actions must provide vulnerability scan evidence |
| SEC-012 | Backup/restore and retention policy | `infra/security/backup-retention-policy.json`, `scripts/ci/check_backup_retention_config.py`, `apps/api/tests/test_backup_retention_config.py`, `docs/security/backup-restore-retention.md`, `SECURITY.md`, `Makefile`, `.github/workflows/*.yml` | n/a | n/a | backup retention config validator, negative config tests, security CI gate | `check_backup_retention_config.py` validates 9 components; focused tests reject disabled persistent backups, unencrypted backups, weak retention, non-tenant-scoped deletion, and missing Makefile wiring; full API tests and security validators pass | tested | This proves policy shape and CI enforcement, not runtime backup job execution or restore drill artifacts on deployed infrastructure |
| SEC-013 | Prompt injection and data poisoning policy guards | `services/content_security.py`, `services/policy.py`, `services/retrieval.py`, `services/orchestrator.py`, `infra/opa/policies/access_risk_approval.rego`, `infra/opa/tests/access_risk_approval_test.rego`, `evals/golden_sets/scenarios.json` | PolicyEvaluation, VerificationRun, Evidence | `/policy/evaluate`, `/evidence/retrieve`, `/verification/run` | scanner tests, retrieval evidence marking test, verification blocking tests, policy tests, Rego/static policy gate, eval scenarios | Python policy tests block direct prompt injection, indirect prompt injection, and data poisoning; Rego baseline includes matching blocking rules and static Rego checks pass; `ContentSecurityScanner` detects direct user-message injection, indirect document/tool-output instructions, and poisoning markers; retrieval marks contaminated document chunks in `Evidence.structured_content.security.threats`; `/verification/run` blocks direct prompt injection before retrieval and blocks poisoned retrieved evidence before claim verification; expanded scenario evals verify both policy-attribute and content-level attacks block with trace IDs | tested | Scanner is deterministic and conservative; adversaries can evade heuristic patterns, so broader classifiers and calibration remain future hardening |
| SEC-014 | OIDC live verification via a local Keycloak provider (importable realm plus client_credentials RS256 claim checks) | `infra/security/keycloak/realm-hallu-defense.json`, `docker-compose.yml`, `scripts/dev/live_keycloak_oidc_smoke.py`, `scripts/ci/check_local_runtime_config.py`, `apps/api/tests/test_local_runtime_config.py`, `.github/workflows/live.yml` | n/a | all guarded endpoints | local runtime config validator, offline Keycloak OIDC smoke tests, live Keycloak OIDC smoke | The `keycloak` service (quay.io/keycloak/keycloak:26.3, `start-dev --import-realm`, host port 8081) imports `realm-hallu-defense.json`, which defines 6 roles, a confidential `hallu-defense-api` client_credentials client, and `aud`/`tenant_id`/`roles` mappers with no PEM material; `check_local_runtime_config.py` `_validate_keycloak` validates the realm and the gate now reports 10 services and 3 volumes; `test_local_runtime_config.py`: 14 passed; the env-gated `live_keycloak_oidc_smoke.py` (`HALLU_DEFENSE_LIVE_KEYCLOAK_OIDC_SMOKE_ENABLED`) offline `--api` path validates the claims with a TestClient and a fake JWT; an independent live verification against a running Keycloak minted an RS256 client_credentials token with `aud=hallu-defense-api`, `tenant_id=tenant-a`, 6 roles, and a 2-key JWKS (passed); `docker compose config --quiet` passed with the keycloak service | tested | Live token minting was verified independently against a real Keycloak; the full `--api` smoke against a running Keycloak and the live.yml keycloak-live job run in CI/integration and remain deployment evidence |
| RAG-001 | Document ingestion | retrieval service, RAG index boundary, ingestion route, RAG access policy, corpus grant registry | DocumentIngestion*, Evidence | `/documents/ingest`, `/evidence/retrieve` | ingestion tests, contract tests, SDK/MCP live tests, owner metadata tests, corpus writer-role tests, corpus grant tests | `/documents/ingest` accepts `DocumentInput` payloads, applies corpus metadata plus tenant-owner metadata, rejects cross-tenant owner metadata and conflicting corpus metadata, enforces metadata-declared `corpus_writer_roles`, enforces durable registry writer roles, and indexes via the persistent RAG backend when configured; local mode returns an explicit non-persistence warning | tested | No durable ingestion worker or backfill pipeline yet |
| RAG-002 | Structural chunking | retrieval service, RAG index adapters | Evidence | `/evidence/retrieve`, `/documents/ingest` | retrieval and RAG index adapter tests | Retriever now preserves paragraph chunking for plain text and adds Markdown heading-aware section chunks with section heading/path/level metadata, flattened persistent-index metadata, and reconstructed `structured_content.structure` for OpenSearch/pgvector results; OpenSearch index writes use tenant-scoped physical document IDs while preserving public `evidence_id`; focused RAG adapter tests pass with explicit same-evidence-id/different-tenant overwrite regression coverage | tested | Parser intentionally covers conservative Markdown-style headings; richer HTML/PDF/table structural parsing remains future work |
| RAG-003 | BM25/OpenSearch search | `services/rag_index.py`, retrieval route, `infra/rag/opensearch/evidence-index-template.json`, `docker-compose.yml`, `scripts/dev/bootstrap_opensearch_template.py`, `scripts/dev/live_opensearch_rag_smoke.py`, `scripts/ci/check_rag_persistence_config.py`, `docs/rag/persistent-indexes.md` | Evidence | `/evidence/retrieve`, `/documents/ingest` | adapter query tests, tenant filter tests, OpenSearch bootstrap tests, live OpenSearch smoke, RAG persistence config validator | OpenSearch adapter builds tenant-scoped bulk and search payloads; search filters by tenant, context refs, and metadata; cross-tenant hits are ignored; bulk item errors fail closed; Compose includes pinned OpenSearch and API backend wiring; template declares `tenant_id` as required query filter; backend can install the template through `_index_template`; live Docker OpenSearch smoke installs the template, creates a smoke index, indexes tenant A/B synthetic documents, refreshes/searches, asserts tenant isolation, and removes the smoke index; bootstrap dry-run and focused tests pass | tested | Live smoke is opt-in and local-service only, not default CI; managed OpenSearch, backfill, and load validation remain pending |
| RAG-004 | Vector search pgvector/Qdrant | `services/rag_index.py`, `scripts/dev/live_pgvector_rag_smoke.py`, `infra/rag/pgvector/001_rag_evidence_chunks.sql`, `docker-compose.yml`, `scripts/ci/check_rag_persistence_config.py`, `docs/rag/persistent-indexes.md` | Evidence | `/evidence/retrieve`, `/documents/ingest` | adapter SQL tests, tenant filter tests, live pgvector smoke, RAG persistence config validator | pgvector adapter builds parameterized upsert and vector search statements with tenant, source ref, metadata, vector, and limit parameters; runtime factory wires pgvector through psycopg when `HALLU_DEFENSE_POSTGRES_DSN` is configured and fails closed on missing DSN, missing psycopg, unsafe table names, transport errors, or non-`VECTOR(16)` embedding dimensions; pgvector migration creates tenant-scoped table and vector/metadata indexes; Compose mounts the migration into Postgres init; opt-in live pgvector smoke verified the `vector` extension and `rag_evidence_chunks` embedding column, indexed tenant A/B synthetic documents, proved same public evidence ID can exist across tenants without leakage, asserted tenant-isolated retrieval, cleaned smoke rows, and removed the isolated Compose volume; focused adapter and persistence-config tests pass | tested | Qdrant is not implemented; managed pgvector service validation, pooled connections, repeatable production migrations, and backfill workers remain future work |
| RAG-005 | Metadata filtering | retrieval, persistent RAG adapters, RAG access policy, corpus grant registry | Evidence | `/evidence/retrieve` | retrieval tests, cross-tenant owner filter tests, corpus reader-role tests, corpus grant tests | endpoint test proves `metadata_filter` excludes non-matching inline document metadata; persistent adapter tests prove metadata filters are pushed to OpenSearch and pgvector; route test proves cross-tenant owner metadata filters are rejected with 403 before persistent backend search; corpus reader-role tests prove unreadable inline documents are rejected and unreadable persistent hits are filtered from evidence and claim maps; registry grant tests prove persistent evidence with `metadata.corpus_id` is filtered unless the principal has the durable reader role | tested | Metadata filter semantics are exact-match only |
| RAG-006 | Reranking/authority/freshness scoring | retrieval | Evidence | `/evidence/retrieve` | retrieval tests | service test proves official/fresh evidence outranks stale unknown evidence and exposes score trace; persistent adapter hits preserve retrieval trace metadata | tested | External search backend scoring calibration pending |
| RAG-007 | Contradictory source detection | verifier/retrieval | ClaimVerdict | `/claims/verify` | contradiction tests | verifier test proves relevant sources with conflicting numeric values return `CONTRADICTED` | tested | Current detector is deterministic numeric contradiction, not full semantic NLI |
| RAG-008 | PostgreSQL ingestion outbox storage | `infra/rag/pgvector/006_ingestion_outbox.sql`, `apps/api/src/hallu_defense/services/ingestion_jobs.py`, `apps/api/tests/test_ingestion_jobs.py`, `apps/api/tests/test_apply_postgres_migrations.py` | n/a | n/a | SQL-shape tests, state-transition tests, migration applier tests | Migration creates tenant-scoped `rag_ingestion_jobs` with `(status, available_at)` index; `PostgresIngestionJobQueue` implements enqueue, atomic `FOR UPDATE SKIP LOCKED` claim, guarded complete, and guarded fail/dead-letter backoff; focused tests cover SQL shape, zero-row guard rejection, retry/dead-letter transitions, jsonb payload parsing, and migration ordering | tested | Storage-only slice; async API, worker runtime, live PostgreSQL race evidence, and backfill remain follow-up Batch 6 slices |
| SBOX-001 | Network denied by default | `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py` | SandboxRun | `/repo/checks/run` | network denied test, Docker argv test, isolation config gate | Python script importing `urllib` is blocked before execution when `network_policy=deny`; Docker backend always includes `--network=none`; focused Docker tests and the static isolation checker both fail if `--network=none` is removed | tested | Preflight remains layer 1; real OS network isolation is exercised by the opt-in live smoke and CI live job when Docker is available |
| SBOX-002 | Filesystem scoped to workspace | `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py` | SandboxRun | `/repo/checks/run` | path traversal, script escape, Docker argv, live-smoke wiring tests | Repo path traversal and `python ../outside.py` are rejected; Docker backend uses `--read-only`, `--tmpfs /tmp`, one writable bind mount at `/workspace`, and `--workdir /workspace`; isolation checker pins the read-only root and one workspace mount, while live smoke is wired to prove writes outside `/workspace` fail | tested | Windows bind mounts with spaces are covered by argv-list tests; real filesystem isolation evidence is CI/live-pending when Docker is unavailable locally |
| SBOX-003 | Destructive command prevention | `apps/api/src/hallu_defense/services/sandbox.py` | SandboxRun | `/repo/checks/run` | command tests, isolation config gate | Python script containing `shutil.rmtree` is blocked by destructive-operation policy; Docker backend is additive and keeps `DESTRUCTIVE_PATTERNS` in `SandboxRunner` as layer 1 before container execution | tested | Pattern-based policy remains conservative; container isolation reduces blast radius but does not replace preflight |
| SBOX-004 | stdout/stderr/exit codes captured | sandbox | SandboxRun, Evidence | `/repo/checks/run` | command tests | sandbox tests assert exit codes/stdout for artifact and env probe commands; command evidence includes command, argv, command_kind, command_target_tokens, exit_code, stdout, stderr, and network_policy | tested | Large output truncation tested indirectly via bounded helper only |
| SBOX-005 | Artifact capture | sandbox | SandboxRun | `/repo/checks/run` | artifact tests | generated `artifacts/report.txt` and `reports/sandbox-inspection.json` are returned in `SandboxRun.artifacts` | tested | Captures changed files under `artifacts/` and `reports/` only; object storage pending |
| SBOX-006 | Secrets unavailable unless mounted | `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py` | SandboxRun | `/repo/checks/run` | secret env test, Docker minimal-env test | Host sandbox command cannot read inherited `API_KEY`; stdout shows fallback `missing`; Docker backend uses a minimal container env allowlist and focused tests prove sensitive names/values are not present in the recorded docker argv | tested | Explicit secret mounts are not implemented; Docker CLI still inherits the operator environment as needed to reach the Docker daemon |
| SBOX-007 | Git diff inspection | sandbox | SandboxRun | `/repo/checks/run` | git diff sandbox test | sandbox writes `reports/sandbox-inspection.json` with git status, `diff_files`, `changed_ranges`, `changed_lines`, and `changed_symbols`; tests initialize local git repos and verify modified Python and TypeScript files plus added line evidence | tested | Uses local git metadata; deeper semantic implementation validation is still conservative |
| SBOX-008 | AST/static inspection | sandbox | SandboxRun | `/repo/checks/run` | static inspection sandbox test | sandbox writes `reports/sandbox-inspection.json` with inspectable files, Python class/function/method symbols parsed by `ast`, and JS/TS symbols from conservative static scanning | tested | Static symbol extraction is intentionally conservative; deeper language analyzers can improve recall later |
| SBOX-009 | Deterministic repo claim verification | verifier/sandbox evidence | ClaimVerdict, Evidence, SandboxRun | `/claims/verify`, `/repo/checks/run` | verifier repo-state and sandbox bridge tests | verifier blocks loose text evidence for repo file claims, supports `service.py` from sandbox file inventory, supports Python `fetch` and TypeScript `loadUser`/`ApiClient.fetchUser` from static evidence, blocks missing symbols, supports file diff claims from git `diff_files`, supports symbol diff claims only from git `changed_symbols`, verifies repo claims directly from `SandboxRun.evidence`, and expanded `/verification/run` scenarios block false file/function claims from `sandbox_inspection.v1` evidence | tested | Semantic “implements Y” diff analysis still pending |
| SBOX-010 | TypeScript/JavaScript static symbol extraction | sandbox/verifier | SandboxRun, Evidence, ClaimVerdict | `/repo/checks/run`, `/claims/verify` | TS/JS static inspection and verifier tests | sandbox reports TypeScript `ApiClient`, `ApiClient.fetchUser`, `loadUser`, and `parseUser`; verifier supports TypeScript function and method claims from `SandboxRun.evidence` | tested | Regex-based static scanner covers common declarations; full TypeScript AST parser pending |
| SBOX-011 | Diff-to-symbol correlation | sandbox/verifier | SandboxRun, Evidence, ClaimVerdict | `/repo/checks/run`, `/claims/verify` | sandbox diff-symbol tests, verifier diff-symbol tests | sandbox correlates unified git hunks to nearest static Python/TS symbols through `git.changed_symbols`; verifier supports `The diff updates the function loadUser in service.ts` only when changed-symbol evidence exists and blocks missing changed symbols | tested | Correlation proves symbol touched, not full semantic correctness of the implementation |
| SBOX-012 | Implementation/fix claim changed-line evidence | sandbox/verifier | SandboxRun, Evidence, ClaimVerdict | `/repo/checks/run`, `/claims/verify`, `/verification/run` | changed-line and implementation-claim verifier tests, expanded eval scenarios | sandbox captures added/removed git lines in `git.changed_lines`; verifier blocks file-only `implements cache` claims when added lines do not contain asserted implementation terms, supports symbol-scoped implementation claims when changed lines contain the asserted terms, and `/verification/run` scenario coverage exercises the block path end-to-end | tested | Term matching is deterministic and conservative; it does not prove arbitrary semantic correctness |
| SBOX-013 | Fix/validation claims require command evidence | verifier/sandbox evidence | SandboxRun, Evidence, ClaimVerdict | `/claims/verify`, `/repo/checks/run`, `/verification/run` | missing/failing/successful command-evidence tests, expanded eval scenarios | verifier requires relevant successful command evidence for repo implementation claims that say fixed, validated, works, tests pass, or build/compile; missing relevant commands block, failing commands contradict, successful commands are included in verdict evidence, and expanded evals include a fixed-cache claim with command evidence | tested | Command class relevance is keyword-based; SBOX-014 adds target overlap for named behavior/file/symbol |
| SBOX-014 | Focused command target mapping for fix/validation claims | verifier/sandbox evidence | SandboxRun, Evidence, ClaimVerdict | `/claims/verify`, `/repo/checks/run`, `/verification/run` | broad-command rejection and targeted-command success/failure tests, expanded eval scenarios | verifier derives command target terms from asserted implementation terms, requested files, requested symbols, and changed symbol records; broad successful commands without target overlap block, while targeted passing/failing commands support or contradict the claim deterministically; expanded evals cover both broad-command rejection and targeted-command support | tested | Target matching now prefers structured `command_target_tokens` when present and falls back to lexical text only for legacy evidence |
| SBOX-015 | Structured sandbox command metadata | sandbox/verifier | SandboxRun, Evidence, ClaimVerdict | `/repo/checks/run`, `/claims/verify`, `/verification/run` | sandbox command metadata tests, verifier structured target tests, expanded eval scenarios | sandbox emits `sandbox_command.v1` command evidence with argv, executable, command_kind, target args/tokens, and targeted flag; verifier uses command_kind for test/build/validation class matching and command_target_tokens for focused claim matching; scenario evals now include structured broad and targeted command metadata | tested | Metadata is deterministic command parsing, not proof that a named test semantically covers the changed behavior |
| SBOX-016 | Docker sandbox execution backend pins OS isolation flags | `apps/api/src/hallu_defense/services/sandbox_exec.py`, `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/config.py`, `infra/docker/sandbox.Dockerfile`, `apps/api/tests/test_sandbox_docker_backend.py` | SandboxRun, Evidence | `/repo/checks/run` | Docker backend argv tests, timeout tests, host compatibility tests, prod/staging settings tests | `DockerContainerBackend` builds docker argv lists only, uses minimal container env, binds the repo cwd once at `/workspace`, pins `--rm`, `--network=none`, `--read-only`, `--tmpfs /tmp`, `--cap-drop ALL`, `--security-opt no-new-privileges`, pids/memory/cpu limits, `--user 10001`, and `--workdir /workspace`; injected-runner tests assert the recorded argv and timeout `docker kill` path; `HostSubprocessBackend` preserves existing sandbox behavior | tested | Docker daemon availability is deployment-dependent; local host backend remains available only outside production-like environments |
| SBOX-017 | Docker sandbox live smoke proves runtime isolation when enabled | `scripts/dev/live_docker_sandbox_smoke.py`, `.github/workflows/live.yml`, `Makefile`, `.env.example` | SandboxRun, Evidence | `/repo/checks/run` | env-gated live smoke, live workflow wiring, isolation config checker | `live_docker_sandbox_smoke.py` is disabled by default and skips cleanly when Docker is unavailable; when enabled it builds/uses `hallu-defense-sandbox:ci`, drives `SandboxRunner` with the Docker backend to prove outbound network failure, outside-workspace write denial, artifact capture, timeout kill behavior, and Docker inspect resource limits; `sandbox-live` is wired after `postgres-live` and `keycloak-live` | tested | Do not claim live smoke passed without a real Docker run; local execution may remain skipped while CI/live provides runtime evidence |
| OBS-001 | OpenTelemetry traces | `services/telemetry.py`, API dependencies/middleware/routes, `services/orchestrator.py`, `apps/api/pyproject.toml`, `infra/otel/otel-collector-config.yaml`, `docker-compose.yml`, `.env.example` | all | all HTTP endpoints plus verification/policy/sandbox flows | HTTP and domain span tests, lint/typecheck/API tests, compose config validation | OpenTelemetry SDK/exporter dependencies installed; focused tests prove HTTP spans capture method, URL path, route, status, outcome, duration, and existing trace ID; domain tests prove verification stage spans, policy decision spans, and sandbox execution spans record low-cardinality counts/results while excluding tenant/payload/source-ref/repo/command/secret attributes; full API tests, lint, typecheck, schema, OpenAPI, eval smoke, and secret scan pass; `docker compose config --quiet` passed and Compose smoke started the API plus local OTel collector | tested | Live collector span ingestion/export content was not asserted; deeper external backend spans for live OpenSearch/pgvector runtime calls remain pending |
| OBS-002 | Prometheus metrics | `services/metrics.py`, API middleware/routes/orchestrator/policy/sandbox/approvals, `infra/prometheus/prometheus.yml`, `docker-compose.yml` | metrics | `/metrics`, all HTTP endpoints and core decision paths | metrics endpoint tests, domain metric tests, OpenAPI content test, lint/typecheck/test | `/metrics` emits Prometheus text with build info, HTTP counters/latency, verification run final decisions/latency, claim verdict counts, policy decisions/latency, approval counts, and sandbox outcomes/latency; focused and full API tests pass locally | tested | In-memory metrics reset on process restart; offline eval metrics are still report files rather than Prometheus series |
| OBS-003 | Grafana dashboards | `infra/grafana/dashboards/hallu-defense-overview.json`, `infra/grafana/provisioning/**`, `scripts/ci/check_grafana_dashboards.py`, `docker-compose.yml` | metrics | n/a | dashboard lint, CI hook | Dashboard lint validates 1 Grafana dashboard with 11 panels covering HTTP, verification, claim verdict, policy, approval, and sandbox metrics; CI and Makefile run `check_grafana_dashboards.py`; Compose smoke started Grafana and `GET /api/health` returned 200 | tested | Browser-level dashboard panel rendering remains future validation |
| EVAL-001 | Golden sets exist | `evals/golden_sets` | n/a | n/a | eval smoke, expanded scenario tests | `smoke.json` plus `scenarios.json` cover 2 smoke scenarios and 21 expanded offline scenarios: partially false document answers, contradictory document sources, false test-result claims, false repo file/function claims with sandbox inspection evidence, semantic implementation/fix claims with changed-line and command evidence, invalid tool input, high-risk tool approval, secret redaction, contradictory tool output, policy-attribute prompt injection/data poisoning, content-level direct prompt injection, content-level indirect prompt injection in documents, content-level data poisoning in documents, sandbox path traversal, destructive command abuse, and denied-network abuse | tested | Broader adversarial coverage remains pending for richer semantic analyzers and live-service eval history |
| EVAL-002 | Metrics measured | evals/console | EvalSmokeReport, EvalScenarioReport, EvalScenarioHistoryReport, scenario metrics JSON | n/a | eval tests, console report parser tests, scenario runner | `evals/runners/smoke.py` emits smoke metrics; `evals/runners/scenarios.py` emits `scenario_count=21`, `pass_rate=1.0`, `verification_decision_accuracy=1.0`, `blocked_high_risk_rate=1.0`, `secret_redaction_rate=1.0`, `prompt_injection_block_rate=1.0`, `data_poisoning_block_rate=1.0`, `tool_contradiction_guard_rate=1.0`, `repo_false_claim_block_rate=1.0`, `repo_semantic_claim_decision_accuracy=1.0`, `sandbox_block_rate=1.0`, and `p95_latency_ms=4.795`; scenario runner also appends bounded recent metrics to `evals/reports/scenario-history.json`; scenario tests assert deterministic pass metrics and bounded history payload behavior; console parser tests load the real expanded scenario and history artifacts and reject malformed reports; console renders expanded metrics and recent run trends | tested | Metrics are still offline report files; calibrated thresholds and live backend ingestion remain pending |
| EVAL-003 | Versioned enforced eval thresholds | `evals/config/thresholds.json`, `evals/runners/thresholds.py`, `evals/runners/smoke.py`, `evals/runners/scenarios.py`, `scripts/ci/check_eval_thresholds_config.py`, `apps/api/tests/test_eval_thresholds_loader.py`, `apps/api/tests/test_eval_runner_thresholds.py`, `apps/api/tests/test_eval_thresholds_config.py` | n/a | n/a | threshold loader tests, runner regression tests, anti-weakening gate tests | `thresholds.json` version `eval-thresholds.v1` defines smoke/scenario min/max gates for hardcoded and previously ungated groundedness, faithfulness, cost, category, and guardrail metrics; runners delegate `_metric_failures` to the shared threshold loader; focused tests cover missing metrics, category floors, runner regression failures, weakened floors/ceilings, missing required metrics, wrong schema version, and missing Makefile/CI/evals workflow wiring | tested | Thresholds are calibrated to current deterministic golden-set behavior; broader statistical calibration and live eval ingestion remain separate Batch 5 slices |
| CI-001 | Python lint gate | Makefile/CI | n/a | n/a | ruff | `ruff check apps/api/src apps/api/tests scripts evals` passed after adding the OpenAPI drift gate | tested | Add CI job invocation of Makefile when make is available |
| CI-002 | Python typecheck gate | Makefile/CI | n/a | n/a | mypy | `mypy apps/api/src` passed with no issues in 37 source files after adding the OpenAPI drift gate | tested | Mypy added to API dev dependencies |
| CI-003 | Python tests gate | Makefile/CI | n/a | n/a | pytest | `pytest apps/api/tests -q`: 383 passed after adding replay, CORS, audit-redaction, browser-e2e support tests, tool validation rate-limit/config coverage, persistent OpenSearch RAG live-smoke/unit coverage, and pgvector runtime/live-smoke coverage | tested | FastAPI TestClient deprecation warning remains |
| CI-004 | TypeScript typecheck/tests/build/e2e gates | Makefile/CI | TS contracts | n/a | npm commands, Playwright browser e2e | `npm run typecheck`, `npm run test`, `npm run build`, and `npm run test:e2e` passed; SDK tests 9, agent-adapters tests 5, MCP tests 6, console eval-report tests 6, and Playwright browser e2e 7 including approval, replay, policy explanation, and sandbox evidence flows; `.github/workflows/ci.yml` installs Chromium and runs the browser e2e suite after build; MCP validates `SandboxRun.evidence` and document ingestion without writing repo-root artifacts; Next production build prerendered `/` and `/_not-found` | tested | Console typecheck requires `next typegen`; MCP tests build SDK first; remote GitHub Actions execution evidence for the new e2e step is pending |
| CI-005 | OpenAPI validation gate | `scripts/ci/export_openapi.py`, `scripts/ci/check_openapi.py`, `apps/api/tests/test_openapi_ci.py`, `Makefile`, `.github/workflows/ci.yml`, `docs/api/openapi.yaml`, `apps/api/pyproject.toml` | OpenAPI | all | export_openapi, check_openapi drift gate, focused checker tests | `scripts/ci/export_openapi.py` writes deterministic YAML to `docs/api/openapi.yaml` with explicit `pyyaml` dev dependency; `scripts/ci/check_openapi.py` compares the committed artifact against freshly generated OpenAPI and fails with a unified diff on drift; CI backend job uses `python -m pytest` and runs the drift gate; Makefile exposes `openapi-check`; focused tests prove generated artifacts pass, stale artifacts fail, the committed artifact matches generation, and Makefile/CI/README wiring exists; latest focused runs: `check_openapi.py` up to date, `python -m pytest apps/api/tests/test_openapi_ci.py -q` 6 passed, bare `pytest apps/api/tests/test_openapi_ci.py -q` 6 passed, full API tests 274 passed | tested | OpenAPI drift is now guarded locally and in CI; deeper semantic API compatibility checks remain future work |
| CI-006 | JSON Schema validation gate | scripts/Makefile | JSON Schema | n/a | check_json_schemas | `check_json_schemas.py` validated 57 schemas, 57 valid examples, 57 invalid examples, and 57 TypeScript interfaces after adding the replay contracts | tested | Uses Python `jsonschema` dev dependency; semantic code generation still pending |
| CI-007 | OPA/Rego tests gate | Makefile/CI/scripts/opa | policy | policy | policy-test | `run_policy_tests.py` selected 29 Python policy/config tests and passed; local host has no `opa` binary, so the runner executed `check_rego_policy.py`, which passed static checks for 2 Rego files including prompt injection and data poisoning identifiers; CI installs OPA 0.70.0 before policy tests | tested | Local Rego validation is static because `opa` is unavailable on PATH; executable `opa test infra/opa` evidence must come from CI or a host with OPA installed |
| CI-008 | Security tests/gates | scripts/CI | n/a | n/a | secret scan, auth config, OIDC provider smoke, encryption config, secrets config, audit ledger config, approval queue config, corpus grants config, backup retention config, RAG persistence config, OpenSearch bootstrap dry-run, sandbox isolation config, Python dependency audit, npm audit, container scan config | `secret_scan.py` passed after ignoring the local `.codex-fable-work` auxiliary copy; `check_auth_config.py` validates auth/RBAC policy, production fail-closed settings, docs, role matrix wiring, Makefile, CI, security workflow wiring, and OIDC provider smoke wiring; `oidc_provider_smoke.py` skips by default unless `HALLU_DEFENSE_OIDC_PROVIDER_SMOKE_ENABLED=true` and deployed-provider JWT/source env vars are supplied; `check_encryption_config.py` validated 9 components; `check_secrets_config.py` validates the Vault-compatible secrets baseline; `check_audit_ledger_config.py` validates audit ledger persistence wiring; `check_approval_queue_config.py` validates approval queue persistence wiring; `check_corpus_grants_config.py` validates corpus grants env keys, PostgreSQL migration, fail-closed storage, lifecycle/history docs, psycopg DSN runtime wiring, API dependency wiring, Compose-aligned Postgres DSN docs, Makefile, and CI/security workflow wiring; `check_backup_retention_config.py` validated 9 components; `check_rag_persistence_config.py` validates pgvector/OpenSearch persistence artifacts and opt-in live-smoke wiring; `bootstrap_opensearch_template.py --dry-run` validates the template bootstrap path without network; `check_sandbox_isolation_config.py` validates Docker sandbox isolation settings, flags, Dockerfile, Makefile, security workflow, and live workflow wiring; `python_dependency_audit.py` passed after adding `psycopg[binary]`; `npm audit --omit dev` 0 vulnerabilities; `check_container_scan_config.py` validates API/console/sandbox Trivy image scan config | tested | Local Trivy scan, live OIDC smoke, live Docker sandbox smoke, runtime backup jobs, and managed-service RAG validation remain deployment work |
| CI-009 | SDK/API and MCP/API live contract gates | `packages/sdk/tests/live-api.test.ts`, `packages/mcp-server/tests/server.test.ts`, `.github/workflows/ci.yml` | all TS/API contracts | `/verification/run`, `/documents/ingest`, `/rag/corpus-grants/upsert`, `/rag/corpus-grants/disable`, `/rag/corpus-grants/list`, `/rag/corpus-grants/history`, `/rag/corpus-grants/history/diff`, `/audit/export`, MCP `tools/*` | live contract tests in TypeScript CI | `npm run test` starts FastAPI and verifies SDK/API including corpus grant upsert/disable/list/history/history-diff with `actor_id` filter plus all 8 MCP tools for trace, tenant, audit, and positive/negative schema behavior | tested | CI now installs API in TypeScript job; tests still use in-memory services |
| CI-010 | Grafana dashboard lint gate | `scripts/ci/check_grafana_dashboards.py`, `.github/workflows/ci.yml`, `Makefile`, `infra/grafana/**` | metrics | n/a | dashboard lint | `check_grafana_dashboards.py` validates dashboard JSON, provisioning references, Prometheus datasource use, required panels, required metric coverage, and forbidden sensitive query terms; Compose smoke started Grafana and `/api/health` returned 200 | tested | Browser-level dashboard rendering remains future validation |
| CI-011 | Backup/retention config gate | `scripts/ci/check_backup_retention_config.py`, `Makefile`, `.github/workflows/*.yml`, `infra/security/backup-retention-policy.json` | n/a | n/a | config validator, focused negative tests | `check_backup_retention_config.py` validates policy/supporting-file wiring; `test_backup_retention_config.py`: 6 passed | tested | Validator checks policy and CI wiring, not live backup or restore execution |
| CI-012 | RAG persistence config gate | `scripts/ci/check_rag_persistence_config.py`, `scripts/dev/bootstrap_opensearch_template.py`, `scripts/dev/live_opensearch_rag_smoke.py`, `scripts/dev/live_pgvector_rag_smoke.py`, `apps/api/tests/test_rag_persistence_config.py`, `apps/api/tests/test_opensearch_bootstrap.py`, `Makefile`, `.github/workflows/*.yml`, `infra/rag/**`, `docker-compose.yml` | n/a | n/a | config validator, bootstrap dry-run, focused negative tests | `check_rag_persistence_config.py` validates pgvector migration, OpenSearch template, Compose backend wiring, Compose-aligned Postgres DSN example, Makefile target, CI/security workflow wiring, bootstrap dry-run wiring, opt-in live OpenSearch smoke wiring, and opt-in live pgvector smoke wiring; `bootstrap_opensearch_template.py --dry-run` validates the OpenSearch template without network; focused tests cover current artifacts plus tenant PK, tenant filter metadata, pinned image, Compose backend, Makefile, CI, bootstrap dry-run, live-smoke safety markers, acknowledged installation, and unacknowledged installation failure cases | tested | Validator and dry-run check static artifacts and request construction; live smokes remain opt-in/local-service and managed-service validation remains pending |
| CI-013 | Audit ledger config gate | `scripts/ci/check_audit_ledger_config.py`, `apps/api/tests/test_audit_ledger_config.py`, `apps/api/tests/test_audit_ledger.py`, `Makefile`, `.github/workflows/*.yml`, `docs/security/audit-ledger.md`, `infra/rag/pgvector/003_audit_ledger.sql`, `scripts/dev/apply_postgres_migrations.py` | n/a | n/a | config validator, focused negative tests | `check_audit_ledger_config.py` validates audit ledger env keys, docs, service behavior snippets, Makefile target, and CI/security workflow wiring; focused tests cover current artifacts plus missing env key, missing production persistent-backend rejection, and missing CI wiring | tested | Gate validates local JSONL audit configuration, not deployed database/object-store durability. Batch 1 adds repeatable migration `003_audit_ledger.sql` applied idempotently by `apply_postgres_migrations.py`; live migration/DB run remains Docker/CI |
| CI-014 | Approval queue config gate | `scripts/ci/check_approval_queue_config.py`, `apps/api/tests/test_approval_queue_config.py`, `apps/api/tests/test_approval_queue.py`, `Makefile`, `.github/workflows/*.yml`, `docs/security/approvals.md`, `infra/rag/pgvector/004_approval_queue.sql`, `scripts/dev/apply_postgres_migrations.py` | n/a | n/a | config validator, focused negative tests | `check_approval_queue_config.py` validates approval queue env keys, execution grant TTL config, docs, service behavior snippets, API dependency factory wiring, Makefile target, and CI/security workflow wiring; focused tests cover current artifacts plus missing env key, missing grant TTL env key, missing production persistent-backend rejection, missing factory wiring, and missing CI wiring | tested | Gate validates local JSONL approval configuration, not deployed database/object-store durability. Batch 1 adds repeatable migration `004_approval_queue.sql` applied idempotently by `apply_postgres_migrations.py`; live migration/DB run remains Docker/CI |
| CI-015 | Auth/RBAC config gate | `scripts/ci/check_auth_config.py`, `scripts/ci/oidc_provider_smoke.py`, `apps/api/tests/test_auth_config.py`, `apps/api/tests/test_oidc_provider_smoke.py`, `apps/api/tests/test_oidc_route_auth.py`, `infra/security/auth-policy.json`, `Makefile`, `.github/workflows/*.yml`, `docs/security/auth-rbac.md`, `infra/security/keycloak/realm-hallu-defense.json`, `scripts/dev/live_keycloak_oidc_smoke.py`, `.github/workflows/live.yml` | n/a | all guarded endpoints | config validator, focused negative tests, OIDC route auth tests, optional deployed-provider smoke | `check_auth_config.py` validates auth policy, `.env.example`, docs, production/staging fail-closed settings, OIDC issuer/audience/JWKS source requirements, remote JWKS URL/discovery/cache env keys, deployed-provider smoke env keys, signed-headers trusted-gateway support, endpoint role matrix wiring, Makefile target, and CI/security workflow wiring; focused tests cover current artifacts plus production auth disabled, production unsigned claims, missing OIDC config, OIDC path and remote URL production acceptance, insecure production JWKS URL rejection, policy regression, missing CI wiring, missing smoke CI wiring, smoke skip, smoke missing JWT, smoke discovery/JWKS/JWT success, smoke expected-claim mismatch, insecure smoke URL rejection, OIDC route verifier/auditor role enforcement, route tenant mismatch rejection, and audit tenant propagation from verified JWT claims; Batch 2 adds a runnable live provider (SEC-014): the local Keycloak realm and env-gated `live_keycloak_oidc_smoke.py` exercise the same RS256/JWKS path and the `.github/workflows/live.yml` keycloak-live job runs the live smoke in CI | tested | Gate validates local policy/config artifacts; live deployed-provider smoke execution requires external provider URL/discovery and a short-lived test JWT; the local Keycloak realm now gives a self-hosted live-provider option whose token mint was verified independently (RS256 client_credentials with correct aud/tenant/roles) |
| CI-016 | Corpus grants config gate | `scripts/ci/check_corpus_grants_config.py`, `apps/api/tests/test_corpus_grants_config.py`, `Makefile`, `.github/workflows/*.yml`, `docs/security/auth-rbac.md`, `infra/rag/pgvector/002_rag_corpus_grants.sql` | n/a | `/rag/corpus-grants/upsert`, `/rag/corpus-grants/disable`, `/rag/corpus-grants/list`, `/rag/corpus-grants/history`, `/rag/corpus-grants/history/diff` | config validator, focused negative tests | `check_corpus_grants_config.py` validates corpus grant env keys, docs, settings, PostgreSQL migration, fail-closed storage behavior snippets, pagination/lifecycle/history/history-diff support, actor/time history filter snippets, optimistic concurrency snippets, psycopg DSN runtime wiring, Compose-aligned Postgres DSN docs, API dependency factory wiring, role matrix entries, Makefile target, and CI/security workflow wiring; focused corpus grant/config tests: 7 passed for the config gate and broader corpus grant tests remain covered by the full Python suite | tested | Gate validates adapter/migration/runtime DSN config shape; pooled PostgreSQL connection lifecycle remains deployment work |
| CI-017 | Traceability matrix quality gate | `scripts/ci/check_traceability_matrix.py`, `apps/api/tests/test_traceability_matrix.py`, `Makefile`, `.github/workflows/ci.yml`, `docs/TRACEABILITY_MATRIX.md` | n/a | n/a | traceability validator, focused negative tests | `check_traceability_matrix.py` validates 163 requirement rows; `test_traceability_matrix.py`: 6 passed; CI backend job and Makefile run `check_traceability_matrix.py` | tested | Static gate enforces table health, status vocabulary, unique IDs, and accepted-row evidence discipline; semantic completeness still needs engineering review |
| CI-018 | Worklog quality gate | `scripts/ci/check_worklog.py`, `apps/api/tests/test_worklog.py`, `Makefile`, `.github/workflows/ci.yml`, `docs/WORKLOG.md` | n/a | n/a | worklog validator, focused negative tests | `check_worklog.py` validates 94 entries; focused worklog and traceability tests: 12 passed; CI backend job and Makefile run `check_worklog.py` | tested | Static gate enforces heading/latest-entry health only; historical semantic completeness still needs review |
| CI-019 | Foundation docs quality gate | `scripts/ci/check_foundation_docs.py`, `apps/api/tests/test_foundation_docs.py`, `Makefile`, `.github/workflows/ci.yml`, `AGENTS.md`, `docs/PLAN_MASTER.md`, `docs/ADR/*.md` | n/a | n/a | foundation docs validator, focused negative tests | `check_foundation_docs.py` validates 7 ADR files plus AGENTS and master plan required markers; `test_foundation_docs.py`: 6 passed; CI backend job and Makefile run `check_foundation_docs.py` | tested | Static gate validates required durable-doc structure, not full semantic adequacy of every architectural decision |
| CI-020 | Foundation infrastructure quality gate | `scripts/ci/check_foundation_infra.py`, `apps/api/tests/test_foundation_infra.py`, `Makefile`, `.github/workflows/ci.yml`, `package.json`, `apps/api/pyproject.toml` | n/a | n/a | foundation infrastructure validator, focused negative tests | `check_foundation_infra.py` validates 29 required paths, 16 Makefile targets, 3 workflow files, root Node workspaces, API Python metadata, and CI wiring; `test_foundation_infra.py`: 7 passed; CI backend job and Makefile run `check_foundation_infra.py` | tested | Static gate validates repo infrastructure wiring, not runtime execution of every target or remote GitHub Actions result |
| CI-021 | Local runtime Compose quality gate | `scripts/ci/check_local_runtime_config.py`, `apps/api/tests/test_local_runtime_config.py`, `docker-compose.yml`, `infra/prometheus/prometheus.yml`, `infra/otel/otel-collector-config.yaml`, `infra/grafana/**`, `Makefile`, `.github/workflows/ci.yml` | n/a | n/a | local runtime config validator, focused negative tests, compose config smoke | `check_local_runtime_config.py` validates 9 Compose services, 3 named volumes, pinned images/build Dockerfiles, ports, dependencies, local env wiring, Prometheus scrape target, OTel trace pipeline, Grafana provisioning, and Makefile/CI wiring; `test_local_runtime_config.py`: 9 passed; `docker compose config --quiet` passed; CI backend job and Makefile run `check_local_runtime_config.py` | tested | CI gate is static; live Docker smoke was run locally after installing Docker Desktop, but GitHub Actions does not currently start the full Compose stack |
| CI-022 | Live CI lane workflow for Postgres and Keycloak | `.github/workflows/live.yml`, `scripts/dev/live_postgres_persistence_smoke.py`, `scripts/dev/live_keycloak_oidc_smoke.py`, `infra/security/keycloak/realm-hallu-defense.json`, `docker-compose.yml` | n/a | n/a | live workflow wiring, live Postgres persistence smoke, live Keycloak OIDC smoke | `.github/workflows/live.yml` defines `postgres-live` and `keycloak-live` jobs triggered by `workflow_dispatch`, `push`, and a `cron` schedule, each standing up its services, running the env-gated live smokes, and tearing down with `docker compose down -v`; `docker compose config --quiet` passed with the keycloak service; an independent live verification confirmed the realm mints an RS256 client_credentials token with correct `aud`/`tenant_id`/6 roles and a 2-key JWKS (passed) | tested | The live jobs execute on GitHub Actions (ubuntu); local evidence covers compose config validity and an independent real-Keycloak token mint, while full job execution remains CI/integration evidence |
| CI-023 | Sandbox Docker isolation CI and live wiring | `scripts/ci/check_sandbox_isolation_config.py`, `apps/api/tests/test_sandbox_isolation_config.py`, `scripts/ci/check_container_scan_config.py`, `apps/api/tests/test_container_scan_config.py`, `.github/workflows/security.yml`, `.github/workflows/live.yml`, `Makefile` | n/a | n/a | isolation config validator, negative checker tests, container scan config tests, security workflow, sandbox live job | `check_sandbox_isolation_config.py` validates required sandbox env settings, production/staging host fail-closed text, Docker argv isolation flags, timeout kill wiring, non-root sandbox Dockerfile, Makefile targets, security workflow, docs, and `sandbox-live` workflow placement; focused tests reject missing `--network=none`, missing prod fail-closed guard, root Dockerfile user, and missing Makefile wiring; `check_container_scan_config.py` validates API, console, and sandbox Trivy scan config | tested | Static gate proves wiring and pinned flags; actual sandbox image build, Trivy scan, and live Docker smoke execute in GitHub Actions or on a Docker-enabled host |
| CI-025 | Eval thresholds anti-weakening config gate | `scripts/ci/check_eval_thresholds_config.py`, `apps/api/tests/test_eval_thresholds_config.py`, `evals/config/thresholds.json`, `Makefile`, `.github/workflows/ci.yml`, `.github/workflows/evals.yml` | n/a | n/a | config validator, focused negative tests, Makefile/CI/evals workflow wiring | Gate validates threshold schema version, required embedded floors/ceilings, runner threshold-loader usage, `eval-thresholds-config` target, backend CI wiring, and evals workflow wiring; tests prove weakened floors, raised ceilings, removed metrics, missing category floor, wrong schema version, and missing wiring all fail | tested | Gate enforces config rigor; it must run with the eval runners to prove current computed metrics |
| CI-027 | Ingestion pipeline storage config gate | `scripts/ci/check_ingestion_pipeline_config.py`, `apps/api/tests/test_ingestion_pipeline_config.py`, `Makefile`, `.github/workflows/ci.yml`, `.github/workflows/security.yml` | n/a | n/a | config validator, focused negative tests, CI/security wiring | Gate validates `006_ingestion_outbox.sql`, `PostgresIngestionJobQueue` atomic SQL markers (`FOR UPDATE SKIP LOCKED`, guarded `RETURNING`, dead-letter/backoff), `ingestion-pipeline-config` target, security-check wiring, backend CI wiring, and security workflow wiring; negative tests reject missing index, `DROP TABLE`, missing SKIP LOCKED, missing dead-letter status, and missing Makefile/CI/security wiring | tested | This gate covers storage only; later async API, worker, and backfill slices add their own runtime gates |
