# Traceability Matrix

Statuses: `not_started`, `designed`, `implemented`, `tested`, `documented`, `accepted`.

No item may be marked `accepted` without implementation, tests, documentation, and evidence.

The 2026-07-13 mass-QA acceptance decision is recorded in
`docs/qa/2026-07-13-mass-qa-acceptance.md`. It accepts the named gate executions
and live capabilities in that ledger; requirement rows remain at `tested` until
their individual residual risks are closed. In particular, a green offline test
does not convert an external-infrastructure or unresolved semantic requirement
to `accepted`.

The public-launch campaign started at checkpoint
`fb111c1e15c87e40006844d62e37616a84ab796f` and produced historical candidate
`d6c15bda15dda7a5e901f913d1007fd04d3089c5`. The 2026-07-20/21 hardening
campaign starts from `98a6bd135e075ed5db48af28fe2b6d6fc01e3fda`; its final
commit is recorded only after the last global rerun. TS-011, SEC-020, and CI-033
remain `tested`: current local evidence includes the 148/216 production browser
phase, expanded 77/117 form/Axe phase, a six-node Redis Cluster discovery/MOVED/
failover smoke, live PostgreSQL worker recovery, live Docker sandbox, encrypted
MinIO restore and current Trivy scans. They are not `accepted`; BrowserStack
minimum-version sessions, native browser zoom/manual assistive-technology review,
production Redis TLS/CRM/ingress/secrets/legal evidence and a deployed production
profile remain external. GitHub Actions for this campaign are blocked before
runner allocation by the repository billing/spending limit, so no remote-green
claim is made. Historical evidence below remains commit-specific.

PR validation follow-up: the first GitHub `evals` run exposed a missing API
source path on clean Linux runners. The workflow now exports the exact
`${{ github.workspace }}/apps/api/src` path, and the config gate rejects its
removal before the smoke and 21-scenario runners execute.

The subsequent clean Linux backend typecheck exposed platform-specific ctypes
attributes in `sandbox_exec.py`. Windows FFI callables are now resolved once
through explicitly typed adapters; mypy passes all 59 files for both native
Windows and `--platform linux` analysis.

The next Linux API run exposed four additional Windows-masked fixture and path
assumptions. ADR discovery now uses the repository's lowercase `docs/adr`, deep
JSON recursion reports the stable fail-closed depth violation, transport tests
isolate OPA ownership validation, and secret-file fixtures apply or mock the
exact POSIX boundaries they intend to exercise.

| ID | Description | Files / modules affected | Related contracts | Related endpoints | Tests required | Evidence of validation | Status | Risks / notes |
|---|---|---|---|---|---|---|---|---|
| FND-001 | Root agent instructions exist and define working loop | `AGENTS.md`, `scripts/ci/check_foundation_docs.py`, `apps/api/tests/test_foundation_docs.py` | n/a | n/a | foundation docs validator, focused negative tests | `check_foundation_docs.py` validates required AGENTS working-loop, non-negotiable, architecture, and command markers; `test_foundation_docs.py`: 9 passed | tested | Must keep updated as workflow evolves |
| FND-002 | Master product plan exists | `docs/PLAN_MASTER.md`, `scripts/ci/check_foundation_docs.py`, `apps/api/tests/test_foundation_docs.py` | all | all | foundation docs validator, focused negative tests | `check_foundation_docs.py` validates master plan product goal, surfaces, mandatory stack, public contracts, verification pipeline, milestones, and required contract markers; `test_foundation_docs.py`: 9 passed | tested | Plan is high-level, not acceptance evidence |
| FND-003 | Traceability matrix tracks every prompt requirement | `docs/TRACEABILITY_MATRIX.md`, `docs/PLAN_MASTER.md`, `scripts/ci/check_traceability_matrix.py`, `apps/api/tests/test_traceability_matrix.py`, `Makefile`, `.github/workflows/ci.yml` | all | all | traceability validator, plan-ID coverage and focused negative tests | `check_traceability_matrix.py` validates 197 requirement rows, expands shorthand IDs such as `EVAL-003/004/005`, and fails when PLAN_MASTER declares an ID absent from the matrix; focused traceability tests pass | tested | Gate enforces declared plan-ID coverage plus table/evidence hygiene; it previously passed 194 rows while the launch briefs' three undeclared IDs were absent, so semantic completeness still requires engineering review |
| FND-004 | Worklog records commands and outcomes | `docs/WORKLOG.md`, `scripts/ci/check_worklog.py`, `apps/api/tests/test_worklog.py`, `Makefile`, `.github/workflows/ci.yml` | n/a | n/a | worklog validator, focused negative tests | `check_worklog.py` validates dated headings, non-empty entries, latest-entry required sections, validation evidence, and Makefile/CI wiring; `test_worklog.py`: 6 passed | tested | Gate validates format and latest-entry evidence hygiene; it does not semantically audit every historical entry |
| FND-005 | Required ADRs exist | `docs/ADR/*.md`, `scripts/ci/check_foundation_docs.py`, `apps/api/tests/test_foundation_docs.py` | n/a | n/a | foundation docs validator, focused negative tests | `check_foundation_docs.py` validates 8 ADR files and required architecture, data-plane/control-plane, verification-pipeline, security-model, sandbox-model, and policy-engine ADR topics with status/context/decision/consequences sections; `test_foundation_docs.py`: 9 passed | tested | Existing older ADR remains informational |
| FND-006 | Monorepo skeleton exists | `apps`, `packages`, `infra`, `evals`, `scripts`, `package.json`, `apps/api/pyproject.toml`, `scripts/ci/check_foundation_infra.py`, `apps/api/tests/test_foundation_infra.py`, `Makefile`, `.github/workflows/ci.yml` | all | all | foundation infrastructure validator, focused negative tests | `check_foundation_infra.py` validates 29 required app/package/infra/eval/script paths, root Node workspaces, root npm scripts, and API Python project metadata; `test_foundation_infra.py`: 7 passed | tested | Static skeleton gate does not prove runtime service availability |
| FND-007 | Standard task runner exists | `Makefile`, `scripts/ci/check_foundation_infra.py`, `apps/api/tests/test_foundation_infra.py`, `.github/workflows/ci.yml` | n/a | n/a | foundation infrastructure validator, focused negative tests | `check_foundation_infra.py` validates standard Makefile targets, `.PHONY` coverage, virtualenv Python selection, and expected command markers; GNU Make 4.4.1 is available on the audited Windows host | tested | CI remains the canonical pinned Linux execution environment even though local Make targets are now runnable |
| FND-008 | Docker Compose includes required local services | `docker-compose.yml`, `docker-compose.prod.yml`, `infra/prometheus/prometheus.yml`, `infra/grafana/**`, `infra/otel/otel-collector-config.yaml`, `infra/rag/**`, `scripts/ci/check_local_runtime_config.py`, `apps/api/tests/test_local_runtime_config.py`, `Makefile`, `.github/workflows/ci.yml`, `infra/security/keycloak/realm-hallu-defense.json`, `scripts/dev/bootstrap_local_vault.py`, `scripts/dev/live_vault_secrets_smoke.py`, `scripts/dev/live_otel_export_check.py`, `scripts/dev/live_observability_smoke.py` | n/a | n/a | compose config validation, local/runtime profile validators, focused negative tests, opt-in live smokes | Compose includes API, ingestion worker, console, Postgres/pgvector, Redis, MinIO, OpenSearch, Prometheus, Grafana, OpenTelemetry collector, Keycloak, Vault, and the supporting local services validated by the runtime gate; local runtime gates cover 14 services and 4 volumes, and the base+production Compose merge passes configuration validation | tested | Compose syntax/profile validation is current; enabled multi-service Vault, observability, ingestion, and production E2E jobs remain live evidence rather than default-test evidence |
| FND-009 | Initial CI exists | `.github/workflows/ci.yml`, `.github/workflows/security.yml`, `.github/workflows/evals.yml`, `scripts/ci/check_foundation_infra.py`, `apps/api/tests/test_foundation_infra.py`, `Makefile` | all | all | foundation infrastructure validator, focused negative tests | `check_foundation_infra.py` validates `ci.yml`, `security.yml`, and `evals.yml` exist and that `ci.yml` wires backend tests, OpenAPI/docs/traceability/worklog/foundation infra gates, policy/security/RAG/Grafana checks, and TypeScript typecheck/test/build; `test_foundation_infra.py`: 7 passed | tested | Local validator proves workflow wiring, not remote GitHub Actions execution |
| FND-010 | Security CI exists | `.github/workflows/security.yml` | n/a | n/a | secret scan, auth config, OIDC provider smoke, encryption config, secrets config, audit ledger config, approval queue config, corpus grants config, sandbox isolation config, Python dependency audit, npm audit, container image scan | `secret_scan.py` passed; `check_auth_config.py` validates auth/RBAC production defaults and CI wiring; `oidc_provider_smoke.py` is wired into the security workflow and skips by default unless deployed-provider URL/JWT env vars are supplied; `check_encryption_config.py` validates the encryption baseline; `check_secrets_config.py` validates the Vault-compatible secrets baseline; `check_audit_ledger_config.py` validates the audit persistence gate; `check_approval_queue_config.py` validates approval persistence wiring; `check_corpus_grants_config.py` validates corpus grant persistence and lifecycle wiring; `check_sandbox_isolation_config.py` validates Docker sandbox isolation wiring; `python_dependency_audit.py` passed after upgrading vulnerable `pytest`; `npm audit --omit dev` 0 vulnerabilities; the security workflow uses an exact serialized ten-image first-party matrix and scans every image with pinned Trivy, while the local validator verifies the complete gate | tested | Static configuration does not prove current image CVE status; runtime Trivy scans remain GitHub Actions or safe scratch-only evidence, and live OIDC/Docker smokes require the live lane or an isolated local runtime |
| FND-011 | Evals CI exists | `.github/workflows/evals.yml`, `evals/config/thresholds.json`, `scripts/ci/check_eval_thresholds_config.py`, `scripts/ci/check_verifier_calibration.py` | n/a | n/a | eval smoke/scenarios, enforced thresholds, calibration drift, report publish config | Smoke and 21-scenario runners emit reports; versioned threshold and verifier-calibration gates run in CI/evals workflows; runtime eval report publish/list is covered by EVAL-005 | tested | Scenario breadth and deterministic calibration are initial baselines; live statistical sampling and deployed publish evidence remain future hardening |
| FND-012 | Neutral development context package exists for scoped contributor work | `docs/development/project-brief.md`, `docs/development/prior-session-report.md`, `docs/development/enterprise-batch.md`, `docs/development/enterprise-batch-2.md`, `apps/api/tests/test_development_context_config.py` | n/a | n/a | development-context document tests | Neutral filenames preserve the project mission, security/evidence rules, prior implementation context, scoped batch boundaries, and full-slice delivery contract without a vendor-specific executable workflow; `test_development_context_config.py` validates the package | tested | Historical coordination evidence remains in the worklog; current product behavior does not depend on an external-agent repository directory |
| FND-013 | Production Compose overlay exists and fails closed | `docker-compose.prod.yml`, `scripts/ci/check_prod_profile_config.py`, `scripts/dev/export_keycloak_jwks.py`, `scripts/dev/live_prod_profile_e2e.py`, `apps/api/tests/test_prod_profile_config.py`, `apps/api/tests/test_live_prod_profile_e2e.py`, `docs/deployment/production-profile.md`, `docs/adr/0007-production-deployment.md`, `Makefile`, `.github/workflows/*.yml` | n/a | `/verification/run`, `/tools/validate-input`, `/approvals/*`, `/rag/corpus-grants/*`, `/evals/reports/*`, `/documents/ingest*`, `/repo/checks/run`, `/audit/export`, `/metrics` | production profile config gate, backend-construction tests, focused negative tests, live scaffold | Base+prod Compose config passes; the gate validates identical production API/worker env, explicit non-mock provider, PostgreSQL eval reports, auth/Vault/persistent RAG/audit/approval/grants, Docker sandbox, OTLP, HTTPS CORS, runtime-only credentials and Redis Cluster mode. The live runner now requires an explicit `HALLU_DEFENSE_LIVE_PROD_PROFILE_E2E_REPO_REF` and cannot fall back implicitly to the current directory | tested | The ref remains an operator-supplied workspace path, not proof of an immutable source snapshot. The full prod e2e still requires a disposable deployed API, live dependencies and real OIDC token; it was not executed because no authorized target credentials were available. Config validation is not service-startup evidence |
| FND-014 | Helm/kind deployment scaffold exists | `infra/k8s/helm/hallu-defense/**`, `infra/docker/api.Dockerfile`, `scripts/ci/check_helm_chart.py`, `scripts/dev/live_kind_helm_smoke.py`, `apps/api/tests/test_helm_chart_config.py`, `apps/api/tests/test_live_kind_helm_smoke.py`, `docs/deployment/kubernetes-helm.md`, `docs/adr/0007-production-deployment.md`, `Makefile`, `.github/workflows/*.yml` | n/a | n/a | Draft 7 values validation, Helm lint/template plus malformed-value negatives, revision-aware install/upgrade smoke, deterministic cleanup/admission/network/secret tests | The current chart and checker pin fail-closed ServiceAccounts, Vault bootstrap, migrations, RBAC, admission, secret mounts and exact application egress. The authoritative post-dependency-update scratch run `accfinaln98a6bd1` completed two Helm revisions (one superseded, one deployed), reached seven ready workloads with zero restarts, fetched `/console` and validated its OIDC session contract, proved API-only access to both the Kubernetes Service VIP and its discovered post-DNAT control-plane endpoint, denied fifteen malicious workloads, exercised sandbox timeout/path/network isolation, and left zero residual cluster or run-scoped tags | tested | This is local single-node Kind evidence, not target-cluster ingress, managed identity, storage, multi-node scheduling or authenticated end-user proof. The Kubernetes API endpoints are discovered and verified for the scratch cluster; every target CNI/service implementation must rerun the same exact-evidence gate |
| FND-015 | Root README provides complete product, onboarding, operations, and evidence guidance | `README.md`, `.env.example`, `scripts/ci/check_foundation_docs.py`, `apps/api/tests/test_foundation_docs.py` | all public contracts | all public endpoints | README section/marker/link validation, focused negative tests, executable request smoke | `check_foundation_docs.py` validates the root guide and every relative link; `test_foundation_docs.py`: 9 passed; the documented `/verification/run` payload returned HTTP 200 with one claim, one verdict, and `final_decision=allow` | tested | External badge availability and rendered Mermaid remain GitHub concerns; deployment-specific readiness still requires target-environment evidence |
| CTR-001 | `Claim` Pydantic contract | `apps/api/src/hallu_defense/domain/models.py` | Claim | `/claims/*` | contract tests | current tests partial | tested | JSON Schema sync needs broader tests |
| CTR-002 | TypeScript contract surface | `packages/contracts/src/index.ts` | all exported interfaces | n/a | TS typecheck, schema coverage check | `npm run typecheck` passes; `check_json_schemas.py` verifies 72 exported TypeScript interfaces against the current 79-schema inventory | tested | Generated sync is not yet implemented; semantic coverage remains enforced by the explicit checker |
| CTR-003 | `Claim` JSON Schema | `packages/contracts/schemas/claim.schema.json`; examples | Claim | n/a | schema/example validation | `check_json_schemas.py` includes `Claim` in the current 79-schema, 79-valid-example, 79-invalid-example, 72-TypeScript-interface inventory | tested | Pydantic accepts some omitted defaults that JSON Schema now requires |
| CTR-004 | `Evidence` Pydantic/TS/JSON Schema | API/models/contracts/schemas/examples | Evidence | `/evidence/retrieve` | contract tests, persistent adapter tests | Schema/example validation and tenant-isolated OpenSearch/pgvector persistence tests pass | tested | Managed-service scale, availability, and live backend credentials remain deployment evidence |
| CTR-005 | `ClaimVerdict` Pydantic/TS/JSON Schema | API/models/contracts/schemas/examples | ClaimVerdict | `/claims/verify` | contract tests | schema/example validation passed | tested | Status/action vocabulary mismatch with prompt needs v2 mapping |
| CTR-006 | `VerificationRun` Pydantic/TS/JSON Schema | API/models/contracts/schemas/examples | VerificationRun | `/verification/run`, `/audit/export` | e2e/schema tests | schema/example validation and smoke/API tests passed | tested | Schema uses refs to Claim/Evidence/Verdict |
| CTR-007 | `ToolCallEnvelope` Pydantic/TS/JSON Schema | API/models/contracts/schemas/examples/MCP Ajv runtime validator, `services/tool_definitions.py` | ToolCallEnvelope | `/tools/*` | tool/schema/spoofing tests | Six public fields are required consistently; the immutable server registry derives phase schema, risk, approval, action, version/digest, and side effects; the current 79-schema/example gate, TypeScript typecheck, SDK 17 tests, adapter 11 tests, and MCP 41 tests pass | tested | Public metadata remains an assertion and unknown/mismatched definitions fail closed; operator provisioning beyond the built-in registry remains deployment work |
| CTR-008 | `SandboxRun` Pydantic/TS/JSON Schema | API/models/contracts/schemas/examples | SandboxRun, Evidence | `/repo/checks/run` | sandbox/schema tests | schema/example validation passes with typed `evidence`; sandbox tests prove command and inspection evidence are emitted | tested | Public contract is manually synchronized; code generation still pending |
| CTR-009 | `ErrorResponse` public error contract | API/models/contracts/OpenAPI/schemas/examples | ErrorResponse | all REST endpoints | error contract/schema tests | error tests and schema/example validation passed | tested | Error JSON Schema now exists |
| CTR-010 | `AuditEvent` and `AuditExportResponse` contracts | API/models/contracts/SDK/schemas/examples/audit ledger | AuditEvent, AuditExportResponse | `/audit/export` | audit/schema tests, JSONL/PostgreSQL persistence, envelope, redaction, coherent-snapshot, deep-ownership and replay-cardinality tests | JSONL and `PostgresAuditLedgerStorage` share strict tenant/trace/path/envelope and duplicate-ID validation, exact special-event metadata, bounded strictly ordered collections, one coherent run/event export snapshot with visible unit parity, one completion pair per tenant/trace/path, and an atomic replay triple; every persisted run/event crosses the centralized bounded redactor, and each completion stores a Vault-backed private commitment over normalized pre-redaction content | tested | PostgreSQL behavior is deterministic-fake and migration-structure tested; live database durability, Vault rotation, and operational legacy-row migration remain deployment evidence |
| CTR-011 | `DocumentInput` TS/JSON Schema contract | contracts/schemas/examples/MCP validator | DocumentInput | `/evidence/retrieve`, `/verification/run` | schema examples, MCP tests | The current 79-schema/example gate includes `DocumentInput`; MCP validates document inputs with the shared schema | tested | Pydantic defaults still allow omitted authority outside public schema examples |
| CTR-012 | `VerificationRunRequest` TS/JSON Schema contract | contracts/schemas/examples/MCP validator | VerificationRunRequest | `/verification/run` | schema examples, MCP tests | schema/example validation passed; MCP `repair_response` validates request with shared schema | tested | Pydantic allows defaults for optional fields; schema requires only `message_text` |
| CTR-013 | `EvidenceRetrievalRequest`/`EvidenceRetrievalResponse` TS/JSON Schema contracts | contracts/schemas/examples/MCP validator | EvidenceRetrievalRequest, EvidenceRetrievalResponse | `/evidence/retrieve` | schema examples, MCP request/output tests | schema/example validation passed for request and response; MCP validates retrieval input before proxying and validates retrieval output before returning `structuredContent`; SDK uses the typed request/response pair | tested | Contract coverage is structural; live persistent retrieval behavior remains tracked under RAG rows |
| CTR-014 | `ToolValidationResponse` TS/JSON Schema contract | contracts/schemas/examples/MCP validator | ToolValidationResponse | `/tools/validate-input`, `/tools/validate-output` | schema examples, MCP output tests | schema/example validation passed; MCP validates pre/post tool validation outputs | tested | Tool-specific nested input schema evaluation remains backend logic |
| CTR-015 | `PolicyEvaluationRequest` TS/JSON Schema contract | contracts/schemas/examples/MCP validator, policy verified context | PolicyEvaluationRequest | `/policy/evaluate` | schema examples, caller-spoof and MCP negative tests | Contract examples validate; the API replaces the asserted subject with the authenticated principal and ignores approval/evidence assertions; MCP rejects missing `action` before proxying | tested | Attributes remain a simulation surface that can only add restrictive observations |
| CTR-016 | `PolicyEvaluationResponse` TS/JSON Schema contract | contracts/schemas/examples/MCP validator, Python/Rego policy | PolicyEvaluationResponse | `/policy/evaluate` | schema examples, MCP output, Python/Rego parity tests | Response requires body `trace_id`; MCP validates `explain_policy`; a 16-case real-OPA matrix matched Python exactly on allowed/action/primary rule/explanation and Rego tests pass 31/31 | tested | The response is informational and deliberately cannot be used as an execution capability |
| CTR-017 | `RepoChecksRunRequest` TS/JSON Schema contract | contracts/schemas/examples/MCP validator | RepoChecksRunRequest | `/repo/checks/run` | schema examples, MCP negative tests | schema/example validation passed; MCP rejects empty command arrays before proxying | tested | Sandbox enforcement gaps remain tracked under SBOX items |
| CTR-018 | `SourceSpan` and `Freshness` TS/JSON Schema contracts | contracts/schemas/examples | SourceSpan, Freshness | Claim/Evidence nested fields | schema examples, TS/schema coverage | schema/example validation and TS coverage check passed | tested | Existing `Claim`/`Evidence` schemas still inline equivalent shapes instead of `$ref` |
| CTR-019 | Claim extraction/classification/verification endpoint schemas | contracts/schemas/examples, TS contracts | ClaimExtraction*, ClaimClassification*, ClaimVerification* | `/claims/extract`, `/claims/classify`, `/claims/verify` | schema examples, OpenAPI presence tests | 6 claim wrapper schemas validate with examples and TS coverage | tested | Dedicated endpoint behavior tests can be expanded beyond contract shape |
| CTR-020 | Evidence retrieval request and response schemas | contracts/schemas/examples, TS contracts | EvidenceRetrievalRequest, EvidenceRetrievalResponse | `/evidence/retrieve` | schema examples, MCP live tests, persistent adapter and hybrid retrieval tests | Request/response schemas validate with `metadata_filter`; MCP live tests exercise the retrieval response contract; persistent OpenSearch/pgvector adapters and hybrid merge/rerank paths preserve tenant/corpus isolation | tested | Managed external backend availability, capacity, and scoring calibration remain deployment evidence |
| CTR-021 | Response repair and audit export request/response schemas | contracts/schemas/examples, TS contracts | ResponseRepair*, AuditExport* | `/response/repair`, `/audit/export` | schema examples, API/audit tests | repair/audit schemas validate; audit export endpoint tests pass | tested | Response repair behavior still needs deeper semantic tests |
| CTR-022 | TypeScript to JSON Schema coverage gate | `scripts/ci/check_json_schemas.py`, contracts | all exported TS interfaces | n/a | contract coverage script | `check_json_schemas.py` fails if exported TypeScript interfaces lack matching JSON Schema; the current inventory validates 79 schemas, 79 valid examples, 79 invalid examples, and 72 TypeScript interfaces, including eval, release-evidence, replay, and retrieval contracts | tested | Coverage checks names and examples, not semantic equivalence of every property |
| CTR-023 | Approval workflow contracts | API/models/contracts/schemas/examples/SDK/approval queue | ApprovalRecord, ApprovalExecutionGrant, ApprovalList*, ApprovalDecision*, ToolValidationResponse.approval_id | `/approvals/list`, `/approvals/decide`, `/tools/validate-input` | schema examples, API/SDK, Postgres/JSONL, replay/substitution tests | Private v3 binding covers approval ID, origin trace, tenant, subject, tool/action, canonical argument hash, and definition version/digest; commitments differ across identical separate reviews; Postgres validates binding before atomic consume; issuer-bound capabilities are single-use; 109 approval/Postgres/OIDC tests and SDK 17 tests pass | tested | Raw execution tokens remain response-only; production durability still requires a deployed PostgreSQL/Vault run |
| CTR-024 | Document ingestion request/response contracts | API/models/contracts/schemas/examples/SDK/MCP | DocumentIngestionRequest, DocumentIngestionResponse | `/documents/ingest`, MCP `ingest_documents` | schema examples, sync/async API tests, SDK tests, MCP tests | The current 79-schema/example gate covers ingestion payloads; Pydantic/TypeScript/SDK/MCP preserve sync behavior while additive `job_id` and `job_status` represent queued async ingestion | tested | Local sync backend still returns a non-persistence warning; detailed job-status contract coverage is tracked separately under CTR-027 |
| CTR-025 | Corpus grant registry contracts | API/models/contracts/schemas/examples/SDK | CorpusGrant, CorpusGrantUpsertRequest, CorpusGrantDisableRequest, CorpusGrantListRequest, CorpusGrantHistoryRequest, CorpusGrantHistoryDiff, CorpusGrantHistoryDiffRequest, CorpusGrantResponse, CorpusGrantListResponse, CorpusGrantHistoryResponse, CorpusGrantHistoryDiffResponse | `/rag/corpus-grants/upsert`, `/rag/corpus-grants/disable`, `/rag/corpus-grants/list`, `/rag/corpus-grants/history`, `/rag/corpus-grants/history/diff` | schema examples, API tests, SDK tests | Pydantic, TypeScript, JSON Schema, and examples expose tenant-scoped corpus grants with lifecycle version, optimistic concurrency, disabled state, pagination, append-only history filters, and per-revision diffs; the current 79-schema/example gate includes all public grant contracts; SDK/API tests exercise every route; storage tests prove JSONL reload and PostgreSQL replay through DSN-backed or pooled providers | tested | Live pooled PostgreSQL concurrency and lifecycle execution remain Docker/CI evidence |
| CTR-026 | Eval report runtime contracts are synchronized across Python, TypeScript, JSON Schema, examples, SDK, and OpenAPI | `apps/api/src/hallu_defense/domain/models.py`, `packages/contracts/src/index.ts`, `packages/contracts/schemas/eval-report*.json`, `packages/contracts/examples/**/eval-report*.json`, `packages/sdk`, `docs/api/openapi.yaml` | EvalReportMetrics, EvalReport, EvalReportPublishRequest, EvalReportPublishResponse, EvalReportListRequest, EvalReportListResponse | `/evals/reports/publish`, `/evals/reports/list` | schema/example gate, API contract tests, SDK unit/live tests | The current gate validates 79 schemas, 79 valid examples, 79 invalid examples, and 72 TypeScript interfaces including all eval-report contracts; focused runtime/contract and SDK tests pass | tested | Contracts are manually synchronized; code generation and explicit API-version migration tooling remain future hardening |
| CTR-027 | Async document ingestion job and status contracts are additive and synchronized | `apps/api/src/hallu_defense/domain/models.py`, `packages/contracts/src/index.ts`, `packages/contracts/schemas/document-ingestion*.json`, `packages/contracts/examples/**/document-ingestion*.json`, `packages/sdk`, `packages/mcp-server`, `docs/api/openapi.yaml` | DocumentIngestionResponse, DocumentIngestionStatusRequest, DocumentIngestionStatusResponse | `/documents/ingest`, `/documents/ingest/status`, MCP `get_ingestion_status` | schema/example gate, API status tests, SDK tests, MCP tool exposure tests | Pydantic/TypeScript/schema examples cover additive `job_id` and `job_status`; tenant-scoped status API and SDK tests pass; MCP exposes and validates `get_ingestion_status`; the current 79-schema/example gate includes every ingestion status contract | tested | MCP status is schema/tool-list covered but does not yet have a dedicated end-to-end status invocation test |
| API-001 | `POST /claims/extract` exists | `apps/api/src/hallu_defense/api/routes.py` | Claim | `/claims/extract` | endpoint test, OpenAPI | Endpoint test proves atomic claim extraction returns deterministic claim IDs, preserves repository file extensions such as `missing.py`/`service.py`, returns source spans tied to `message_id`, and propagates the trace header | tested | Extraction is deterministic sentence/atomic-part parsing; deeper linguistic extraction remains future improvement |
| API-002 | `POST /claims/classify` exists | routes/classifier | Claim | `/claims/classify` | endpoint test | Endpoint test proves repo-state, test-result, and opinion claims are classified with expected risk and evidence requirements | tested | Classifier remains deterministic rules-based and can be expanded with more task/domain cases |
| API-003 | `POST /evidence/retrieve` exists | routes/retrieval/RAG access policy | Evidence | `/evidence/retrieve` | endpoint test, ABAC metadata filter test, corpus reader-role tests | Endpoint returns retrieved evidence and claim evidence maps; tests prove tenant context reaches persistent backend, inline metadata filters work, cross-tenant `owner_tenant_id`/`tenant_id` metadata filters are rejected with 403 before backend search, restricted inline documents are rejected without `corpus_reader_roles`, restricted persistent evidence is filtered unless the principal has the required metadata reader role, durable registry reader roles filter persistent corpus evidence by tenant/corpus, persistent backend failures return 503, and the live OpenSearch smoke proves retrieval remains tenant-isolated against a running local cluster | tested | Metadata filter semantics are exact-match only; persistent evidence must include `metadata.corpus_id` for registry reader grants to apply |
| API-004 | `POST /claims/verify` exists | routes/verifier | ClaimVerdict | `/claims/verify` | endpoint test | Endpoint test proves contradictory document sources produce a typed `CONTRADICTED` verdict with supporting/contradicting evidence IDs in `validator_trace` | tested | Contradiction detection is deterministic and numeric/source-overlap based; deeper semantic contradiction detection remains future work |
| API-005 | `POST /response/repair` exists | routes/repair | ClaimVerdict | `/response/repair` | endpoint test | Endpoint test proves repair keeps supported claims with citations, removes unsupported claims, records repaired IDs, and returns final decision `repaired` | tested | Repair is conservative template-based rewriting; richer prose-preserving repair remains future work |
| API-006 | `POST /tools/validate-input` exists | routes/tool_safety/approvals/rate limiter/trusted registry | ToolCallEnvelope, ToolValidationResponse, ApprovalRecord, ApprovalExecutionGrant | `/tools/validate-input` | spoofing, schema substitution, approval replay/cross-tenant, Postgres tests | Server definitions replace caller authority; unknown/mismatched tools block; hard policy blocks precede review; grants become issuer-bound single-use capabilities only after tenant/subject/record/args/definition validation; the expanded Front B suite passes 396 focused tests | tested | Rate limiting remains separately configured; deployment registry lifecycle is operator work |
| API-007 | `POST /tools/validate-output` exists | routes/tool_safety/content_security | ToolCallEnvelope | `/tools/validate-output` | recursive redaction, JSON-safety, post-redaction schema, and low-false-positive tests | End-to-end tests cover compact labeled SSN/phone, signed URLs, Azure/Google/AWS/API credentials, complete/folded headers, structured paths, compositional JSON, Unicode/cycles/depth, NaN/±inf, invalid surrogates, and safe phrases/query parameters. Blocked credentials are rechecked for stable sanitization; the sanitized value is revalidated against the trusted output schema and incompatible rewrites return no output | tested | Detection is deterministic and bounded; domain-specific identifiers beyond documented patterns need separate calibration |
| API-008 | `POST /policy/evaluate` exists | routes/policy/OPA/Rego | PolicyEvaluation | `/policy/evaluate` | verified-context, spoofing, Rego and exact parity tests | Authenticated subject/action/request binding and strict fact types are enforced; caller approval/evidence/source assertions cannot authorize; block precedence and one primary rule are identical across Python and OPA; Rego 31/31 and a real-OPA 16-case parity matrix pass | tested | OPA deployment still requires pinned binary/policy distribution and runtime configuration |
| API-009 | `POST /repo/checks/run` exists | `apps/api/src/hallu_defense/api/routes.py`, `apps/api/src/hallu_defense/api/dependencies.py`, `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py`, `apps/api/src/hallu_defense/services/sandbox_kubernetes.py` | SandboxRun | `/repo/checks/run` | sandbox backend, path-safety, boundary and isolation-gate tests | Endpoint returns typed `SandboxRun`; `SandboxRunner` keeps traversal, command/network policy, artifacts and inspection evidence while delegating to Docker locally/CI and tenant-bound Kubernetes in production-like environments. Focused tests cover Docker isolation, Kubernetes UID-bound cleanup, canonical Git evidence, and bounded workspace/output behavior | tested | Live Docker and kind evidence remain environment-dependent; production-like construction rejects Docker and requires Kubernetes |
| API-010 | `POST /audit/export` exists | routes/audit/audit ledger/auth | VerificationRun, AuditEvent, AuditExportResponse | `/audit/export` | audit tests, tenant filter tests, RBAC tests, OIDC route auth tests | endpoint exports tenant-filtered verification runs and audit events; persistent JSONL ledger reload and tenant filtering tests pass; non-auditor callers receive 403; `auditor` callers can export; and OIDC mode derives tenant identity from a verified JWT while rejecting header/token tenant mismatches | tested | Local optional-auth mode still permits explicit header-scoped identity; deployed OIDC provider execution remains live evidence |
| API-011 | Every endpoint has request/response/error models | routes/models/schemas | all | all | OpenAPI inspection, schema examples | OpenAPI tests confirm all required endpoints and `ErrorResponse`; the current gate validates 79 schemas, 79 valid examples, 79 invalid examples, and 72 TypeScript interfaces | tested | Standalone schemas cover current TS endpoint contracts; code generation remains pending |
| API-012 | Every endpoint exposes trace_id | middleware/routes/SDK/MCP | all | all | endpoint and live contract tests | Required endpoints tested for `x-trace-id`; SDK and MCP live tests verify propagated run trace IDs | tested | Trace is header-wide for most endpoints; body trace exists on `VerificationRun`/audit/error responses |
| API-013 | Every endpoint writes audit event | audit service/middleware/auth/SDK/MCP | all | all | audit tests, OIDC route auth tests, and live contract tests | Required endpoints tested through `/audit/export`; SDK/MCP live tests verify `/verification/run` audit events; OIDC route test proves the HTTP audit event uses the verified JWT tenant when `x-tenant-id` is omitted; corpus grant upsert and disable routes also record explicit `corpus_grant_upsert`/`corpus_grant_disable` audit events | tested | Middleware audit falls back to header/local tenant only before authenticated context exists; persistence remains tracked under audit ledger requirements |
| API-014 | `POST /approvals/list` exists | routes/approval_queue/auth | ApprovalListResponse | `/approvals/list` | approval API tests, OpenAPI, persistent queue tests, RBAC matrix tests, OIDC route auth tests | OpenAPI and high-risk approval flow tests pass; JSONL reload tests prove tenant-scoped pending approvals remain listable after restart; endpoint role matrix maps list access to `approval_reviewer`; and OIDC mode derives tenant and roles from a verified JWT while rejecting header/token tenant mismatches | tested | Local optional-auth mode still permits explicit header-scoped identity; deployed OIDC provider execution remains live evidence |
| API-015 | `POST /approvals/decide` exists | routes/approval_queue/auth | ApprovalDecisionResponse, ApprovalExecutionGrant | `/approvals/decide` | decision/cross-tenant tests, persistent queue tests, execution grant tests, auth tests | Tests prove `approval_reviewer` role is required, missing role returns 403, body `decided_by` spoofing is ignored, persisted `decided_by` comes from `x-subject-id`, approve returns execution grant, cross-tenant lookup returns 404, repeated decision returns 409, decided-state replays after JSONL reload, grants hand off into `/tools/validate-input`, signed gateway headers can bind tenant/subject/roles, and `oidc_jwt` can derive tenant/subject/roles from a verified RS256 JWT before role checks, including refresh on unknown JWKS `kid`; the optional deployed OIDC provider smoke gate exercises the same JWT/JWKS validation path when enabled | tested | Live deployed-provider smoke requires externally supplied provider URL/discovery and a short-lived test JWT |
| API-016 | `POST /documents/ingest` exists | routes/ingestion/retrieval/RAG access policy/outbox | DocumentIngestionRequest, DocumentIngestionResponse | `/documents/ingest` | sync/async endpoint, contracts, SDK/MCP, ABAC and persistent adapter tests | Endpoint preserves validated sync indexing and adds fail-closed async enqueue; tests prove tenant/trace propagation, owner/corpus role enforcement before either indexing or enqueue, persistent backend errors, and tenant-isolated OpenSearch/pgvector behavior | tested | Managed-service/load validation and enabled crash/restart worker smoke remain deployment work |
| API-017 | `POST /rag/corpus-grants/upsert` exists | routes/corpus grant registry/auth/audit | CorpusGrantUpsertRequest, CorpusGrantResponse | `/rag/corpus-grants/upsert` | endpoint, RBAC, audit, SDK tests | Endpoint upserts tenant-scoped corpus reader/writer roles, preserves created attribution, increments grant version, re-enables disabled grants, supports optional `expected_version` optimistic concurrency with 409 on stale writes, appends history, requires authenticated `rag_writer` even when auth is optional, returns typed response, and records explicit audit event; registry persistence can replay through JSONL, injected PostgreSQL storage, or DSN-backed psycopg runtime wiring | tested | Connection pooling remains future deployment tuning for high-throughput PostgreSQL corpus grants |
| API-018 | `POST /rag/corpus-grants/list` exists | routes/corpus grant registry/auth | CorpusGrantListRequest, CorpusGrantListResponse | `/rag/corpus-grants/list` | endpoint, RBAC, SDK tests | Endpoint lists only authenticated-tenant latest corpus grants, supports optional `corpus_id`, `include_disabled`, `limit`, and cursor pagination, rejects malformed cursors with 400, and requires authenticated `rag_writer` or `verifier` even when auth is optional | tested | List returns latest state only; history endpoint covers revisions |
| API-019 | `POST /rag/corpus-grants/disable` exists | routes/corpus grant registry/auth/audit | CorpusGrantDisableRequest, CorpusGrantResponse | `/rag/corpus-grants/disable` | endpoint, RBAC, audit, SDK, JSONL/PostgreSQL tests | Endpoint logically disables the active tenant/corpus grant, increments version, records disabled attribution, returns 404 for missing grants, supports optional `expected_version` optimistic concurrency with 409 on stale writes, is idempotent for already disabled grants when no stale expected version is supplied, requires authenticated `rag_writer` even when auth is optional, and records explicit audit event; production/staging persist through the PostgreSQL backend | tested | No hard delete endpoint by design; deployed PostgreSQL concurrency and connection-pool behavior remain live evidence |
| API-020 | `POST /rag/corpus-grants/history` exists | routes/corpus grant registry/auth | CorpusGrantHistoryRequest, CorpusGrantHistoryResponse | `/rag/corpus-grants/history` | endpoint, RBAC, SDK tests, schema examples | Endpoint returns append-only grant revisions in tenant scope, supports optional `corpus_id`, `actor_id`, `updated_at_from`, `updated_at_to`, `limit`, and cursor pagination, rejects malformed cursors or invalid timestamp ranges with controlled 400 responses, preserves append order after JSONL reload, and requires authenticated `rag_writer` or `verifier` even when auth is optional | tested | Snapshot history remains intentionally separate from the companion diff endpoint |
| API-021 | `POST /rag/corpus-grants/history/diff` exists | routes/corpus grant registry/auth | CorpusGrantHistoryDiffRequest, CorpusGrantHistoryDiffResponse | `/rag/corpus-grants/history/diff` | endpoint, RBAC, SDK, JSONL/PostgreSQL tests, schema examples | Endpoint returns tenant-scoped append-only revision diffs, reuses `corpus_id`, `actor_id`, timestamp range, `limit`, and cursor filters from history, computes each diff from the real previous revision before filters are applied, reports `action`, `previous_version`, role additions/removals, disabled-state changes, `updated_by`, and `updated_at`, rejects malformed cursors with 400, appears in OpenAPI, requires authenticated `rag_writer` or `verifier`, and replays through the configured durable grant storage | tested | Deployed PostgreSQL concurrency, lifecycle operations, and connection-pool behavior remain live evidence |
| API-022 | `POST /evals/reports/publish` persists tenant-scoped reports with RBAC, metrics, and audit | `apps/api/src/hallu_defense/api/routes.py`, `apps/api/src/hallu_defense/api/dependencies.py`, `apps/api/src/hallu_defense/services/eval_reports.py`, `apps/api/src/hallu_defense/services/metrics.py` | EvalReportPublishRequest, EvalReportPublishResponse, AuditEvent | `/evals/reports/publish`, `/metrics` | endpoint, tenant, RBAC, audit, metrics, persistence, SDK tests | `test_eval_reports.py` proves principal-derived tenant/publisher identity, `eval_publisher` enforcement, audit event emission, tenant-isolated persistence, and `hallu_eval_*` gauges; SDK live contract coverage passes | tested | Enabled PostgreSQL/live publish smoke remains deployment evidence; user-controlled suite labels are restricted but operational cardinality still needs monitoring |
| API-023 | `POST /evals/reports/list` returns only tenant-scoped, suite-filtered reports | routes/eval report repository/auth | EvalReportListRequest, EvalReportListResponse | `/evals/reports/list` | endpoint, tenant isolation, RBAC, backend parity, SDK tests | Focused tests prove auditor/verifier role enforcement and tenant/suite/limit filtering across memory, JSONL, and parameterized PostgreSQL adapters; SDK tests pass | tested | Pagination is bounded by `limit` but cursor pagination is not yet implemented for long report histories |
| API-024 | Async ingestion enqueue and tenant-scoped status endpoint exist without changing default sync behavior | `apps/api/src/hallu_defense/config.py`, `apps/api/src/hallu_defense/api/routes.py`, `apps/api/src/hallu_defense/services/ingestion.py`, `apps/api/src/hallu_defense/services/ingestion_jobs.py` | DocumentIngestionRequest, DocumentIngestionResponse, DocumentIngestionStatusRequest, DocumentIngestionStatusResponse | `/documents/ingest`, `/documents/ingest/status` | sync-default, fail-closed async config, ABAC-before-enqueue, tenant status isolation tests | `test_ingestion_runtime.py` proves sync remains default, async requires PostgreSQL, writer/tenant policy runs before enqueue, queued responses are typed, and cross-tenant status lookup returns the same 404 boundary | tested | Enabled crash/restart live worker smoke and real PostgreSQL concurrency remain live/CI evidence |
| API-025 | Verification completion and history are tenant-scoped, atomic and exactly once | `apps/api/src/hallu_defense/api/routes.py`, `apps/api/src/hallu_defense/api/middleware.py`, `apps/api/src/hallu_defense/services/audit.py`, `apps/api/src/hallu_defense/services/verification_history.py`, `infra/rag/pgvector/013_audit_history_integrity.sql`, `docs/api/verification-history.md`, `docs/api/openapi.yaml` | VerificationRun, VerificationReplayResponse, VerificationRunListRequest, VerificationRunListResponse, AuditEvent, ErrorResponse | `/verification/run`, `/v2/verification/run`, `/verification/replay`, `/verification/runs/list`, `/audit/export` | atomic rollback/idempotency/concurrency, snapshot interleaving/parity, deep ownership, replay 0/1/>1 cardinality with cap 1, tenant/filter/envelope negatives, real JSONL 503, OpenAPI 409/503, migration upgrade/rerun tests | Routes validate orchestrator tenant/trace identity and commit only after response construction; v1/v2 commit one run/completion pair and replay commits run/completion/provenance as one conflict-aware unit; source lookup reads at most two exact pre-cap candidates and returns stable 409 on duplicates before orchestration; retries return the canonical timestamp without exposing or aliasing the persisted projection; `/audit/export` consumes one coherent bounded snapshot; storage failures return a generic documented 503 | tested | SQL is structurally and fake-provider tested; enabled scratch PostgreSQL execution was unavailable locally and remains live/CI evidence |
| MCP-001 | MCP server exposes required tools | `packages/mcp-server` | all | MCP tools | MCP contract tests | `npm run test`: MCP `tools/list` verifies all required tools including `ingest_documents` | tested | Uses JSON-RPC-compatible stdio; full MCP protocol package integration can deepen later |
| MCP-002 | MCP tools return trace_id | `packages/mcp-server`, SDK trace option | all | MCP tools | MCP tests | `npm run test`: all 8 MCP tools are called live and each returns or preserves `trace_id` | tested | Trace output is MCP `structuredContent`, not a transport header |
| MCP-003 | MCP tools respect tenant context | `packages/mcp-server`, SDK, API tenant fallback | all | MCP tools | MCP tests | `npm run test`: all live MCP tool calls audit under MCP tenant; cross-tenant `tenant_id` arg is rejected | tested | Tenant context is env/header-based, not auth-derived caller identity yet |
| MCP-004 | MCP tools validate input strictly | `packages/mcp-server/src/schema-validation.ts`, `packages/mcp-server/src/server.ts` | Claim, Evidence, DocumentInput, ToolCallEnvelope, PolicyEvaluationRequest, RepoChecksRunRequest, VerificationRunRequest | MCP tools | MCP validation tests | `npm run test`: invalid nested Claim, empty repo commands, missing policy action, and unsupported fields are rejected before proxying | tested | Full MCP protocol package integration can still deepen later |
| MCP-005 | MCP tools return typed validated output | MCP server + public JSON Schemas | ClaimVerdict, DocumentIngestionResponse, EvidenceRetrievalResponse, ToolValidationResponse, PolicyEvaluationResponse, SandboxRun, VerificationRun | MCP tools | MCP output contract tests | `npm run test`: all tool outputs backed by public schemas validate before returning | tested | Some MCP wrapper shapes include `trace_id` in `structuredContent` outside those response schemas |
| MCP-006 | MCP document ingestion and status tools | `packages/mcp-server`, SDK, public schemas | DocumentIngestionRequest, DocumentIngestionResponse, DocumentIngestionStatusRequest, DocumentIngestionStatusResponse | MCP `ingest_documents`, MCP `get_ingestion_status`, `/documents/ingest*` | MCP live ingestion tests, status schema/tool exposure, contract validation | MCP validates ingestion input/output, propagates tenant context, exposes `get_ingestion_status`, and validates its request/response schemas | tested | Dedicated MCP end-to-end status polling remains pending even though tool exposure and contracts are tested |
| PY-001 | Verification orchestrator | `services/orchestrator.py`, `api/routes.py`, `services/audit.py` | VerificationRun | `/verification/run`, `/v2/verification/run`, `/verification/replay` | e2e, route identity and atomic persistence tests | The orchestrator is compute-only; the route is the sole final persistence boundary, eliminating the former double write while preserving v1/v2/replay response compatibility | tested | Orchestration can still execute again before an idempotent retry resolves at persistence; volatile run/evidence timestamps are normalized only for conflict comparison |
| PY-002 | Atomic claim extractor | `services/claim_extractor.py`, `apps/api/tests/test_claim_services.py` | Claim | `/claims/extract` | extraction tests | dedicated service tests and endpoint e2e passed | tested | Extraction is deterministic sentence/atomic-part parsing; deeper linguistic extraction remains future improvement |
| PY-003 | Claim classifier | `services/claim_classifier.py`, `apps/api/tests/test_claim_services.py` | Claim | `/claims/classify` | classifier tests | dedicated service tests and endpoint e2e passed | tested | Needs broader taxonomy calibration beyond deterministic keyword categories |
| PY-004 | Hybrid retriever BM25 + vector + metadata | `services/retrieval.py`, `services/rag_index.py` | Evidence | `/evidence/retrieve` | retrieval tests | local hybrid scorer, metadata filtering, ranking traces, stable source-scoped persistent evidence IDs, persistent backend tenant propagation, OpenSearch template installation request construction, OpenSearch bulk-error fail-closed handling, pgvector runtime factory wiring through psycopg DSN, pgvector dimension fail-closed validation, live OpenSearch smoke coverage, live pgvector smoke coverage, and OpenSearch/pgvector adapter query construction tested; focused RAG/bootstrap tests pass | tested | Local deterministic ranker remains default; managed pgvector service validation and pooling remain pending |
| PY-005 | Claim verifier | `services/verifier.py` | ClaimVerdict | `/claims/verify` | verifier tests | document contradiction, test/build result, deterministic repo file/function/diff claim tests, Python and TypeScript symbol claim tests, diff-to-symbol claim tests, changed-line implementation claim tests, fix/validation command-evidence tests, focused command-target tests using structured sandbox command metadata, sandbox-run evidence bridge tests, and optional provider-backed NLI adjudication tests pass | tested | Provider NLI is disabled by default and only used for document/web evidence fallback; deterministic repo/test/tool claims remain separate |
| PY-006 | Contradiction detector | `services/verifier.py` | ClaimVerdict | `/claims/verify` | contradiction tests | numeric/test contradiction tests | tested | Source contradiction detection pending |
| PY-007 | Repair/abstain/block engine | `services/repair.py`, `apps/api/tests/test_claim_services.py` | ClaimVerdict | `/response/repair` | repair tests | dedicated service tests cover block precedence, human review, and abstain-without-supported-claims; endpoint e2e covers citation-preserving repair | tested | Repair is conservative template-based rewriting; richer prose-preserving repair remains future work |
| PY-008 | Formal rule engine | `services/policy.py`, `services/opa.py`, `infra/opa` | PolicyDecision | `/policy/evaluate` | strict-context/Rego/Python parity tests | OPA 1.7.1 passed strict check and 31/31 Rego tests; 16 adversarial contexts matched Python exactly on decision, primary rule, and explanation; malformed types, empty verified approval IDs, oversized identities, invalid source/surface enums, and cross-subject reuse fail closed | tested | Runtime remains opt-in and production must pin/verify its OPA artifact |
| PY-009 | Tool input validator | `services/tool_safety.py`, `services/tool_definitions.py` | ToolCallEnvelope | `/tools/validate-input` | registry, spoofing, schema, capability/replay tests | Immutable definitions derive phase schema/risk/approval/action/side effects; unknown and mismatched assertions fail closed; high-risk hard blocks precede review; valid execution requires an issuer-bound single-use capability | tested | Definitions are built in server-side code; hot-reload/operator distribution is out of this slice |
| PY-010 | Tool output validator | `services/tool_safety.py`, `services/content_security.py` | ToolCallEnvelope | `/tools/validate-output` | output schema/redaction/serialization tests | Recursive bounded sanitization covers documented secret/PII formats, structured header paths, serialized JSON, non-finite numbers, and invalid surrogates; safe query parameters/phrases remain. Stability and trusted-schema postvalidation prevent either a remaining credential or a schema-invalid rewritten value from being returned | tested | Pattern redaction is conservative and does not replace a specialized DLP service |
| PY-011 | Approval workflow backend | `services/approvals.py`, auth/config/dependencies/routes/docs, Postgres migration | ApprovalRecord, ApprovalExecutionGrant, ApprovalList*, ApprovalDecision* | `/tools/validate-input`, `/approvals/list`, `/approvals/decide` | API/auth/persistence/config, race/replay/substitution tests | JSONL/Postgres preserve decide-once/consume-once; v3 HMAC binding authenticates record ID/origin trace/tenant/subject/tool/action/args/definition before mutation; cross-record, cross-tenant, cross-subject, schema and rotation substitutions fail without burning the valid grant; post-consume authority is an in-process issuer-bound capability | tested | Live multi-worker PostgreSQL/Vault durability is deployment evidence; no F key-rotation work is claimed here |
| PY-012 | Sandbox/test runner | `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py`, `apps/api/src/hallu_defense/services/sandbox_kubernetes.py`, `infra/docker/sandbox_*.py` | SandboxRun, Evidence | `/repo/checks/run` | sandbox backend, Git-inspector, path-safety, exact-boundary and isolation-checker tests | Tests cover traversal, command/network policy, policy-scan TOCTOU, env scrub, typed evidence, artifacts, static symbols, canonical Git lines/ranges, hidden-index/config/attribute bypasses, before/after fingerprints, exact aggregate caps, zero-byte edges, bounded directory/output streaming, identity-aware copy/hash, descendant cleanup, and UID-conditioned Kubernetes lifecycle | tested | Persistent artifact storage is separate scope; runtime still depends on operator-provided tenant-isolated Kubernetes resources |
| PY-013 | Audit ledger | `services/audit.py`, `config.py`, `api/dependencies.py`, `docs/security/audit-ledger.md`, `services/postgres.py`, `infra/rag/pgvector/003_audit_ledger.sql` | VerificationRun, AuditEvent | `/audit/export` | audit tests, redaction tests, config validator, PostgreSQL storage and lifecycle tests | JSONL persists/reloads local verification units and fails closed on corruption; production/staging require `PostgresAuditLedgerStorage`, whose bounded atomic export and history integrity are tracked under PY-019; centralized redaction, private request commitments, and policy-driven retention/deletion gates are implemented | tested | Live PostgreSQL durability, Vault rotation, operator-controlled retention execution, and encrypted backup/restore remain deployment evidence |
| PY-014 | Trace system | middleware/orchestrator/current trace id/OpenTelemetry spans | VerificationRun, ErrorResponse, AuditEvent | all REST endpoints | trace tests | Required endpoints tested for `x-trace-id`; error bodies include trace_id; OpenTelemetry tests prove HTTP spans reuse safe trace IDs and record route/status/outcome without sensitive request attributes; domain-span tests cover verification stages, policy decisions, and sandbox execution without tenant/payload/secret attributes; Compose smoke started the local OTel collector with API OTLP wiring | tested | End-to-end exported span capture from the live collector remains a future assertion |
| PY-015 | Continuous eval system | `evals`, `services/eval_reports.py`, `scripts/dev/generate_verifier_calibration.py` | EvalSmokeReport, EvalScenarioReport, EvalReport | `/evals/reports/publish`, `/evals/reports/list` | smoke/scenario metrics, threshold, calibration drift and runtime publish tests | Runners measure the required safety/quality/latency/cost metrics; versioned thresholds enforce floors/ceilings; deterministic calibration is drift-gated; runtime reports persist and emit gauges | tested | Golden sets and calibration remain deterministic initial baselines rather than live statistical sampling |
| PY-016 | Provider abstraction | `services/providers.py`, `services/secrets.py`, API dependencies/config, `.env.example`, `docs/security/providers.md`, `apps/api/tests/test_provider_adapters.py`, `SECURITY.md` | n/a | n/a | provider adapter unit tests, secret scan, typecheck | OpenAI-compatible, Ollama/local, and deterministic mock providers are selected by settings; OpenAI-compatible credentials are read only through `SecretManager`; mock backend is rejected in production-like environments; injected transport tests prove request payloads, auth header construction, timeout use, response parsing, and no real network dependency | tested | Runtime provider connectivity is not verified locally |
| PY-017 | Provider-backed NLI adjudication fallback | `services/nli.py`, `services/verifier.py`, API dependencies/config, `.env.example`, `docs/security/providers.md`, `apps/api/tests/test_nli_adjudicator.py` | ClaimVerdict | `/claims/verify`, `/verification/run` | NLI adjudicator tests, verifier regression tests, lint/typecheck/API tests | Optional `HALLU_DEFENSE_PROVIDER_NLI_ENABLED` adjudicator requests strict JSON from the selected provider, redacts secret-like prompt text, rejects malformed output and unknown evidence IDs, refuses support without cited evidence, routes high-risk supported claims to human review, and is not used for repo/test deterministic claims; 7 focused tests and full API tests pass | tested | Disabled by default; runtime provider connectivity and calibration are not locally verified |
| PY-018 | Postgres shared connection pool provider | `services/postgres.py`, `apps/api/tests/test_postgres_provider.py` | n/a | n/a | provider/pool unit tests | `services/postgres.py` defines the `SqlConnectionProvider` protocol, a `PooledPostgresProvider` that shares a bounded psycopg connection pool across audit, approval, and corpus-grant storage, and a `RecordingSqlProvider` fake for deterministic tests; `test_postgres_provider.py`: 17 passed covering pool acquire/reuse, bounded capacity, cursor execution, and rollback-on-error | tested | Pool behavior is proven against the recording fake; live psycopg pool behavior against a real PostgreSQL requires Docker/CI and is not exercised on local Windows |
| PY-019 | Postgres audit ledger backend with bounded, atomic verification history | `services/audit.py`, `services/verification_history.py`, `apps/api/tests/test_audit_ledger.py`, `apps/api/tests/test_verification_history.py`, `scripts/ci/check_audit_ledger_config.py`, `infra/rag/pgvector/003_audit_ledger.sql`, `infra/rag/pgvector/013_audit_history_integrity.sql` | VerificationRun, AuditEvent, VerificationRunSummary | `/audit/export`, `/verification/runs/list` | provider transaction/rollback/race tests, coherent snapshot interleaving/parity, deep ownership, pre-cap replay cardinality, tenant/filter/path/envelope/order/ID negatives, config and migration gates | PostgreSQL uses one transaction and conflict-aware `RETURNING` inserts for completion pairs/replay triples; retries load/validate the canonical unit; export reads both collections through one `REPEATABLE READ, READ ONLY` provider and cross-checks visible units, while local storage copies both under one lock and deeply clones ingress/returns; source lookup filters non-null replay markers before `LIMIT 2` and enforces 0/1/>1 cardinality; exports/history reject out-of-scope, malformed, invalid-path, duplicate-ID, over-limit or unordered rows | tested | Backend behavior is proven against stateful/recording providers and structural migration gates; a real PostgreSQL scratch run remains live/CI evidence |
| PY-020 | Postgres approval queue backend with atomic decide-once/consume-once guards | `services/approvals.py`, `apps/api/tests/test_approval_queue.py`, `scripts/ci/check_approval_queue_config.py`, `infra/rag/pgvector/004_approval_queue.sql` | ApprovalRecord, ApprovalExecutionGrant | `/approvals/decide`, `/tools/validate-input` | approval backend tests, config gate | `services/approvals.py` adds `PostgresApprovalQueueStorage` beside the JSONL `ApprovalQueueStorage`, enforcing decide-once and consume-once through atomic `UPDATE ... WHERE ... RETURNING` statements that return no row on a repeated decision or reused grant; migration `004_approval_queue.sql` creates the tenant-scoped approval and grant tables; `test_approval_queue.py` passed and `check_approval_queue_config.py` validated the persistence gate | tested | Atomicity is proven against the recording provider; truly concurrent decide/consume races against a live PostgreSQL remain Docker/CI evidence |
| TS-001 | Official SDK TypeScript | `packages/sdk` | all | REST | SDK tests, typecheck, schema coverage | `npm run test`: 5 SDK tests pass; TS contracts typecheck; schemas cover 33 exported interfaces | tested | Retry behavior and typed error payload details still need expansion |
| TS-002 | Agent tool-calling adapters | `packages/agent-adapters` | ToolCallEnvelope, ToolValidationResponse, ApprovalExecutionGrant | tools | adapter tests, typecheck, build | `@hallu-defense/agent-adapters` provides typed envelope builders, approved execution grant attachment, pre-tool validation, post-tool validation, sanitized output handling, and typed blocked-validation errors; package typecheck/build pass and 5 Vitest tests pass | tested | Initial adapter is provider-agnostic SDK glue; broader framework-specific adapters remain future work |
| TS-005 | Replay UI | console, `apps/api/src/hallu_defense/api/routes.py`, `apps/api/src/hallu_defense/services/orchestrator.py`, `apps/api/src/hallu_defense/services/audit.py`, `apps/api/src/hallu_defense/domain/models.py`, `packages/contracts/**`, `packages/sdk/src/client.ts`, `apps/console/app/run-console.tsx`, `apps/console/e2e/replay.spec.ts` | VerificationReplayRequest, VerificationReplayResponse, VerificationRun | `/verification/replay` | replay API tests, RBAC tests, redaction tests, atomic retry/rollback and duplicate-source tests, SDK unit/live tests, schema/example validation, browser e2e | Replay keeps the public response and `verification_replay` audit signal compatible while committing the replayed run, `verification_completed`, and provenance event atomically/exactly once; provenance is cross-validated against `run.input.replay_of`; retry returns the first committed timestamp; missing/cross-tenant source is 404, duplicate exact sources are stable 409 with zero orchestrator calls, and storage failure is generic 503 | tested | Replay uses the stored redacted snapshot by design; it does not re-run live retrieval against current corpora |
| TS-003 | Next.js Console | `apps/console/**` | VerificationRun and operational contracts | same-origin `/api/*` BFF to allowlisted API endpoints | Console unit tests, static e2e-runtime tests, typecheck, lint, production build, Playwright collection | At launch code candidate `d6c15bd`, the Console unit suite passed 255 tests across 33 files; lint, typecheck and the optimized production build passed, and its deployable-artifact checker scanned 339 artifacts. Independent browser inspection reached `/console`, established the configured local session boundary, rendered the tenant/subject/role and exposed generic unavailable states for the intentionally absent API | tested | The browser inspection is not live OIDC/API/Docker evidence. Earlier 10-test operational execution predates the `/console` migration and modified specs; full Docker-backed Playwright and live Keycloak browser evidence must be rerun on the candidate commit |
| TS-004 | Approval queue UI | console, `apps/console/e2e/approval-queue.spec.ts`, `apps/console/playwright.config.ts`, `apps/console/scripts/run-e2e-api-webserver.ts`, `apps/console/lib/e2e-{python-runtime,api-lifecycle,sandbox}.ts`, `apps/console/e2e/global-teardown.ts`, `scripts/ci/check_e2e_python_source.py`, `scripts/ci/check_sandbox_isolation_config.py`, `.github/workflows/ci.yml` | ApprovalRecord, ApprovalListResponse, ToolCallEnvelope | `/tools/validate-input`, `/approvals/list`, `/approvals/decide` | UI build/typecheck, static lifecycle/config tests, Playwright collection | Console panel and committed Playwright specs retain enqueue/approve/reject and redaction assertions. The e2e runner requires an existing absolute `E2E_PYTHON_BIN`, pins `PYTHONPATH` to this worktree's `apps/api/src`, verifies the imported module path before Docker, derives a worktree/run-scoped scratch image tag, and performs bounded exact-tag cleanup. At `fb111c1e`, 35 e2e-static Vitest tests, 62 focused Python config/preflight/isolation tests, the sandbox checker, exact-source preflight, and a 10-test Playwright collection passed | tested | Full Playwright execution against the real API/Docker sandbox and live Keycloak remain pending current-commit evidence; no browser or Docker run is claimed here |
| TS-006 | Claim/evidence/verdict viewers | console | Claim/Evidence/Verdict | n/a | build/manual | build passed | tested | Needs route-level detail view |
| TS-007 | Policy explanation viewer | console, `apps/console/e2e/operations.spec.ts` | PolicyEvaluationResponse | `/policy/evaluate` | console typecheck/build, runtime smoke, Playwright browser e2e | Console panel calls typed SDK `evaluatePolicy`, renders allow/block action, policy version, explanation, and matched rules; the API policy layer includes the OPA adapter and the local policy gate passed Python plus executable Rego tests | tested | Browser/IdP live proof remains deployment evidence; the Console does not itself execute Rego |
| TS-008 | Diff/test/artifact viewer | console, `apps/console/e2e/operations.spec.ts`, `apps/console/playwright.config.ts` | SandboxRun | `/repo/checks/run` | console typecheck/build, runtime smoke, Playwright browser e2e | Console sandbox panel calls typed SDK `runRepoChecks`, renders commands, exit codes, stdout/stderr snippets, artifacts, `sandbox_command.v1` targets, and `sandbox_inspection.v1` diff/static summaries with redaction helpers; console typecheck/build pass and worker runtime smoke saw HTTP 200 with sandbox panel text; Playwright e2e runs `python --version` through the browser against the real API, asserts `SUPPORTED`, command text, exit code, artifact summary, and keeps sandbox artifacts inside ignored `var/e2e` instead of the repo root | tested | Sandbox isolation hardening remains tracked under SBOX risks |
| TS-009 | Live eval, verification-history, and corpus dashboards | `apps/console/app/live-dashboards.tsx`, `apps/console/lib/console-client.ts`, eval/run/corpus contracts | EvalReport, VerificationRunSummary, CorpusGrant | `/evals/reports/list`, `/verification/runs/list`, `/rag/corpus-grants/list`, `/documents/ingest*` | ordering/deduplication tests, Console typecheck/lint/build, Playwright | Dashboards read persisted API resources through the same-origin BFF, show explicit loading/empty/error states, paginate without duplicate stable IDs, prefer the newest report/run and highest grant version, and contain no local report or demo fallback; focused merge/order tests pass inside the 101-test Console suite | tested | Live data accuracy and persistence remain API/deployment responsibilities; the full real-API browser suite remains integration evidence |
| TS-010 | Browser request concurrency and safe status handling | `apps/console/app/live-dashboards.tsx`, `apps/console/lib/request-coordinator.ts`, `apps/console/lib/api-error.ts` | live Console response contracts | live dashboard and mutation endpoints | epoch/abort/dedupe races, status mapping, pagination overlap | Per-channel monotonic epochs plus `AbortController` supersede stale requests, identical in-flight fingerprints share one promise, unmount/refresh aborts cannot overwrite current state, and 401/403/429/408/5xx/network messages are generic; unit tests cover slow-A/fast-B, identical dedupe, abort versus genuine timeout, status mapping, and overlapping pages | tested | SDK converts aborts to status 408, so the coordinator suppresses them only when its own signal was aborted; no automatic 429 retry is implemented |
| TS-011 | Bilingual public landing, privacy, metadata, accessibility and responsive behavior | `apps/console/app/(marketing-*)/**`, `apps/console/app/opengraph-image/route.ts`, `apps/console/components/marketing/**`, `apps/console/lib/marketing/**`, `apps/console/e2e-marketing/**` | localized marketing content and route metadata | `/`, `/en`, `/privacy`, `/en/privacy`, `/console`, `/opengraph-image` | Vitest, lint/typecheck/build, 320/768/1440 Playwright matrix, axe, keyboard/focus, reduced motion, progressive enhancement, synthetic Web Vitals budgets, visual inspection | Current production `standalone` phase enumerated 216 cases across Chromium/Firefox/WebKit and 320/768/1440: 148 passed, 68 deliberate scope skips, 0 failed. The expanded form/Axe phase ran 117: 77 passed, 40 deliberate skips, 0 failed, covering ES/EN initial form, step two and native-invalid state. Its first run exposed eight contrast failures caused by reveal opacity; opacity was corrected and the complete phase reran green | tested | Native browser-UI 200% zoom is not claimed: the automated check is 2x device-scale equivalence. Manual screen-reader, contrast and native-zoom review plus BrowserStack minimum-version sessions remain required. Synthetic lab budgets are not field Core Web Vitals, and legal/controller copy remains deployment-governed |
| SEC-001 | OIDC JWT/JWKS authentication | `services/auth.py`, `services/oidc.py`, `api/dependencies.py`, `api/middleware.py`, `config.py`, `scripts/ci/oidc_provider_smoke.py`, `docs/security/auth-rbac.md`, `infra/security/auth-policy.json`, `infra/security/keycloak/realm-hallu-defense.json`, `scripts/dev/live_keycloak_oidc_smoke.py`, `.github/workflows/live.yml` | n/a | all | auth tests, OIDC JWT tests, OIDC route auth tests, auth config gate, deployed-provider smoke gate, OpenAPI export | `Principal` abstraction maps subject and roles from verified auth claims; `signed_headers` mode verifies an HMAC-SHA256 trusted gateway signature over tenant/subject/roles/timestamp using `SecretManager`; `oidc_jwt` mode verifies stdlib-only RS256 JWT signatures against JWKS, enforces `kid`, issuer, audience, required `exp`, optional `nbf`/`iat` with skew, subject claim, roles claim, and tenant claim; JWKS can come from local path, direct remote URL, or OIDC discovery document `jwks_uri`; remote JWKS is cached by TTL and refreshed once on unknown `kid`; `get_request_context()` rejects tenant header/token mismatches and exposes the authenticated tenant to middleware audit; `load_settings()` rejects production/staging with optional auth or unsigned claim headers and requires issuer/audience plus a JWKS source in `oidc_jwt`; `oidc_provider_smoke.py` validates a deployed provider with direct JWKS or discovery plus a short-lived JWT when enabled and skips safely by default; tests cover valid RS256 JWTs, tampered signatures, wrong issuer, expired tokens, remote cache, TTL refresh, discovery, unknown-kid refresh, FastAPI context wiring, route-level JWT tenant propagation, route-level verifier/auditor role enforcement, tenant mismatch, signed headers, endpoint roles, smoke skip, missing smoke JWT, discovery/JWKS/JWT success, expected-claim mismatch, insecure remote URL rejection, and auth production config validation; Batch 2 adds live OIDC verification against a local Keycloak provider (see SEC-014): the `realm-hallu-defense.json` confidential client mints an RS256 client_credentials token whose `aud`/`tenant_id`/roles claims match the verifier, exercised by the env-gated `live_keycloak_oidc_smoke.py` offline `--api` tests and confirmed by an independent live token mint (passed) | tested | Local CI runs the deployed-provider smoke in skipped mode; live provider validation requires external URL/discovery and a short-lived test JWT; Batch 2 adds a runnable self-hosted live provider through the local Keycloak realm, exercised by the live.yml keycloak-live job and verified independently via a real RS256 client_credentials token mint |
| SEC-002 | RBAC/ABAC | `services/auth.py`, `services/oidc.py`, `services/rag_access.py`, `services/corpus_grants.py`, `api/dependencies.py`, routes, policy/security/opa, docs/security, auth config gate | n/a | all guarded endpoints plus policy and RAG endpoints | auth tests, OIDC JWT tests, auth config tests, approval API tests, policy tests, RAG ABAC tests, corpus grant registry tests | Initial ABAC tenant/resource rule blocks cross-tenant access through Python policy and Rego baseline; RAG access policy stamps ingested evidence metadata with `owner_tenant_id`, rejects cross-tenant owner metadata, enforces metadata-declared `corpus_writer_roles`, rejects unreadable inline documents, filters persistent evidence by `corpus_reader_roles`, applies active durable tenant/corpus registry reader/writer grants, and ignores disabled registry grants; corpus grant mutation/list/history/history-diff routes always require authenticated `rag_writer`/`verifier` roles even when local auth is optional; corpus grant storage supports fail-closed JSONL, injectable PostgreSQL replay, and DSN-backed psycopg runtime wiring; high-risk/sensitive action policies require review; `ENDPOINT_ROLE_REQUIREMENTS` defines guarded roles for metrics, claims, evidence, ingestion, corpus grants, tools, policy, approvals, sandbox, audit, and verification | tested | Environment/sensitivity ABAC and pooled PostgreSQL connection lifecycle remain future work |
| SEC-003 | Tenant isolation | dependencies/middleware/retrieval/SDK/MCP/auth/RAG access policy, `apps/api/src/hallu_defense/services/sandbox_kubernetes.py` | all | all | tenant, auth, OIDC, RAG and Kubernetes sandbox boundary tests | Verified identities propagate through API/SDK/MCP/audit; approvals bind tenant and subject; RAG grants/history/diff remain tenant-scoped; sandbox construction rejects tenant mismatch before Kubernetes Job creation | tested | Existing chunks may lack owner metadata; Kubernetes still requires one namespace/release/ServiceAccount/PVC per tenant because names and labels are not an authorization boundary |
| SEC-004 | Audit ledger active | audit service, verification history service, central content redactor, docs/security/audit-ledger.md, migration 013, config, CI | VerificationRun, AuditEvent | `/audit/export`, `/verification/runs/list` | bounded redaction, private HMAC commitment, nested ownership, tenant isolation, coherent snapshot/parity, atomic rollback/race, config and migration tests | Memory/JSONL remain local/test only and production/staging fail closed to PostgreSQL; every persisted run/event crosses the central bounded redactor and incomplete/non-finite/cyclic payloads fail before write; a Vault-backed non-exported HMAC binds normalized pre-redaction content and rejects distinct originals that minimize identically; validated replay identities remain intact; storage deep-owns all graphs and export shares one coherent snapshot | tested | Sensitive legacy rows without a commitment fail closed on retry; live database durability, Vault rotation and operational migration execution remain deployment evidence |
| SEC-005 | Secret/PII redaction | `content_security.py`, tool_safety, approvals, telemetry | ToolValidation | `/tools/validate-output` | adversarial recursive/serialization/false-positive tests | Bounded Unicode-aware traversal redacts secret/PII values and keys, signed URL/Azure/Google/AWS/API/header/cookie credentials, structured paths, compact labeled PII, and compositional JSON; cycles, limits, NaN/±inf, decoder recursion, and invalid surrogates fail closed. Trusted-schema postvalidation prevents invalid sanitized output from crossing REST/MCP; the expanded Front B suite passes 396 focused tests | tested | Broader non-US/domain-specific identifiers remain future calibrated hardening |
| SEC-006 | Secret scan in CI | `scripts/ci/secret_scan.py`, `apps/api/tests/test_secret_scan.py`, `Makefile`, `.github/workflows/security.yml` | n/a | n/a | secret scan, focused scanner tests | `secret_scan.py` returned `No obvious secrets found.`; `test_secret_scan.py`: 5 passed and covers synthetic secret detection, private-key marker detection, generated/local tooling skips, lockfile/binary skips, and current-repo cleanliness | tested | Pattern-based only; scanner intentionally skips generated/local tooling directories and does not replace dedicated secret-management controls |
| SEC-007 | Dependency audit in CI | `apps/api/pyproject.toml`, `requirements/python/*`, `scripts/ci/python_dependency_audit.py`, `apps/api/tests/test_python_dependency_audit.py`, `Makefile`, `.github/workflows/*.yml`, `SECURITY.md` | n/a | n/a | pip-audit wrapper tests, Python dependency audit, npm audit | The accepted campaign's four exact Python locks reported no known vulnerabilities and `pip check` found no broken requirements. At `fb111c1e`, both full and production-only npm audit reported 0 vulnerabilities after the lockfile change | tested | Current npm evidence used host Node/npm 24.14.0/11.13.0 rather than the pinned 24.18.0/11.16.0 CI lane; the local editable Python package is not on PyPI, and container scanning remains separate |
| SEC-008 | Encryption config | `infra/security/encryption-policy.json`, `docs/security/encryption.md`, `scripts/ci/check_encryption_config.py`, `apps/api/tests/test_security_config.py`, `Makefile`, `.github/workflows/*.yml`, `SECURITY.md` | n/a | n/a | config validator, negative policy tests, CI security gate | `check_encryption_config.py` validates 9 components; tests reject plaintext external interfaces, weak TLS, and plaintext/local key management; full API tests, lint, typecheck, secret scan, JSON schema checks, and npm audit pass | tested | This proves versioned policy and CI enforcement, not runtime TLS/encrypted volume activation in local Docker or future Kubernetes |
| SEC-009 | Egress allowlist | `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py`, `infra/docker/sandbox.Dockerfile`, policy/OPA | SandboxRun, PolicyEvaluation | `/repo/checks/run`, `/policy/evaluate` | sandbox, Docker backend, policy, and isolation config tests | Deny-mode sandbox preflight blocks network attempts; Python policy and Rego baseline require human review for allowlisted sandbox network access; Docker backend argv pins `--network=none`, its mutation test fails if the recorded argv lacks it, and `check_sandbox_isolation_config.py` statically pins the flag. The enabled local Docker smoke proved outbound failure and outside-workspace write denial with exact scratch cleanup | tested | A destination-aware enterprise allowlist is still future work. Local Docker evidence is not a production-node or remote live-lane result; Kubernetes evidence is tracked separately under FND-014/CI-029 |
| SEC-010 | Vault-compatible secret manager integration | `apps/api/src/hallu_defense/services/secrets.py`, `apps/api/src/hallu_defense/config.py`, `apps/api/src/hallu_defense/api/dependencies.py`, `docker-compose.yml`, `docker-compose.prod.yml`, `infra/security/secrets-policy.json`, `scripts/ci/check_secrets_config.py`, `scripts/dev/bootstrap_local_vault.py`, `scripts/dev/live_vault_secrets_smoke.py`, `docs/security/secrets.md`, `SECURITY.md`, `Makefile`, `.github/workflows/*.yml` | n/a | `/metrics` and provider credential consumers | secret manager unit tests, bootstrap/smoke tests, secrets/profile gates | Env secrets are local-only; production requires Vault. Local bootstrap seeds metrics, auth and backup keys; metrics scrape auth, backup drill and the explicit production provider secret name consume the abstraction without logging values | tested | Enabled Vault connectivity and secret rotation against a deployed service remain live/operational evidence |
| SEC-011 | Container image scanning | `.github/workflows/security.yml`, `infra/docker/*.Dockerfile`, `infra/docker/otel-collector-builder.yaml`, `scripts/ci/check_container_scan_config.py`, `apps/api/tests/test_container_scan_config.py`, `docs/security/container-scanning.md`, `docs/qa/2026-07-13-mass-qa-acceptance.md`, `SECURITY.md`, `Makefile` | n/a | n/a | exact ten-image matrix validator, mutation tests, security workflow scan | Trivy 0.72.0 rebuilt and scanned the current ten-image first-party matrix plus immutable Prometheus and Redis. Initial OpenSearch OS-package and SeaweedFS `golang.org/x/image` findings were fixed at the image inputs; all twelve final scans report zero HIGH/CRITICAL. The exact-matrix checker and its mutation tests pass | tested | Evidence is local amd64 scratch execution, not a current GitHub Actions run or multi-architecture publication. Actions is blocked before runner allocation by the repository billing/spending limit; remote provenance remains required before `accepted` |
| SEC-012 | Backup/restore and retention policy | `infra/security/backup-retention-policy.json`, `apps/api/src/hallu_defense/services/data_lifecycle.py`, `scripts/dev/backup_restore_drill.py`, `scripts/dev/minio_backup_restore_drill.py`, `scripts/dev/run_retention_execution.py`, `scripts/ci/check_backup_retention_config.py`, `scripts/ci/check_minio_backup_drill.py`, `apps/api/tests/test_backup_retention_config.py`, `apps/api/tests/test_data_lifecycle.py`, `apps/api/tests/test_backup_restore_drill.py`, `apps/api/tests/test_live_minio_backup_restore_drill.py`, `docs/security/backup-restore-retention.md`, `.github/workflows/live.yml` | n/a | n/a | policy/config, lifecycle SQL, deletion confirmation, encrypted drill orchestration tests, live S3 replica/restore lane | Gates validate retention/backup requirements, runtime scripts, and mandatory live-lane wiring; focused tests prove minimum retention, tenant-scoped parameterized deletion, audit events, Vault-keyed encryption flow and restore parity; the live S3 test passed against the pinned SeaweedFS source with tenant isolation, AES-GCM replica restore, corruption rejection, and empty synthetic-prefix cleanup | tested | The combined live pg_dump-to-object-store-to-scratch-PostgreSQL drill and operator-controlled deletion remain deployment evidence; the S3-only encrypted replica/restore boundary is now locally proven and wired into the isolated live workflow |
| SEC-013 | Prompt injection and data poisoning policy guards | content_security/policy/retrieval/orchestrator/Rego/evals | PolicyEvaluation, VerificationRun, Evidence | `/policy/evaluate`, `/evidence/retrieve`, `/verification/run`, `/tools/*` | scanner/retrieval/verification/policy/Rego/eval tests | Direct/indirect injection and poisoning are server-derived restrictive facts; ToolSafety applies block before approval review; Python/Rego primary-rule precedence is exact and adversarial combined-signal tests prevent rewrite/review from downgrading a block | tested | Deterministic scanners remain evadable and require ongoing calibration |
| SEC-014 | OIDC live verification via a local Keycloak provider (importable realm plus client_credentials RS256 claim checks) | `infra/security/keycloak/realm-hallu-defense.json`, `docker-compose.yml`, `scripts/dev/live_keycloak_oidc_smoke.py`, `scripts/ci/check_local_runtime_config.py`, `apps/api/tests/test_local_runtime_config.py`, `.github/workflows/live.yml` | n/a | all guarded endpoints | local runtime config validator, offline Keycloak OIDC smoke tests, live Keycloak OIDC smoke | The `keycloak` service (quay.io/keycloak/keycloak:26.3, `start-dev --import-realm`, host port 8081) imports `realm-hallu-defense.json`, which defines 6 roles, a confidential `hallu-defense-api` client_credentials client, and `aud`/`tenant_id`/`roles` mappers with no PEM material; `check_local_runtime_config.py` validates the realm within the current 14-service/4-volume runtime; the env-gated `live_keycloak_oidc_smoke.py` offline `--api` path validates the claims with a TestClient and a fake JWT; an independent live verification against a running Keycloak minted an RS256 client_credentials token with `aud=hallu-defense-api`, `tenant_id=tenant-a`, 6 roles, and a 2-key JWKS (passed); `docker compose config --quiet` passed with the keycloak service | tested | Live token minting was verified independently against a real Keycloak; the full `--api` smoke against a running Keycloak and the live.yml keycloak-live job run in CI/integration and remain deployment evidence |
| SEC-015 | Policy-driven retention and tenant-scoped deletion execute through guarded SQL and emit audit events | `apps/api/src/hallu_defense/services/data_lifecycle.py`, `scripts/dev/run_retention_execution.py`, `infra/security/backup-retention-policy.json`, `apps/api/tests/test_data_lifecycle.py`, `docs/security/backup-restore-retention.md` | AuditEvent | n/a | policy parsing, minimum-retention, dry-run, parameterized tenant deletion, confirmation and audit tests | `test_data_lifecycle.py` proves minimum-day enforcement, parameterized cutoff/tenant SQL, dry-run behavior, explicit tenant deletion, and retention/deletion audit metadata; backup-retention config gate passes | tested | SQL behavior is proven with recording fakes; enabled deletion against a live production database requires operator-controlled deployment evidence |
| SEC-016 | Encrypted backup/restore drill integrates PostgreSQL, Vault-compatible keys, MinIO, and parity verification | `scripts/dev/backup_restore_drill.py`, `apps/api/tests/test_backup_restore_drill.py`, `scripts/ci/check_backup_retention_config.py`, `docs/security/backup-restore-retention.md`, `.github/workflows/live.yml` | n/a | n/a | command construction, key handling, encryption, object upload, scratch restore, checksum/row parity, env-gate tests | In isolated Compose project `halluacceptminio98a6bd1`, the enabled PostgreSQL -> encrypted object -> MinIO replica -> scratch restore drill passed (`1 passed in 4.45s`) with checksum/row parity; its container, exact volume and network were removed. Unit/config gates remain green | tested | This is disposable local runtime evidence, not a restore against production storage, KMS or retained backups; remote live-lane evidence and recovery-time validation remain required |
| SEC-017 | Console OIDC BFF keeps OAuth credentials server-side and closes CSRF/session/logout boundaries | `apps/console/app/auth/**`, `apps/console/app/api/[...path]/route.ts`, `apps/console/lib/{auth-store,browser-session,console-bff,console-client,oidc,request-security}.ts`, `apps/console/proxy.ts`, `docs/security/console-oidc.md` | Console identity/session metadata | `/auth/*`, same-origin `/api/*` | OIDC/JWT/state/PKCE/session/logout/BFF/rate-limit tests, static e2e-runtime tests, Playwright collection | State/nonce/PKCE S256 and issuer-bound discovery/JWKS/end-session validation remain fail closed; login, callback rotation, opaque Strict `HttpOnly` sessions, BFF origin/CSRF/allowlist/size/media controls, sanitized failures, logout and the explicit marketing-to-Console session boundary are covered by the current 255-test Console suite. Typecheck, lint and production build pass; browser inspection rendered the isolated Console with the configured local tenant/subject/role | tested | This local unsigned test-mode inspection is not current Keycloak/OIDC proof. Process-local state/session/rate stores require an atomic shared implementation for replicas; the earlier live Keycloak/browser evidence predates the modified candidate and must not be treated as current execution |
| SEC-018 | Approval authorization v3 binds trusted semantics, environment, and bounded key rotation | `apps/api/src/hallu_defense/services/{approvals,tool_safety}.py`, `apps/api/src/hallu_defense/{config.py,api/dependencies.py,api/routes.py}`, `apps/api/tests/{test_approval_queue,test_trusted_tool_approvals,test_oidc_route_auth}.py`, `scripts/ci/check_approval_queue_config.py`, `scripts/dev/live_postgres_persistence_smoke.py`, `docker-compose.prod.yml`, `infra/k8s/helm/hallu-defense/**`, `docs/security/approvals.md` | `ApprovalRecord`, private `ApprovalBinding`, issuer-bound capability | `/approvals/*`, `/tools/validate-input` | JSONL/PostgreSQL rotation, previous-key expiry/TTL, legacy/provisional format, environment, trusted-definition, atomic consume and capability replay tests | Domain-separated v3 commits approval/trace/tenant/subject/environment/tool/action/argument hash/definition version+digest/algorithm/opaque key ID; production requires Vault HMAC plus explicit non-derived ID, one distinct previous key may overlap for at most seven days, grants cannot outlive that overlap, ToolSafety rechecks environment, and legacy v1/v2/provisional records require explicit archival and reapproval | tested | Focused suites and static Helm/Compose gates pass; enabled multi-worker PostgreSQL plus Vault rotation/restart remains live deployment evidence and incompatible legacy rows must be archived before rollout |
| SEC-019 | Release encryption compliance is externally attested and replay-safe | `scripts/ci/verify_release_encryption_evidence.py`, `.github/workflows/verify-release-encryption.yml`, `packages/contracts/schemas/release-*.schema.json`, `apps/api/tests/test_release_encryption_evidence.py`, `apps/api/tests/test_release_encryption_workflow_config.py`, `docs/security/{release-encryption-evidence,encryption-at-rest-and-transit}.md` | six release encryption evidence/control schemas | n/a | schema, signature/HMAC, provenance/material, tenant/environment, timestamp, rotation, replay/CAS/rollback, path-safety and workflow negatives | The verifier accepts only bundle/report CLI paths, loads protected trust/deployment/keyring/replay inputs from the external control plane, rejects embedded trust or policy, verifies Ed25519 plus keyed commitments, and emits an authenticated report; the two-phase workflow remains non-compliant until the external monotonic CAS anchor matches the authenticated next state | tested | The workflow cannot prove the external controller's atomicity by itself; live CAS and deployment attestation remain operator evidence |
| SEC-020 | Privacy-preserving, disabled-by-default public demo intake | `apps/console/app/demo-request/route.ts`, `apps/console/lib/{marketing,demo-request}/**`, `apps/console/instrumentation.ts`, `docker-compose*.yml`, `infra/k8s/helm/hallu-defense/**`, `infra/prometheus/demo-request-alerts.yml`, `docs/deployment/marketing-launch.md` | `DemoRequestPayloadV1`, `DemoRequestAcceptedResponseV1`, `DemoRequestErrorResponseV1`, internal `NormalizedDemoRequest` | `/demo-request`, `/metrics` | schema/origin/provenance/size/consent tests, no-PII responses/metrics, rate limits, payload-bound idempotency/concurrency, webhook HMAC/retry, enabled browser/Axe flows and scratch Redis Cluster | Production requires Redis Cluster mode and DB 0; the adapter uses discovery, first-key EVAL routing, no replica reads, bounded redirects, and propagates decoded credentials to every discovered node. A six-node scratch Cluster proved real `MOVED`, reserve/delivery/finalize/duplicate semantics and failover; a second authenticated six-node probe wrote/read/deleted slot 7272 outside the seed master and cleaned all resources. Ambiguous `dispatching` retries increment `hallu_demo_dispatching_guard_total`; the production Prometheus config loads `HalluDefenseDemoDispatchingGuardObserved`, scrapes Console with its distinct bearer, and Helm requires an exact Console metrics peer. The local disabled profile intentionally loads neither source nor alert. Form/Axe passed 77/117 with 40 deliberate skips and zero failures | tested | Production still requires legal/controller/contact approval, exact domain/ingress, file-backed secrets, target `rediss://` CA/hostname proof, CRM retention/deletion and a downstream consumer that honors `Idempotency-Key`. Cluster smokes used plaintext isolated networks, not production TLS. The stock in-cluster Console scrape is HTTP behind bearer plus NetworkPolicy; environments requiring encrypted east-west traffic need a real TLS/mTLS listener. A process crash after `dispatching` can suppress retry for up to 24h, trading availability for duplicate prevention |
| RAG-001 | Document ingestion | retrieval service, RAG index boundary, ingestion route/outbox/worker, RAG access policy, corpus grants | DocumentIngestion*, Evidence | `/documents/ingest`, `/documents/ingest/status`, `/evidence/retrieve` | sync/async ingestion, contracts, SDK/MCP, ABAC, worker/backfill tests | Ingestion applies tenant/corpus policy before sync indexing or durable async enqueue; PostgreSQL outbox, worker and idempotent reindex pipeline are covered under RAG-008/RAG-009, including a local real-process crash/restart smoke | tested | Managed-service throughput, multi-worker scale and target-environment recovery remain deployment work |
| RAG-002 | Structural chunking | retrieval service, RAG index adapters | Evidence | `/evidence/retrieve`, `/documents/ingest` | retrieval and RAG index adapter tests | Retriever now preserves paragraph chunking for plain text and adds Markdown heading-aware section chunks with section heading/path/level metadata, flattened persistent-index metadata, and reconstructed `structured_content.structure` for OpenSearch/pgvector results; OpenSearch index writes use tenant-scoped physical document IDs while preserving public `evidence_id`; focused RAG adapter tests pass with explicit same-evidence-id/different-tenant overwrite regression coverage | tested | Parser intentionally covers conservative Markdown-style headings; richer HTML/PDF/table structural parsing remains future work |
| RAG-003 | BM25/OpenSearch search | `services/rag_index.py`, retrieval route, `infra/rag/opensearch/evidence-index-template.json`, `docker-compose.yml`, `scripts/dev/bootstrap_opensearch_template.py`, `scripts/dev/live_opensearch_rag_smoke.py`, `scripts/ci/check_rag_persistence_config.py`, `docs/rag/persistent-indexes.md` | Evidence | `/evidence/retrieve`, `/documents/ingest` | adapter query tests, tenant filter tests, OpenSearch bootstrap tests, live OpenSearch smoke, RAG persistence config validator | OpenSearch adapter builds tenant-scoped bulk and search payloads; search filters by tenant, context refs, and metadata; cross-tenant hits are ignored; bulk item errors fail closed; Compose includes pinned OpenSearch and API backend wiring; template declares `tenant_id` as required query filter; backend can install the template through `_index_template`; live Docker OpenSearch smoke installs the template, creates a smoke index, indexes tenant A/B synthetic documents, refreshes/searches, asserts tenant isolation, and removes the smoke index; bootstrap dry-run and focused tests pass | tested | Live smoke is opt-in and local-service only, not default CI; managed OpenSearch, backfill, and load validation remain pending |
| RAG-004 | Vector search pgvector/Qdrant | `services/rag_index.py`, `services/rag_backfill.py`, `scripts/dev/live_pgvector_rag_smoke.py`, `infra/rag/pgvector/*.sql`, `scripts/dev/apply_postgres_migrations.py`, `docker-compose.yml`, `docs/rag/persistent-indexes.md`, `docs/rag/backfill.md` | Evidence | `/evidence/retrieve`, `/documents/ingest` | adapter SQL, tenant isolation, live pgvector, repeatable migration and reindex tests | Pgvector adapter uses tenant-scoped parameterized queries and dimension validation; repeatable migrations and idempotent reindex/backfill are packaged in the API image. Reconciliation now derives its stable advisory lock from the authoritative group `corpus_id`; during the compatibility window it first acquires the historical JSON-filter lock and then the stable lock in deterministic order, preserving exclusion with old pods during rolling upgrades. Exact tests pin both keys and order | tested | The legacy lock must remain until no supported rollout can contain the previous binary. Qdrant is not implemented; managed pgvector pooling/load and enabled live backfill at scale remain future work |
| RAG-005 | Metadata filtering | retrieval, persistent RAG adapters, RAG access policy, corpus grant registry | Evidence | `/evidence/retrieve` | retrieval tests, cross-tenant owner filter tests, corpus reader-role tests, corpus grant tests | endpoint test proves `metadata_filter` excludes non-matching inline document metadata; persistent adapter tests prove metadata filters are pushed to OpenSearch and pgvector; route test proves cross-tenant owner metadata filters are rejected with 403 before persistent backend search; corpus reader-role tests prove unreadable inline documents are rejected and unreadable persistent hits are filtered from evidence and claim maps; registry grant tests prove persistent evidence with `metadata.corpus_id` is filtered unless the principal has the durable reader role | tested | Metadata filter semantics are exact-match only |
| RAG-006 | Reranking/authority/freshness scoring | retrieval | Evidence | `/evidence/retrieve` | retrieval tests | service test proves official/fresh evidence outranks stale unknown evidence and exposes score trace; persistent adapter hits preserve retrieval trace metadata | tested | External search backend scoring calibration pending |
| RAG-007 | Contradictory source detection | verifier/retrieval | ClaimVerdict | `/claims/verify` | contradiction tests | verifier test proves relevant sources with conflicting numeric values return `CONTRADICTED` | tested | Current detector is deterministic numeric contradiction, not full semantic NLI |
| RAG-008 | PostgreSQL ingestion outbox storage | `infra/rag/pgvector/006_ingestion_outbox.sql`, `apps/api/src/hallu_defense/services/ingestion_jobs.py`, `apps/api/tests/test_ingestion_jobs.py`, `apps/api/tests/test_apply_postgres_migrations.py`, `scripts/dev/live_ingestion_worker_smoke.py` | n/a | n/a | SQL-shape, guarded transition, terminal-row and migration tests plus live crash/recovery | Migration creates tenant-scoped jobs; queue implements enqueue, `FOR UPDATE SKIP LOCKED` claim, guarded completion, retry/dead-letter backoff and stale-lock recovery. A real isolated PostgreSQL smoke proved lease expiry/rotation, old-token fencing, exact terminal audit, tenant isolation and zero residual rows/database | tested | Disposable local evidence does not prove managed PostgreSQL load, multi-worker scale or production latency; runtime worker behavior is tracked separately under RAG-009 |
| RAG-009 | Durable ingestion worker and idempotent tenant/corpus reindex pipeline | `apps/api/src/hallu_defense/worker.py`, `apps/api/src/hallu_defense/services/ingestion_jobs.py`, `apps/api/src/hallu_defense/services/rag_backfill.py`, `scripts/dev/run_rag_backfill.py`, `scripts/dev/live_ingestion_worker_smoke.py`, `docker-compose.yml`, `docs/rag/backfill.md`, `.github/workflows/live.yml` | DocumentIngestionStatusResponse, Evidence | `/documents/ingest`, `/documents/ingest/status` | worker retry/dead-letter/audit/metrics tests, reindex pagination/idempotency tests, config/live-smoke wiring and real process crash/restart | Focused ingestion tests pass for atomic claims, retry/dead-letter transitions, stale-lock recovery, terminal audit state, metrics and tenant-safe reindex/upsert. The enabled local smoke killed the actual worker after claim, waited on PostgreSQL time/lease expiry, restarted it, proved a new fencing token and successful exactly-once terminal outcome, then removed its unique database/Compose resources | tested | No managed-service load or multi-host concurrency result is claimed. The remote live job remains unobserved because GitHub Actions is blocked before runner allocation |
| SBOX-001 | Network denied by default | `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py` | SandboxRun | `/repo/checks/run` | network denied test, Docker argv test, isolation config gate | Python script importing `urllib` is blocked before execution when `network_policy=deny`; Docker backend always includes `--network=none`; focused Docker tests and the static isolation checker both fail if `--network=none` is removed | tested | Preflight remains layer 1; real OS network isolation is exercised by the opt-in live smoke and CI live job when Docker is available |
| SBOX-002 | Filesystem scoped to workspace | `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py` | SandboxRun | `/repo/checks/run` | path traversal, script escape, Docker argv and live-smoke tests | Traversal and `python ../outside.py` are rejected; Docker mounts source read-only at `/hallu-source`, creates a separate bounded tmpfs working copy at `/workspace`, uses a read-only root and hardened `/tmp`, and discards the container/working copy after each run | tested | Docker Desktop path translation is covered by argv and live scratch validation; production uses Kubernetes PVC source plus bounded `emptyDir` |
| SBOX-003 | Destructive command prevention | `apps/api/src/hallu_defense/services/sandbox.py` | SandboxRun | `/repo/checks/run` | command tests, isolation config gate | Python script containing `shutil.rmtree` is blocked by destructive-operation policy; Docker backend is additive and keeps `DESTRUCTIVE_PATTERNS` in `SandboxRunner` as layer 1 before container execution | tested | Pattern-based policy remains conservative; container isolation reduces blast radius but does not replace preflight |
| SBOX-004 | stdout/stderr/exit codes captured | sandbox | SandboxRun, Evidence | `/repo/checks/run` | command tests, bounded streaming output tests | sandbox tests assert exit codes/stdout for artifact and env probe commands; command evidence includes command, argv, command_kind, command_target_tokens, exit_code, stdout, stderr, and network_policy; adversarial helpers write output beyond the retained cap and prove both Docker control and batch-runner paths drain without retaining unbounded bytes | tested | Detached descendants can keep inherited pipes open until the outer container deadline; output retained by the API remains bounded |
| SBOX-005 | Artifact capture | sandbox | SandboxRun | `/repo/checks/run` | artifact, aggregate-byte and unsafe-path decode tests | Generated regular files below `artifacts/` and `reports/` are returned in `SandboxRun.artifacts`; control-plane inspection is returned separately as `sandbox://inspection` evidence. Both fallback and batch paths bound aggregate content/path bytes and reject absolute/traversal artifact paths | tested | Persistent object storage is separate scope |
| SBOX-006 | Secrets unavailable unless mounted | `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py` | SandboxRun | `/repo/checks/run` | secret env test, Docker minimal-env test | Host sandbox command cannot read inherited `API_KEY`; stdout shows fallback `missing`; Docker backend uses a minimal container env allowlist and focused tests prove sensitive names/values are not present in the recorded docker argv | tested | Explicit secret mounts are not implemented; Docker CLI still inherits the operator environment as needed to reach the Docker daemon |
| SBOX-007 | Git diff inspection | sandbox | SandboxRun | `/repo/checks/run` | git diff sandbox tests, adversarial canonical-patch tests | sandbox returns `sandbox://inspection` evidence with git status, `diff_files`, `changed_ranges`, `changed_lines`, and `changed_symbols`; tests initialize scratch repositories and verify modified Python/TypeScript files, added-line evidence, filenames containing spaces and patch-header text, and rejection of noncanonical patch headers | tested | Uses local Git metadata; deeper semantic implementation validation is still conservative |
| SBOX-008 | AST/static inspection | sandbox | SandboxRun | `/repo/checks/run` | static inspection sandbox test | `sandbox://inspection` evidence includes inspectable files, Python class/function/method symbols parsed by `ast`, and conservative JS/TS symbols without writing a report into source | tested | Static symbol extraction is intentionally conservative; deeper language analyzers can improve recall later |
| SBOX-009 | Deterministic repo claim verification | verifier/sandbox evidence | ClaimVerdict, Evidence, SandboxRun | `/claims/verify`, `/repo/checks/run` | verifier repo-state and sandbox bridge tests | verifier blocks loose text evidence for repo file claims, supports `service.py` from sandbox file inventory, supports Python `fetch` and TypeScript `loadUser`/`ApiClient.fetchUser` from static evidence, blocks missing symbols, supports file diff claims from git `diff_files`, supports symbol diff claims only from git `changed_symbols`, verifies repo claims directly from `SandboxRun.evidence`, and expanded `/verification/run` scenarios block false file/function claims from `sandbox_inspection.v1` evidence | tested | Semantic “implements Y” diff analysis still pending |
| SBOX-010 | TypeScript/JavaScript static symbol extraction | sandbox/verifier | SandboxRun, Evidence, ClaimVerdict | `/repo/checks/run`, `/claims/verify` | TS/JS static inspection and verifier tests | sandbox reports TypeScript `ApiClient`, `ApiClient.fetchUser`, `loadUser`, and `parseUser`; verifier supports TypeScript function and method claims from `SandboxRun.evidence` | tested | Regex-based static scanner covers common declarations; full TypeScript AST parser pending |
| SBOX-011 | Diff-to-symbol correlation | sandbox/verifier | SandboxRun, Evidence, ClaimVerdict | `/repo/checks/run`, `/claims/verify` | sandbox diff-symbol tests, verifier diff-symbol tests | sandbox correlates unified git hunks to nearest static Python/TS symbols through `git.changed_symbols`; verifier supports `The diff updates the function loadUser in service.ts` only when changed-symbol evidence exists and blocks missing changed symbols | tested | Correlation proves symbol touched, not full semantic correctness of the implementation |
| SBOX-012 | Implementation/fix claim changed-line evidence | sandbox/verifier | SandboxRun, Evidence, ClaimVerdict | `/repo/checks/run`, `/claims/verify`, `/verification/run` | changed-line and implementation-claim verifier tests, expanded eval scenarios | sandbox captures added/removed git lines in `git.changed_lines`; verifier blocks file-only `implements cache` claims when added lines do not contain asserted implementation terms, supports symbol-scoped implementation claims when changed lines contain the asserted terms, and `/verification/run` scenario coverage exercises the block path end-to-end | tested | Term matching is deterministic and conservative; it does not prove arbitrary semantic correctness |
| SBOX-013 | Fix/validation claims require command evidence | verifier/sandbox evidence | SandboxRun, Evidence, ClaimVerdict | `/claims/verify`, `/repo/checks/run`, `/verification/run` | missing/failing/successful command-evidence tests, expanded eval scenarios | verifier requires relevant successful command evidence for repo implementation claims that say fixed, validated, works, tests pass, or build/compile; missing relevant commands block, failing commands contradict, successful commands are included in verdict evidence, and expanded evals include a fixed-cache claim with command evidence | tested | Command class relevance is keyword-based; SBOX-014 adds target overlap for named behavior/file/symbol |
| SBOX-014 | Focused command target mapping for fix/validation claims | verifier/sandbox evidence | SandboxRun, Evidence, ClaimVerdict | `/claims/verify`, `/repo/checks/run`, `/verification/run` | broad-command rejection and targeted-command success/failure tests, expanded eval scenarios | verifier derives command target terms from asserted implementation terms, requested files, requested symbols, and changed symbol records; broad successful commands without target overlap block, while targeted passing/failing commands support or contradict the claim deterministically; expanded evals cover both broad-command rejection and targeted-command support | tested | Target matching now prefers structured `command_target_tokens` when present and falls back to lexical text only for legacy evidence |
| SBOX-015 | Structured sandbox command metadata | sandbox/verifier | SandboxRun, Evidence, ClaimVerdict | `/repo/checks/run`, `/claims/verify`, `/verification/run` | sandbox command metadata tests, verifier structured target tests, expanded eval scenarios | sandbox emits `sandbox_command.v1` command evidence with argv, executable, command_kind, target args/tokens, and targeted flag; verifier uses command_kind for test/build/validation class matching and command_target_tokens for focused claim matching; scenario evals now include structured broad and targeted command metadata | tested | Metadata is deterministic command parsing, not proof that a named test semantically covers the changed behavior |
| SBOX-016 | Docker sandbox execution backend pins OS isolation flags | `apps/api/src/hallu_defense/services/sandbox_exec.py`, `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/config.py`, `infra/docker/sandbox.Dockerfile`, `apps/api/tests/test_sandbox_docker_backend.py` | SandboxRun, Evidence | `/repo/checks/run` | Docker argv, timeout, compatibility and prod/staging settings tests | Backend uses argv lists, minimal env, source RO plus bounded working tmpfs, `--rm`, `--network=none`, `--read-only`, `/tmp:rw,nosuid,nodev,size=64m,mode=1777`, dropped capabilities, no-new-privileges, pids/memory/cpu limits, UID 10001 and `/workspace`; timeout reads the cidfile and kills that exact container ID by argv | tested | Docker remains local/CI-only and production-like construction requires Kubernetes |
| SBOX-017 | Docker sandbox live smoke proves runtime isolation when enabled | `scripts/dev/live_docker_sandbox_smoke.py`, `.github/workflows/live.yml`, `Makefile`, `.env.example` | SandboxRun, Evidence | `/repo/checks/run` | env-gated live smoke, live workflow wiring, isolation config checker | The enabled scratch smoke completed against the Docker backend and proved outbound network denial, outside-workspace write denial, artifact capture, timeout/process kill and exact CPU/memory/PID limits. Its subprocess reader now decodes hostile bytes with bounded UTF-8 replacement; the unique scratch tag/container was removed and the pre-existing acceptance image was preserved | tested | This is local Docker Desktop evidence, not Kubernetes sandbox or production-node evidence; the remote live job remains blocked by Actions billing |
| SBOX-018 | Git inspection resists hidden-index, control-surface, attribute, and patch bypasses | `infra/docker/sandbox_git_inspector.py`, `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/tests/test_sandbox_git_inspector.py`, `scripts/ci/check_sandbox_isolation_config.py` | SandboxRun, Evidence | `/repo/checks/run` | behavioral scratch-repository preflight/bypass tests and static gate negatives | Before Git, scratch tests reject HEAD/index gitlinks, missing-index and index-only/case-variant `.gitmodules`, unmerged stages, `.git/modules`, submodule/include config, BOM config, ignored nested `.git` directories/gitfiles, alternates/http-alternates, info/exclude and core excludes/attributes files. Safe config keys remain inventoried without values and enforce exact 2,000-key/4,096-character limits. The reconstructed stat-zero private index detects same-size/restored-mtime changes, repeated inspection preserves every source `.git` byte/hash, and canonical NUL/fixed-prefix/full-index/text evidence is discarded on any error | tested | Packed/unsupported/split index/object formats fail closed; conservative FSMN detection may safely deny crafted false positives |
| SBOX-019 | Workspace/control streams and descendants have exact aggregate bounds | `apps/api/src/hallu_defense/services/sandbox.py`, `apps/api/src/hallu_defense/services/sandbox_exec.py`, `infra/docker/sandbox_workspace.py`, `infra/docker/sandbox_runner.py`, `infra/docker/sandbox_batch_runner.py`, `apps/api/tests/test_sandbox_workspace_boundaries.py`, `apps/api/tests/test_sandbox_path_safety.py` | SandboxRun, Evidence | `/repo/checks/run` | exact-boundary, zero-byte, path/count, streaming, identity-race, Windows-ctime, policy-race and process-tree tests | Tests prove exact caps pass/+1 fails, zero-byte edges remain valid, both artifact roots share cumulative content/path budgets, walkers use bounded scandir, copy/hash reject identity/mode/size swaps, cross-API Windows ctime is ignored while same-descriptor mode/size/time drift is detected, and policy parsing is fingerprint-bracketed. Windows CLIs are created suspended, Job-assigned and resumed; POSIX uses process groups. Success/timeout, drain-failure, persistent-termination and non-consuming-stdin tests bound wall time and leave no descendant or pipe thread; assignment/termination errors propagate | tested | POSIX modes are normalized for Docker Desktop compatibility; direct workspace executable dispatch is rejected, and OS/container deadlines bound hostile latency |
| SBOX-020 | Kubernetes cleanup is UID-conditioned, foreground, and independently budgeted | `apps/api/src/hallu_defense/config.py`, `apps/api/src/hallu_defense/services/sandbox_kubernetes.py`, `apps/api/tests/test_sandbox_kubernetes_backend.py`, `.env.example`, `docs/deployment/kubernetes-sandbox-jobs.md`, `scripts/ci/check_sandbox_isolation_config.py` | SandboxRun | `/repo/checks/run` | create ambiguity, UID/retry/replacement/Pod-owner, real-clock/default/boundary and dual-error tests | Tests prove delayed/transient/invalid create reconciliation, managed UID validation, no name-only delete, retry-safe UID preconditions, Foreground, Job 404 plus old-owner-Pod absence, replacement safety, and bounded primary+cleanup diagnostics. Kubernetes cleanup defaults to 20 seconds, validates 15–30, remains independent of Docker's 2 seconds, and a real-clock regression completes after that old two-second threshold | tested | Real foreground GC and zero residual Jobs/Pods remain a kind/live requirement for front D; no Helm file is changed in this slice |
| OBS-001 | OpenTelemetry traces | `services/telemetry.py`, API dependencies/middleware/routes, `services/orchestrator.py`, `apps/api/pyproject.toml`, `infra/otel/otel-collector-config.yaml`, `docker-compose.yml`, `.env.example`, `scripts/dev/live_otel_export_check.py` | all | all HTTP endpoints plus verification/policy/sandbox flows | HTTP and domain span tests, lint/typecheck/API tests, compose config validation, opt-in live export smoke | OpenTelemetry SDK/exporter dependencies installed; focused tests prove HTTP spans capture method, URL path, route, status, outcome, duration, and existing trace ID; domain tests prove verification stage spans, policy decision spans, and sandbox execution spans record low-cardinality counts/results while excluding tenant/payload/source-ref/repo/command/secret attributes; full API tests, lint, typecheck, schema, OpenAPI, eval smoke, and secret scan pass; `docker compose config --quiet` passed and Compose smoke started the API plus local OTel collector; Batch 4 adds collector JSONL file export with rotation plus `live_otel_export_check.py`, whose tests prove HTTP/domain span assertions and leak rejection | tested | Deeper external backend spans for live OpenSearch/pgvector runtime calls remain pending; enabled collector-file live smoke requires the live workflow or a Docker-enabled host |
| OBS-002 | Prometheus metrics | `services/metrics.py`, API middleware/routes/orchestrator/policy/sandbox/approvals/ingestion/evals, `infra/prometheus/*.yml`, `infra/grafana/**`, `docker-compose.yml` | metrics | `/metrics`, all HTTP endpoints and core decision paths | endpoint/domain/eval/ingestion metric tests, scrape-auth and live observability gates | `/metrics` includes HTTP, verification, verdict, policy, approval, sandbox, ingestion-job and runtime eval gauges; production scrape uses a SecretManager-backed bearer token and `credentials_file` | tested | Metrics are in-process and reset on restart; enabled Prometheus/Grafana live smoke remains deployment evidence |
| OBS-003 | Grafana dashboards | `infra/grafana/dashboards/hallu-defense-overview.json`, `infra/grafana/provisioning/**`, `scripts/ci/check_grafana_dashboards.py`, `docker-compose.yml` | metrics | n/a | dashboard lint, CI hook | Dashboard lint validates 1 Grafana dashboard with 11 panels covering HTTP, verification, claim verdict, policy, approval, and sandbox metrics; CI and Makefile run `check_grafana_dashboards.py`; Compose smoke started Grafana and `GET /api/health` returned 200 | tested | Browser-level dashboard panel rendering remains future validation |
| OBS-004 | OTel collector file exporter with rotation | `infra/otel/otel-collector-config.yaml`, `docker-compose.yml`, `scripts/ci/check_local_runtime_config.py`, `apps/api/tests/test_local_runtime_config.py`, `scripts/ci/check_observability_config.py`, `apps/api/tests/test_observability_config.py`, `Makefile`, `.github/workflows/live.yml` | n/a | n/a | local runtime config gate, observability config gate, focused negative tests, opt-in live smoke | Collector traces pipeline exports both `debug` and `file`; file exporter writes JSON to `/otel-output/spans.jsonl` with rotation and Compose mounts `./var/otel:/otel-output`; focused observability/local-runtime tests pass and `check_local_runtime_config.py` plus `check_observability_config.py` validate the wiring | tested | Static gates prove config shape; runtime file creation is asserted only by the opt-in live job or a Docker-enabled local run |
| OBS-005 | Live OTel export leak guard | `scripts/dev/live_otel_export_check.py`, `apps/api/tests/test_live_otel_export_check.py`, `.github/workflows/live.yml`, `Makefile`, `.env.example` | n/a | `/verification/run`, `/policy/evaluate`, `/repo/checks/run` | env-gated live script tests, skip behavior, live workflow wiring, sensitive leak negative tests | `live_otel_export_check.py` is disabled unless `HALLU_DEFENSE_LIVE_OTEL_EXPORT_CHECK_ENABLED=true`; tests prove it generates API traffic, polls rotated JSONL spans, requires HTTP plus verification/policy/sandbox domain spans, and rejects sensitive attributes, payloads, secrets, message markers, and tenant leaks | tested | The enabled live check depends on API plus OTel collector writing `var/otel/spans.jsonl`; local default execution is a clean skip |
| OBS-006 | Live Prometheus and Grafana observability smoke | `scripts/dev/live_observability_smoke.py`, `apps/api/tests/test_live_observability_smoke.py`, `scripts/ci/check_observability_config.py`, `apps/api/tests/test_observability_config.py`, `.github/workflows/live.yml`, `Makefile`, `.env.example` | metrics | `/verification/run`, Prometheus API, Grafana API | env-gated live script tests, Prometheus/Grafana negative tests, config gate, live workflow wiring | `live_observability_smoke.py` is disabled unless `HALLU_DEFENSE_LIVE_OBSERVABILITY_SMOKE_ENABLED=true`; tests prove Prometheus target health checking, verification load generation, positive `hallu_http_requests_total` and `hallu_verification_*` queries, Grafana health/datasource checks, and password redaction on failures | tested | The enabled live smoke depends on API, Prometheus, and Grafana running together; default local execution is a clean skip |
| EVAL-001 | Golden sets exist | `evals/golden_sets` | n/a | n/a | eval smoke, expanded scenario tests | `smoke.json` plus `scenarios.json` cover 2 smoke scenarios and 21 expanded offline scenarios: partially false document answers, contradictory document sources, false test-result claims, false repo file/function claims with sandbox inspection evidence, semantic implementation/fix claims with changed-line and command evidence, invalid tool input, high-risk tool approval, secret redaction, contradictory tool output, policy-attribute prompt injection/data poisoning, content-level direct prompt injection, content-level indirect prompt injection in documents, content-level data poisoning in documents, sandbox path traversal, destructive command abuse, and denied-network abuse | tested | Broader adversarial coverage remains pending for richer semantic analyzers and live-service eval history |
| EVAL-002 | Metrics measured | evals/console/runtime report API | EvalSmokeReport, EvalScenarioReport, EvalScenarioHistoryReport, EvalReport | `/evals/reports/publish`, `/metrics` | eval runner, console parser, thresholds, calibration and runtime publish tests | Smoke/scenario runners measure the required quality, safety, latency and cost metrics; thresholds are enforced, calibration is drift-gated, console renders real artifacts, and published runtime summaries emit Prometheus gauges | tested | Console remains offline-file mode and live statistical calibration/deployed publish remain future work |
| EVAL-003 | Versioned enforced eval thresholds | `evals/config/thresholds.json`, `evals/runners/thresholds.py`, `evals/runners/smoke.py`, `evals/runners/scenarios.py`, `scripts/ci/check_eval_thresholds_config.py`, `apps/api/tests/test_eval_thresholds_loader.py`, `apps/api/tests/test_eval_runner_thresholds.py`, `apps/api/tests/test_eval_thresholds_config.py` | n/a | n/a | threshold loader tests, runner regression tests, anti-weakening gate tests | `thresholds.json` version `eval-thresholds.v1` defines smoke/scenario min/max gates for hardcoded and previously ungated groundedness, faithfulness, cost, category, and guardrail metrics; runners delegate `_metric_failures` to the shared threshold loader; focused tests cover missing metrics, category floors, runner regression failures, weakened floors/ceilings, missing required metrics, wrong schema version, and missing Makefile/CI/evals workflow wiring | tested | Thresholds are calibrated to current deterministic golden-set behavior; broader statistical calibration and live eval ingestion remain separate Batch 5 slices |
| EVAL-004 | Reproducible verifier calibration | `scripts/dev/generate_verifier_calibration.py`, `evals/reports/verifier-calibration.json`, `scripts/ci/check_verifier_calibration.py`, `apps/api/tests/test_verifier_calibration.py`, `apps/api/tests/test_verifier_calibration_config.py` | ClaimVerdict, Evidence | n/a | calibration generator tests, drift gate tests, Makefile/CI/evals workflow wiring | Calibration generator writes deterministic `verifier-calibration.v1` JSON over fixed verifier fixtures without timestamps; committed artifact covers supported, partial, contradicted, not-found, and out-of-scope verdicts; drift gate regenerates and diffs the artifact; focused tests and `check_verifier_calibration.py` passed | tested | Calibration uses deterministic local fixtures, not live statistical sampling |
| EVAL-005 | Eval reports persist at runtime and publish safety metrics to Prometheus/Grafana | `infra/rag/pgvector/005_eval_reports.sql`, `apps/api/src/hallu_defense/services/eval_reports.py`, `apps/api/src/hallu_defense/services/metrics.py`, `scripts/dev/publish_eval_reports.py`, `scripts/ci/check_eval_ingestion_config.py`, `infra/grafana/dashboards/hallu-defense-overview.json` | EvalReport*, AuditEvent | `/evals/reports/publish`, `/evals/reports/list`, `/metrics` | storage parity, API/RBAC/audit, metrics, dashboard/config, env-gated live publish tests | Focused eval-report/config tests prove memory/JSONL/PostgreSQL adapter behavior, tenant isolation, production fail-closed storage, publish/list RBAC, audit events and `hallu_eval_*` gauges; Grafana and ingestion config gates pass | tested | Live publish against deployed PostgreSQL/Prometheus remains env-gated; console continues to read offline artifacts rather than this API |
| CI-001 | Python lint gate | Makefile/CI | n/a | n/a | ruff | `ruff check apps/api/src apps/api/tests scripts evals` passed after adding the OpenAPI drift gate | tested | Add CI job invocation of Makefile when make is available |
| CI-002 | Python typecheck gate | Makefile/CI | n/a | n/a | mypy | `mypy apps/api/src` passed with no issues in 37 source files after adding the OpenAPI drift gate | tested | Mypy added to API dev dependencies |
| CI-003 | Python test lanes fail closed on runtime skips and warnings | `Makefile`, `.github/workflows/{ci,release,security,live}.yml`, `apps/api/pyproject.toml`, `apps/api/tests/conftest.py`, `scripts/ci/test_lanes.py`, platform/live tests | n/a | n/a | pytest auto, POSIX, Windows, and live lanes | The prior 28 skips were classified into one dependency-simulation defect, 20 POSIX/Linux cases, and seven live cases. Explicit strict markers now deselect incompatible lanes before execution and any remaining runtime skip fails the session. Starlette TestClient now selects `httpx2` directly; strict evidence is `python -W error -m pytest apps/api/tests --suite-lane=auto`: `2716 passed, 27 deselected, 0 skipped, 0 warnings`. Windows lane passed 4 tests; WSL POSIX 20; Gitleaks live 6; pinned SeaweedFS S3 live 1 | tested | The seven live tests are intentionally selected only in provisioned live jobs, while incompatible platform cases are reported as deselected rather than falsely executed |
| CI-004 | TypeScript typecheck/tests/build/e2e gates | `Makefile`, `.github/workflows/ci.yml`, workspace scripts | TS contracts | n/a | npm commands, production artifact checker, Playwright browser flow | The current tree passes TypeScript typecheck/build across all workspaces, SDK 17 tests, agent-adapters 11, MCP 41 and Console 262; the Next 16.2.11 optimized build passes and its production checker scans 339 artifacts. The public marketing gates tracked by CI-033 are green locally across Chromium, Firefox and WebKit | tested | No current live Keycloak/OIDC browser execution or remote CI run is claimed. Exact pinned Node/npm and remote lanes remain authoritative for environment-specific acceptance |
| CI-005 | OpenAPI validation gate | `scripts/ci/export_openapi.py`, `scripts/ci/check_openapi.py`, `apps/api/tests/test_openapi_ci.py`, `Makefile`, `.github/workflows/ci.yml`, `docs/api/openapi.yaml`, `apps/api/pyproject.toml` | OpenAPI | all | export_openapi, check_openapi drift gate, focused checker tests | `scripts/ci/export_openapi.py` writes deterministic YAML to `docs/api/openapi.yaml` with explicit `pyyaml` dev dependency; `scripts/ci/check_openapi.py` compares the committed artifact against freshly generated OpenAPI and fails with a unified diff on drift; the CI backend exposes the repository's exact `apps/api/src` through `PYTHONPATH` so standalone OpenAPI and later API-aware gates resolve `hallu_defense` consistently on clean Linux; the focused test enforces that workflow contract and all 6 OpenAPI tests pass | tested | The replacement GitHub backend run remains the authoritative clean-run evidence; deeper semantic API compatibility checks remain future work |
| CI-006 | JSON Schema validation gate | scripts/Makefile | JSON Schema | n/a | check_json_schemas | `check_json_schemas.py` validated 79 schemas, 79 valid examples, 79 invalid examples, and 72 TypeScript interfaces | tested | Uses Python `jsonschema` dev dependency; semantic code generation still pending |
| CI-007 | OPA/Rego tests gate | Makefile/CI/scripts/opa | policy | policy | policy-test | `run_policy_tests.py` selected 164 integrated Python policy/config tests and passed; local OPA 1.7.1 executed strict check plus `opa test infra/opa` with `31/31` passing; CI and the API image pin OPA 1.18.2, whose static image binary is rebuilt reproducibly with Go 1.26.5 | tested | Local OPA differs from the CI pin, so the pinned remote run remains the version-authoritative evidence |
| CI-008 | Security tests/gates | scripts/CI | n/a | n/a | secret scan, auth config, OIDC provider smoke, encryption config, secrets config, audit ledger config, approval queue config, corpus grants config, backup retention config, RAG persistence config, OpenSearch bootstrap dry-run, sandbox isolation config, Python dependency audit, npm audit, container scan config, observability config | Current `make security-check` passes: pinned Gitleaks 8.30.1 scans snapshot and full history with 79 exact synthetic fingerprints, the secondary scan is clean, and Python locked dependencies report no known vulnerabilities. The full npm audit has two acknowledged moderate Hono/MCP development-path advisories but is clean at the enforced HIGH threshold; the production-only audit reports zero vulnerabilities. Next 16.2.11, Sharp 0.35.3 and fast-uri 3.1.4 close the newly published HIGH findings. Trivy 0.72.0 rebuilt/scanned all ten first-party images plus immutable Prometheus/Redis; OpenSearch and SeaweedFS findings were fixed and all twelve final images report zero HIGH/CRITICAL | tested | GitHub Actions has not run the final commit because billing blocks allocation before a runner starts. The moderate Hono issue has only a breaking MCP SDK remediation and must remain tracked; live external OIDC, managed services and production deployment evidence remain separate |
| CI-009 | SDK/API and MCP/API live contract gates | `packages/sdk/tests/live-api.test.ts`, `packages/mcp-server/tests/server.test.ts`, `.github/workflows/ci.yml` | all TS/API contracts | `/verification/run`, `/documents/ingest`, `/rag/corpus-grants/upsert`, `/rag/corpus-grants/disable`, `/rag/corpus-grants/list`, `/rag/corpus-grants/history`, `/rag/corpus-grants/history/diff`, `/audit/export`, MCP `tools/*` | live contract tests in TypeScript CI | `npm run test` starts FastAPI and verifies SDK/API including corpus grant upsert/disable/list/history/history-diff with `actor_id` filter plus all 8 MCP tools for trace, tenant, audit, and positive/negative schema behavior | tested | CI now installs API in TypeScript job; tests still use in-memory services |
| CI-010 | Grafana dashboard lint gate | `scripts/ci/check_grafana_dashboards.py`, `.github/workflows/ci.yml`, `Makefile`, `infra/grafana/**` | metrics | n/a | dashboard lint | `check_grafana_dashboards.py` validates dashboard JSON, provisioning references, Prometheus datasource use, required panels, required metric coverage, and forbidden sensitive query terms; Compose smoke started Grafana and `/api/health` returned 200 | tested | Browser-level dashboard rendering remains future validation |
| CI-011 | Backup/retention runtime config gate | `scripts/ci/check_backup_retention_config.py`, `Makefile`, `.github/workflows/*.yml`, `infra/security/backup-retention-policy.json`, `services/data_lifecycle.py`, `scripts/dev/backup_restore_drill.py` | n/a | n/a | policy/runtime wiring validator, focused negative and lifecycle/drill tests | Gate requires lifecycle, retention and encrypted drill scripts plus CI/live wiring; focused config, lifecycle and backup-drill tests pass | tested | Gate and fakes do not replace an enabled live backup/restore/deletion run |
| CI-012 | RAG persistence config gate | `scripts/ci/check_rag_persistence_config.py`, `scripts/dev/bootstrap_opensearch_template.py`, `scripts/dev/live_opensearch_rag_smoke.py`, `scripts/dev/live_pgvector_rag_smoke.py`, `apps/api/tests/test_rag_persistence_config.py`, `apps/api/tests/test_opensearch_bootstrap.py`, `Makefile`, `.github/workflows/*.yml`, `infra/rag/**`, `docker-compose.yml` | n/a | n/a | config validator, bootstrap dry-run, focused negative tests | `check_rag_persistence_config.py` validates pgvector migration, OpenSearch template, Compose backend wiring, Compose-aligned Postgres DSN example, Makefile target, CI/security workflow wiring, bootstrap dry-run wiring, opt-in live OpenSearch smoke wiring, and opt-in live pgvector smoke wiring; `bootstrap_opensearch_template.py --dry-run` validates the OpenSearch template without network; focused tests cover current artifacts plus tenant PK, tenant filter metadata, pinned image, Compose backend, Makefile, CI, bootstrap dry-run, live-smoke safety markers, acknowledged installation, and unacknowledged installation failure cases | tested | Validator and dry-run check static artifacts and request construction; live smokes remain opt-in/local-service and managed-service validation remains pending |
| CI-013 | Audit ledger and PostgreSQL migration integrity gates | `scripts/ci/check_audit_ledger_config.py`, `scripts/ci/check_postgres_migrations.py`, `apps/api/tests/test_audit_ledger_config.py`, `apps/api/tests/test_apply_postgres_migrations.py`, `apps/api/tests/test_postgres_migrations_config.py`, `infra/rag/pgvector/013_audit_history_integrity.sql`, `docs/security/audit-ledger.md`, `docs/rag/postgres-migrations.md` | n/a | n/a | config validator, exact marker/negative tests, applier checksum/idempotency tests | Gates require PostgreSQL-only production, atomic pair/replay boundaries, coherent `REPEATABLE READ, READ ONLY` snapshots, deep-copy ownership, at-most-two replay candidate lookup with typed conflict, legacy pair/triple reconciliation, checksum format/NOT NULL, exact validated constraints, drift-correcting indexes, no audit-row deletion, and CI/security wiring | tested | Gates and fakes do not replace executing 013 against representative live PostgreSQL data and measuring its locks |
| CI-014 | Approval queue config gate | `scripts/ci/check_approval_queue_config.py`, `apps/api/tests/test_approval_queue_config.py`, `apps/api/tests/test_approval_queue.py`, `Makefile`, `.github/workflows/*.yml`, `docs/security/approvals.md`, `infra/rag/pgvector/004_approval_queue.sql`, `scripts/dev/apply_postgres_migrations.py` | n/a | n/a | config validator, focused negative tests | `check_approval_queue_config.py` validates approval env/TTL, persistent-backend and factory wiring, immutable trusted definitions, v3 binding/capability documentation and CI/security wiring; approval/Postgres/OIDC tests cover issuance, atomic single-use consume, tamper, tenant and subject binding | tested | Static/config evidence does not replace a live multi-worker PostgreSQL/Vault run |
| CI-015 | Auth/RBAC config gate | `scripts/ci/check_auth_config.py`, `scripts/ci/oidc_provider_smoke.py`, `apps/api/tests/test_auth_config.py`, `apps/api/tests/test_oidc_provider_smoke.py`, `apps/api/tests/test_oidc_route_auth.py`, `infra/security/auth-policy.json`, `infra/prometheus/prometheus.prod.yml`, `Makefile`, `.github/workflows/*.yml`, `docs/security/auth-rbac.md`, `infra/security/keycloak/realm-hallu-defense.json`, `scripts/dev/live_keycloak_oidc_smoke.py`, `.github/workflows/live.yml` | n/a | all guarded endpoints plus `/metrics` | config validator, focused negative tests, OIDC route auth tests, optional deployed-provider smoke, Prometheus scrape-auth config | `check_auth_config.py` validates auth policy, `.env.example`, docs, production/staging fail-closed settings, OIDC issuer/audience/JWKS source requirements, remote JWKS URL/discovery/cache env keys, deployed-provider smoke env keys, signed-headers trusted-gateway support, endpoint role matrix wiring, Makefile target, CI/security workflow wiring, and Batch 4 authenticated `/metrics` scrape config; focused tests cover current artifacts plus production auth disabled, production unsigned claims, missing OIDC config, OIDC path and remote URL production acceptance, insecure production JWKS URL rejection, policy regression, missing CI wiring, missing smoke CI wiring, smoke skip, smoke missing JWT, smoke discovery/JWKS/JWT success, smoke expected-claim mismatch, insecure smoke URL rejection, OIDC route verifier/auditor role enforcement, route tenant mismatch rejection, audit tenant propagation from verified JWT claims, metrics bearer token success/failure, metrics-reader fallback, non-metrics bearer denial, and production fail-closed metrics secret settings; Batch 2 adds a runnable live provider (SEC-014): the local Keycloak realm and env-gated `live_keycloak_oidc_smoke.py` exercise the same RS256/JWKS path and the `.github/workflows/live.yml` keycloak-live job runs the live smoke in CI | tested | Gate validates local policy/config artifacts; live deployed-provider smoke execution requires external provider URL/discovery and a short-lived test JWT; the local Keycloak realm now gives a self-hosted live-provider option whose token mint was verified independently; deployed Prometheus scrape secret provisioning/rotation remains operational work |
| CI-016 | Corpus grants config gate | `scripts/ci/check_corpus_grants_config.py`, `apps/api/tests/test_corpus_grants_config.py`, `Makefile`, `.github/workflows/*.yml`, `docs/security/auth-rbac.md`, `infra/rag/pgvector/002_rag_corpus_grants.sql` | n/a | `/rag/corpus-grants/upsert`, `/rag/corpus-grants/disable`, `/rag/corpus-grants/list`, `/rag/corpus-grants/history`, `/rag/corpus-grants/history/diff` | config validator, focused negative tests | `check_corpus_grants_config.py` validates corpus grant env keys, docs, settings, PostgreSQL migration, fail-closed storage behavior snippets, pagination/lifecycle/history/history-diff support, actor/time history filter snippets, optimistic concurrency snippets, psycopg DSN runtime wiring, Compose-aligned Postgres DSN docs, API dependency factory wiring, role matrix entries, Makefile target, and CI/security workflow wiring; focused corpus grant/config tests: 7 passed for the config gate and broader corpus grant tests remain covered by the full Python suite | tested | Gate validates adapter/migration/runtime DSN config shape; pooled PostgreSQL connection lifecycle remains deployment work |
| CI-017 | Traceability matrix quality and plan-coverage gate | `scripts/ci/check_traceability_matrix.py`, `apps/api/tests/test_traceability_matrix.py`, `Makefile`, `.github/workflows/ci.yml`, `docs/PLAN_MASTER.md`, `docs/TRACEABILITY_MATRIX.md` | n/a | n/a | table/evidence validation, shorthand plan-ID expansion and missing-ID negative tests | Gate validates 197 rows and fails when a requirement ID declared by PLAN_MASTER is absent; focused tests pass | tested | The pre-audit gate passed 194 rows while three brief-only IDs were absent from both plan and matrix; unnumbered or external brief scope still requires independent semantic review |
| CI-018 | Worklog quality gate | `scripts/ci/check_worklog.py`, `apps/api/tests/test_worklog.py`, `Makefile`, `.github/workflows/ci.yml`, `docs/WORKLOG.md` | n/a | n/a | worklog validator, focused negative tests | `check_worklog.py` validates the committed dated entries; focused worklog and traceability tests: 14 passed; CI backend job and Makefile run `check_worklog.py` | tested | Static gate enforces heading/latest-entry health only; historical semantic completeness still needs review |
| CI-019 | Foundation docs quality gate | `scripts/ci/check_foundation_docs.py`, `apps/api/tests/test_foundation_docs.py`, `Makefile`, `.github/workflows/ci.yml`, `AGENTS.md`, `docs/PLAN_MASTER.md`, `docs/ADR/*.md` | n/a | n/a | foundation docs validator, focused negative tests | `check_foundation_docs.py` validates 7 ADR files plus AGENTS and master plan required markers; `test_foundation_docs.py`: 6 passed; CI backend job and Makefile run `check_foundation_docs.py` | tested | Static gate validates required durable-doc structure, not full semantic adequacy of every architectural decision |
| CI-020 | Foundation infrastructure quality gate | `scripts/ci/check_foundation_infra.py`, `apps/api/tests/test_foundation_infra.py`, `Makefile`, `.github/workflows/ci.yml`, `package.json`, `apps/api/pyproject.toml` | n/a | n/a | foundation infrastructure validator, focused negative tests | `check_foundation_infra.py` validates 29 required paths, 16 Makefile targets, 3 workflow files, root Node workspaces, API Python metadata, and CI wiring; `test_foundation_infra.py`: 7 passed; CI backend job and Makefile run `check_foundation_infra.py` | tested | Static gate validates repo infrastructure wiring, not runtime execution of every target or remote GitHub Actions result |
| CI-021 | Local runtime Compose quality gate | `scripts/ci/check_local_runtime_config.py`, `apps/api/tests/test_local_runtime_config.py`, `docker-compose.yml`, `infra/prometheus/prometheus.yml`, `infra/otel/otel-collector-config.yaml`, `infra/grafana/**`, `Makefile`, `.github/workflows/ci.yml` | n/a | n/a | local runtime config validator, focused negative tests, compose config smoke | `check_local_runtime_config.py` validates 14 Compose services and 4 named volumes, pinned images/build Dockerfiles, ports, dependencies, local env wiring, Prometheus/OTel/Grafana, Keycloak, Vault and persistent stores; Compose config passes | tested | Enabled Vault, observability and managed-service behavior remains live deployment evidence |
| CI-022 | Live CI lane workflow for Postgres and Keycloak | `.github/workflows/live.yml`, `scripts/dev/live_postgres_persistence_smoke.py`, `scripts/dev/live_keycloak_oidc_smoke.py`, `scripts/dev/live_console_oidc_smoke.mjs`, `infra/security/keycloak/realm-hallu-defense.json`, `docker-compose.yml` | n/a | n/a | live workflow wiring, live Postgres persistence smoke, API OIDC smoke, browser Console OIDC/BFF smoke | The isolated `keycloak-live` job now pins Node/npm, installs/builds Console dependencies, installs Chromium, starts the OIDC Console, runs the boolean-only browser smoke against Keycloak plus a loopback API stub without CORS, and tears down both API and Console processes before removing Keycloak; the smoke remains explicit opt-in and its disabled path exits successfully | tested | Workflow wiring and skip path are local evidence; enabled GitHub Actions execution remains integration evidence until observed |
| CI-023 | Sandbox isolation CI and live wiring | `scripts/ci/check_sandbox_isolation_config.py`, `apps/api/tests/test_sandbox_isolation_config.py`, `scripts/ci/check_container_scan_config.py`, `apps/api/tests/test_container_scan_config.py`, `.github/workflows/security.yml`, `.github/workflows/live.yml`, `Makefile` | n/a | n/a | isolation validator negatives, exact ten-image matrix tests, security workflow and sandbox live job | Isolation retains Docker/tmpfs/path/process/Git/Kubernetes UID and foreground-cleanup invariants; the integrated workflow validator additionally proves exactly one sandbox row inside the serialized ten-image matrix, its exact Dockerfile/tag and fail-closed Trivy inputs, while focused mutations reject missing or weakened boundaries | tested | Static gates do not replace real Trivy results or the enabled Docker/Kind cleanup proofs |
| CI-024 | Observability config and live workflow gate | `scripts/ci/check_observability_config.py`, `apps/api/tests/test_observability_config.py`, `scripts/dev/live_otel_export_check.py`, `scripts/dev/live_observability_smoke.py`, `infra/prometheus/prometheus.prod.yml`, `infra/otel/otel-collector-config.yaml`, `docker-compose.yml`, `Makefile`, `.github/workflows/ci.yml`, `.github/workflows/security.yml`, `.github/workflows/live.yml` | n/a | `/metrics`, `/verification/run`, `/policy/evaluate`, `/repo/checks/run` | observability config validator, focused negative tests, skip-safe live scripts, live workflow wiring | The gate validates OTel file export/rotation, Compose mount, and two exact production scrape contracts: API HTTPS with its materialized bearer and Console HTTP on the real port 3000 with a distinct file-backed bearer, exact target, and no inline credentials. Marketing/Helm gates require the Console source, alert rule, and explicit metrics-scraper NetworkPolicy peer together. The focused observability/profile/marketing selection passed 147 tests | tested | Static gates prove coherent source/rule wiring, not a deployed Prometheus `up==1`. Console HTTP is restricted by bearer and NetworkPolicy but is not transport encryption; target environments requiring encrypted east-west traffic must supply and validate a real TLS/mTLS listener. Enabled observability smoke remains external/live evidence |
| CI-025 | Eval thresholds anti-weakening config gate | `scripts/ci/check_eval_thresholds_config.py`, `apps/api/tests/test_eval_thresholds_config.py`, `evals/config/thresholds.json`, `Makefile`, `.github/workflows/ci.yml`, `.github/workflows/evals.yml` | n/a | n/a | config validator, focused negative tests, Makefile/CI/evals workflow wiring | Gate validates threshold schema version, required embedded floors/ceilings, runner threshold-loader usage, `eval-thresholds-config` target, backend CI wiring, and evals workflow wiring; tests prove weakened floors, raised ceilings, removed metrics, missing category floor, wrong schema version, and missing wiring all fail | tested | Gate enforces config rigor; it must run with the eval runners to prove current computed metrics |
| CI-026 | Verifier calibration drift gate | `scripts/ci/check_verifier_calibration.py`, `scripts/dev/generate_verifier_calibration.py`, `evals/reports/verifier-calibration.json`, `Makefile`, `.github/workflows/ci.yml`, `.github/workflows/evals.yml` | n/a | n/a | drift gate, focused config tests, Makefile/CI/evals workflow wiring | `check_verifier_calibration.py` regenerates the deterministic calibration artifact and fails on drift; focused tests cover artifact shape and gate wiring; backend CI, evals workflow, and security-check/eval Makefile wiring include the gate | tested | Gate catches fixture/report drift only; it does not establish live statistical confidence intervals |
| CI-027 | Durable ingestion pipeline and live-recovery config gate | `scripts/ci/check_ingestion_pipeline_config.py`, `apps/api/tests/test_ingestion_pipeline_config.py`, `Makefile`, `.github/workflows/{ci,security,live}.yml`, `scripts/dev/live_ingestion_worker_smoke.py` | n/a | n/a | outbox/runtime/live validator, focused mutations, CI/security/live wiring | Gate pins both migrations, `FOR UPDATE SKIP LOCKED`, lease-token fencing/heartbeat, guarded terminal transitions, dead-letter/backoff, async runtime/worker/backfill invariants and exact cleanup. The real bounded subprocess crash/restart smoke passed against isolated PostgreSQL, including lease expiry/rotation, fencing, tenant isolation and cleanup. CLI details are serialized only for the typed sanitized smoke error; generic exceptions expose type only | tested | Local scratch evidence is not managed PostgreSQL/load evidence; remote live-lane execution is blocked by Actions billing |
| CI-028 | Production profile configuration gate | `scripts/ci/check_prod_profile_config.py`, `apps/api/tests/test_prod_profile_config.py`, `docker-compose.prod.yml`, `docs/deployment/production-profile.md`, `Makefile`, `.github/workflows/ci.yml`, `.github/workflows/security.yml`, `.github/workflows/live.yml` | n/a | n/a | static profile gate, focused negative tests, Docker Compose config render | Gate validates the production overlay, Redis Cluster mode, file-backed credentials and corrected `/run/hallu-defense/kubernetes` ServiceAccount mount; base+prod `docker compose config --quiet` passes. The enabled runner now requires an explicit repo ref instead of an implicit current-directory fallback | tested | The supplied repo ref is not itself immutable evidence. Enabled prod e2e was not run: it requires an authorized disposable target, external OIDC and live dependencies. No production-data mutation is claimed |
| CI-029 | Helm chart and kind smoke gate | `scripts/ci/check_helm_chart.py`, `apps/api/tests/test_helm_chart_config.py`, `scripts/dev/live_kind_helm_smoke.py`, `apps/api/tests/test_live_kind_helm_smoke.py`, `infra/k8s/helm/hallu-defense/**`, `docs/deployment/kubernetes-helm.md`, `Makefile`, `.github/workflows/ci.yml`, `.github/workflows/security.yml`, `.github/workflows/live.yml` | n/a | n/a | Helm chart static/template checker, direct and checker-level malformed-value negatives, env-gated two-revision Kind smoke, exact scratch cleanup | `check_helm_chart.py` passes with Helm lint/template and validates cleanup, migrations, images, workers, secrets, RBAC, NetworkPolicy and admission invariants. The authoritative post-dependency-update `accfinaln98a6bd1` runtime completed two revisions, seven ready/zero-restart workloads, `/console` OIDC contract, exact VIP plus post-DNAT Kubernetes API reachability for API only, fifteen admission denials, sandbox path/timeout/network controls and deterministic zero-residual cleanup | tested | This is a local scratch cluster and not a remote workflow, production CNI, target ingress, managed persistence or real identity-provider result. GitHub Actions remains blocked before runner allocation by billing |
| CI-030 | Reproducible Console production and live OIDC gates | `apps/console/scripts/check-production-build.mjs`, `apps/console/package.json`, `scripts/dev/live_console_oidc_smoke.mjs`, `Makefile`, `.github/workflows/ci.yml`, `.github/workflows/live.yml` | Console runtime boundary | `/auth/*`, `/api/*`, `/console` | production artifact scan, skip-safe/enabled browser smoke, workflow/Make wiring | `make console-check`, `console-build-check`, and `console-oidc-live-smoke` expose the focal gates; at `d6c15bd`, the Console build succeeded, the checker scanned 339 artifacts, and the public production browser phase served the copied `.next/standalone` artifact rather than `next start` | tested | Enabled Keycloak/OIDC browser proof was not run for this candidate. Workflow wiring is not a completed remote result, and the public marketing matrix does not replace the operational Console live smoke |
| CI-031 | Four release stages plus fail-closed final verdict | `.github/workflows/release.yml`, `scripts/ci/check_release_security_config.py`, `apps/api/tests/test_release_security_config.py`, `docs/security/release-process.md`, `Makefile`, `.github/workflows/{ci,security}.yml` | signed source, protected control commit, image archives, checksums, SBOMs, scan and provenance evidence | n/a | exact DAG/permissions/actions, GPG trust, protected-control ancestry, peeled-commit handoff, archive/path/tag/cardinality/config/layer/digest/verdict mutations | The exact `verify-tag -> build-release -> scan-release -> attest-release` stages isolate public tag trust, unprivileged source execution, permission-empty serialized scanning, and final OIDC attestation; both trust boundaries require the peeled signed source to be an ancestor of the exact immutable protected-default-branch dispatch commit; a permission-empty `release-verdict` with `if: always()` fails skipped/cancelled/failed stage runs; immutable artifact IDs/digests, canonical archives, exact RepoTags, checksums, and SBOM subjects are independently revalidated before attestation | tested | Local tests/checkers do not create GitHub attestations or prove current CVE status; enabled signed-tag execution remains remote release evidence |
| CI-032 | Operational release encryption evidence gate | `.github/workflows/verify-release-encryption.yml`, `scripts/ci/check_release_encryption_workflow.py`, `apps/api/tests/test_release_encryption_workflow_config.py`, `Makefile`, `.github/workflows/{ci,security}.yml` | release encryption verification report and replay state | n/a | protected-ref/environment/artifact-ID wiring, unsafe override, permissions and prepare/finalize negatives | The protected workflow downloads one immutable evidence bundle by run/artifact ID, materializes external control inputs in a mode-0700 temporary directory, authenticates the verifier output and exact replay transition, and a final permissions-empty job accepts only an externally anchored compliant finalize report | tested | Prepare intentionally remains non-compliant; live external CAS and deployment attestation remain operator/controller evidence |
| CI-033 | Public-launch browser, accessibility, performance and compatibility gates | `.github/workflows/ci.yml`, `Makefile`, `apps/console/playwright.marketing.config.ts`, `apps/console/e2e-marketing/**`, `apps/console/scripts/run-browserstack-marketing.mjs`, `scripts/ci/check_marketing_compatibility_config.py`, `apps/api/tests/test_marketing_compatibility_config.py` | localized route and demo-request UI contracts | `/`, `/en`, `/privacy`, `/en/privacy`, `/console`, `/demo-request` | production `standalone` three-browser x three-viewport matrix, hydrated-form/Axe matrix, keyboard/reduced-motion/progressive-enhancement/CSP/performance labs and BrowserStack catalog/session gate | Current production phase ran 216 cases with 148 passed, 68 deliberate scope skips and zero failed; expanded form/Axe ran 117 with 77 passed, 40 deliberate skips and zero failed. It covers both languages and all three engines/viewports at initial, step-two and native-invalid states. Eight initial animation/contrast failures were fixed at the CSS root cause and the full phase reran green. Marketing compatibility validation governs this coverage | tested | No BrowserStack credentials, catalog result, session IDs or real minimum-version execution were available. Native browser-UI zoom and manual screen-reader/contrast evidence remain pending; local WebKit is not Safari 16.4 proof |
